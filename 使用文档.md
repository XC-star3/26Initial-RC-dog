# DM02 四足使用与调试文档

本文档按当前代码整理，统一说明实体遥控器操作、USB CDC 维护命令、电机映射、用户零点、单腿/双腿调试、MIT 保护和 VSCode/OpenOCD 烧录。

本文是底层维护和电机调试手册，不是上位机通信协议，也不是比赛操作流程。文档职责如下：

- USB 二进制帧以 `COMMUNICATION_README.md` 为准。
- 上下位机接入查看 `任务赛通信实现.md`。
- 越障阶段、砂砾坑、木桥 B、T 字形台阶逐级上台，以及可选的 IMU/高墙专项查看 `8DOF轮足四足越障分析.md`；下台阶和台阶动作交接不在当前开发范围内。

第一阶段接入 USB 二进制控制后，合法二进制帧必须先被协议解析器消费，不能再逐字节进入本手册所述的单字符命令。正式控制接口保留只读 `p` 和 `Y`（当前兼容 `y`）供人工按需查询；其他单字符命令只用于明确进入的维护/调试模式。

新版四足运动相关调参已经移植回原版：后腿落足补偿为 `15 mm`，四条腿统一使用通用 MIT STAND/SWING PID，小跑偏航修正入口 `DOG_TROT_YAW_TRIM_X_MM` 默认为 `0.0 mm`。SBUS 遥控器、机械臂 DM4310/EL05 和 DMA buffer 等原版功能仍保留。

相关代码入口：

| 内容 | 文件 |
| --- | --- |
| 串口命令 | `User_File/task/control_task.cpp` |
| 电机映射、方向、安全保护 | `User_File/task/motor_task.cpp` |
| 对外接口和保护参数 | `User_File/task/motor_task.h` |
| OpenOCD 配置 | `stm32h743_daplink.cfg` |
| VSCode 任务 | `.vscode/tasks.json` |

## 实体遥控器操作

### ET07 / RF207S 配置

RF207S 的 W.BUS 输出兼容 S.BUS。ET07 进入 `通用功能 -> 辅助通道`，确认：

| 通道 | 控件 | 用途 |
| --- | --- | --- |
| CH1 | 右摇杆横向 | 左右转向 |
| CH2 | 右摇杆纵向 | 普通模式前进/后退；障碍模式仅在 `FAULT` 时拉到底请求回退 |
| CH3 | 左摇杆纵向 | 普通模式轮速/步态档；障碍模式选择障碍 |
| CH4 | 左摇杆横向 | 保留；8DOF 不做真实横移 |
| CH5 | SB 三挡 | 九宫格行 |
| CH6 | V1 | 机械臂 J0 |
| CH7 | V2 | 机械臂 J1 |
| CH8 | SC 三挡 | 九宫格列 |
| CH9 | SD 瞬时开关 | 唯一锁存急停输入 |
| CH10 | SA 两档开关 | 障碍模式单步前进；每次拨动均请求一个前进阶段 |

CH8 默认可能未分配，必须手动设置为 SC。8DOF 每腿只有髋/膝两个关节，足端主要在 x/z 平面运动，不应把 CH4 配成真实横移。

### 九宫格模式速查

| CH5+CH8 | 当前功能 | 轮状态 |
| --- | --- | --- |
| LOW+LOW | 无动作检测、恢复和机械限位许可准备 | OFF |
| LOW+MID | 机械限位低姿态轮行 | DRIVE |
| LOW+HIGH | 腿 RX-only 安全保持 | HOLD |
| MID+LOW | MIT 站立 | HOLD |
| MID+MID | MIT 站立轮行 | DRIVE |
| MID+HIGH | 站立后机械臂 jog | HOLD |
| HIGH+LOW | 纯足步态 | HOLD |
| HIGH+MID | 步态和轮驱同步 | DRIVE |
| HIGH+HIGH | 实体 SBUS 障碍模式 | HOLD |

HOLD 是零轮速主动制动，不是轮轴位置环。当前已通过直角绕杆、限高杆、大斜坡和木桥 A。实体 SBUS 的 `HIGH+HIGH` 已用于第二阶段障碍入口；USB `0x10` 的 mode 8 仍是保留站立 HOLD，不会进入障碍状态机。IMU 接入和高墙状态机均为可选专项，不阻塞当前越障主线。

### 速度和操作手感

- CH1/CH2 超过 `25` 才启动，运行后两轴回到 `12` 内停止。
- 轮驱最大转速按 `max_rpm = clamp(CH3_norm + 100, 0, 200)`。
- CH3 `<=-60` 为 LOW，`>=60` 为 HIGH，中间为 MID。
- LOW/MID/HIGH 普通步态参数分别约为 `1.0/2.0/2.5 Hz`，普通摆高为 `30/60/100 mm`。
- 右摇杆前后控制移动，左右控制偏航；不提供横移。

### HIGH+HIGH 障碍模式

该入口当前只接受实体 SBUS。进入 `HIGH+HIGH` 后轮驱保持 HOLD，固件先完成站立，再确认四轮反馈在线、每轮绝对速度小于 `0.5 rad/s` 并连续保持 `200 ms`，随后执行障碍腿闭环和几何预检查。越障开始后若轮驱失效或 HOLD 状态下轮速重新超限，当前足端阶段不会被直接截断；固件会在阶段结束后回退并保持阻塞，必须退出再重新进入 `HIGH+HIGH`：

| CH3 档位 | 选择 | 当前状态 |
| --- | --- | --- |
| LOW（`norm <= -60`） | 木桥 B | 已实现软件状态机；当前仅允许 50 mm 软缝测试 |
| MID | T 字形台阶 | 已实现 100 mm x 300 mm 三级上台软件状态机；尚未完成实体验收 |
| HIGH（`norm >= 60`） | 砂砾碎木坑 | `UNAVAILABLE`，不执行腿部轨迹 |

CH3 只在动作尚未准备且没有运动时更新选择。木桥 B 和 T 台阶都使用 SA（CH10）离散切换，不是连续速度控制：

- 每次进入 `HIGH+HIGH` 后，需等待上述门控完成；固件只同步 SA 当前档位，不会因初始位置执行动作。
- 状态为 `READY` 时，SA 从 `-100` 拨到 `+100` 或从 `+100` 拨到 `-100` 并连续保持 3 个新鲜 SBUS 帧，才请求前进一步；无需回中，下一次反向拨动就是下一次单步请求。单帧跳变、预检或足端运动期间的拨动不会排队。
- CH2 不再执行正常前进或回退。仅当状态显示 `FAULT`，且故障后已经观察到 CH2 回中时，才接受随后拉至底端（`norm <= -95`，对应遥控器 `-100`）的回退边沿；故障前已经保持在底端不会自动回退。回退后须再次将 CH2 回中，才能在下一次故障中请求回退。
- 动作未完成时新的单步请求不会并行或串接执行。木桥 B 切档后按检查点退回普通站姿；T 台阶切档后完成当前足端分段并保持稳定姿态，中间第 1/2 级不允许退出，重新进入 `HIGH+HIGH + CH3 MID` 后自动续跑尚未完成的本级序列。整个阻塞期间 `can_exit=0`。
- 正常回退完成后若目标是 LOW 档，腿部还会先执行现有受控下降，再进入 LOW；不会从普通站姿直接撤掉腿部闭环。CH9 硬中止不使用这条自动回退/下降路径。

当前仅对 `50 mm` 软缝启用以下参数。足端坐标以机身为参考，`+X` 为前方、`+Z` 为下方；因此准备姿态的 Z 小于普通站姿，表示机身相对降低。不得直接将这些坐标用于正式 `150 mm` 缝。

| 参数 | 当前值 | 说明 |
| --- | ---: | --- |
| 前后轮轴距配置 | `400 mm` | 木桥 B 检查点建模值，仍需本机复核 |
| 轮轴离地高度 | `58 mm` | 机械与验收参考，尚未参与轮缘净空计算 |
| 踏板沿行进方向长度 | `400 mm` | 已确认 |
| 正式缝宽 | `150 mm` | 已确认，当前未启用 |
| 活动缝宽 / 测试上限 | `50 / 50 mm` | 当前唯一允许的软缝档位 |
| 当前踏板节距 | `450 mm` | `400 + 50` |
| 降身量 | `40 mm` | CP0/CP4 相对普通站姿 |
| 跨缝摆动裕量 | `30 mm` | CP1->CP2、CP2->CP3 的前/后腿对轨迹 |
| 准备或恢复普通站姿 | `1500 ms`，超时 `4000 ms` | 四腿同时运动 |
| 四腿整体平移 | `1200 ms`，超时 `3700 ms` | CP0->CP1、CP3->CP4 |
| 前腿对或后腿对跨缝 | `1800 ms`，超时 `4300 ms` | CP1->CP2、CP2->CP3 |
| 轮速门控 | `< 0.5 rad/s` 连续 `200 ms` | 四轮在线且 HOLD 后才允许准备 |

木桥 B 使用 CP0..CP4 五个稳定检查点：

| 检查点 | 前腿 `(X,Z)` | 后腿 `(X,Z)` | 动作含义 |
| --- | --- | --- | --- |
| CP0 | `(+25, 260) mm` | `(-25, 275) mm` | 四腿进入降低 40 mm 的跨缝准备姿态并居中 |
| CP1 | `(-200, 260) mm` | `(-250, 275) mm` | 四腿整体移到当前踏板的跨缝起始位置 |
| CP2 | `(+250, 260) mm` | `(-250, 275) mm` | 前腿对跨过一条缝，摆动轨迹增加 30 mm 高度裕量 |
| CP3 | `(+250, 260) mm` | `(+200, 275) mm` | 后腿对跨过同一条缝，摆动轨迹增加 30 mm 高度裕量 |
| CP4 | `(+25, 260) mm` | `(-25, 275) mm` | 四腿重新居中，`gaps` 增加 1；再次前进会开始下一条缝 |

前进依次执行 CP0 -> CP1 -> CP2 -> CP3 -> CP4，回退按相反方向逐检查点执行；在 CP0 回退会恢复普通站姿。当前几何参数位于 `User_File/task/obstacle_config.h`：前后轮轴距 `400 mm`、踏板沿行进方向长度 `400 mm`、正式缝宽 `150 mm`。轮轴离地高度 `58 mm` 当前只是机械与验收参考，尚未进入检查点或轮缘净空计算。`ACTIVE_GAP` 与编译期 `TEST_GAP_LIMIT` 当前均为 `50 mm`；完成本级验收后才可同时提升测试上限并验证 `100 mm`、`150 mm`，不能跳级。正式 150 mm 下现有 40 mm 降身姿态可能使极限检查点不可达，因此还必须重新计算准备高度，并通过全轨迹 IK、关节限位和实体碰撞检查。

#### T 字形台阶三级上台

该功能只完成从地面逐级上到顶部平台并恢复普通站姿，不包含正常下台阶或顶部动作交接，也不代表已经完整通过比赛中的 T 字形台阶。规则图已明确单侧台阶总进深 `900 mm`、通道宽 `400 mm`、每级踏面深 `300 mm`、阶高 `100 mm`，顶平台 `1000 x 1000 mm`，总长 `2800 mm`；当前纵向建模值与规则图一致。

`400 mm` 是台阶通道横向宽度，不是前后轮轴距。当前 8DOF 状态机只控制足端 X/Z，不提供横向收腿，因此实体启动前必须测量左右轮胎、腿杆和摆动过程的最大外侧包络。包络必须小于 `400 mm` 并留有对中误差余量；若等于或超过 `400 mm`，禁止使用当前正向上台动作，不能依靠遥控修正横向宽度。

进入和触发顺序：

1. 使用防跌落绳，确认整机横向运动包络可进入 `400 mm` 通道，再将前轮轴中心对准第一级立边前约 `58 mm`，机身中心线对准台阶中心线。
2. CH5、CH8 均拨到 HIGH，CH3 保持 MID；等待轮驱 HOLD、四轮在线且绝对轮速 `<0.5 rad/s` 连续 `200 ms`。
3. 保持 SA 在任一档位，用 `p` 或 `Y` 确认 `selected=STAIRS state=READY fault=0 phase=IDLE level=0/3`；首次读取 SA 只同步档位。
4. 拨动 SA 一次。第一次切换先进入紧凑姿态并自动完成第一级；到 `phase=LEVEL_READY level=1/3` 后人工检查落轮位置。
5. 再分别拨动 SA 两次。第二次完成第 2 级；第三次完成第 3 级、继续进入顶平台并恢复普通站姿。最终应为 `phase=TOP_READY level=3/3 can_exit=1`。

足端坐标以机身为参考，`+X` 向前、`+Z` 向下。一级自动序列如下：

| 分段 | 前腿 `(X,Z)` mm | 后腿 `(X,Z)` mm | 时间 |
| --- | --- | --- | ---: |
| 紧凑准备（仅第 1 级） | `(-108,300)` | 先随身移到 `(-108,315)`，再摆到 `(+108,315)` | `1500 + 1500 ms`，随后等待 `300 ms` |
| 前腿抬起 | `(-108,160)` | `(+108,315)` | `1000 ms` |
| 前腿前送 | `(+192,160)` | `(+108,315)` | `1500 ms` |
| 前腿落到上一级 | `(+192,200)` | `(+108,315)` | `1800 ms`，随后等待 `300 ms` |
| 机身前移一个踏面 | `(-108,200)` | `(-192,315)` | `2000 ms` |
| 后腿抬起 | `(-108,200)` | `(-192,175)` | `1000 ms` |
| 后腿前送 | `(-108,200)` | `(+108,175)` | `1500 ms` |
| 后腿落到同一级 | `(-108,200)` | `(+108,215)` | `1800 ms`，随后等待 `300 ms` |
| 四腿伸长抬升机身 | `(-108,300)` | `(+108,315)` | `1800 ms`，随后等待 `300 ms` |

第 3 级完成上述动作后自动执行顶平台序列：前腿前送 `300 mm`，机身前移 `300 mm`，后腿前送 `300 mm`；再将机身前移 `108 mm`，最后把前腿从 `X=-216 mm` 调到 `X=0`。终点四腿恢复普通站姿，前腿 `(0,300) mm`、后腿 `(0,315) mm`。

每个足端分段都使用五次平滑轨迹，并在启动前按 16 个离散区间检查工作空间、IK 和电机用户角限制；每段运行时继续检查支撑腿稳定、编码器新鲜度、MIT 状态、超时，以及 `17.5 A` 持续 `100 ms` 的电流保护。轨迹完成还要求关节误差和速度连续稳定。`300 ms` 只是无落轮传感器条件下的等待时间，不能证明轮胎真实接触台面，操作员必须在每一级 `READY` 后目视确认。

故障时，先保持机器人和防跌落绳稳定，再让 CH2 回中并拉到底（`norm <= -95`）。固件每次故障只接受一次回退请求，只把当前失败一级按已经确认的子阶段逆序恢复到该级起点，不会退下此前已经完成的台阶。稳定级间的后退输入会被忽略。CH9 始终立即取消足端轨迹并建立安全锁存，不执行回退。

配置中的 `33..143 deg` 是大腿与小腿的几何夹角，用于限制轮轴工作半径；它不等于 KNEE 电机经过零点和方向换算后的用户角。当前 KNEE 电机用户角校准范围保持 `-120..143 deg`，两类角度不得混用。

`p` 和 `Y`（兼容 `y`）都会增加一行障碍诊断：

```text
OBSTACLE: request=1 selected=BRIDGE_B state=READY fault=0 prepared=1 cp=2 target=2 gaps=0 motion=0 can_exit=0 gap=50/150mm
OBSTACLE: request=1 selected=STAIRS state=READY fault=0 prepared=1 phase=LEVEL_READY level=1/3 motion=0 can_exit=0 stair=100x300mm
```

木桥 B 行显示当前/目标检查点、已完成缝数及活动/正式缝宽；台阶行显示当前子阶段、已完成级数/总级数和阶高/踏面深度。两者均显示模式请求、状态、故障、准备、足端运动和退出许可。未准备的木桥 `cp/target` 可显示 `255`；当前只有砂砾碎木坑返回 `state=UNAVAILABLE`。

当前实现仍有明确的硬件验收边界：

- 软件构建和逆解检查不能代替实体越障。当前 50 mm 参数尚不代表木桥 B 已通过，更不代表 150 mm 正式缝可用。
- 足端执行器在启动前离散检查整条五次平滑轨迹，运行中任一点 IK 失败即进入故障保持；状态机还检查关节误差、速度、腿电机在线、编码器新鲜、MIT 闭环、故障和超时。双腿摆动期间支撑腿误差/速度持续异常，或任一腿指令电流达到 `17.5 A` 持续 `100 ms`，也会终止阶段。这些保护仍不能替代足端传感器、实测电流落轮阈值、视觉或 IMU 的可靠落板确认。
- 木桥 B 沿行进方向板长 `400 mm` 和正式缝宽 `150 mm` 已确认；本机前后轮轴距 `400 mm`、58 mm 轮轴高度及实际落轮余量仍须在实机复核。软缝应使用可压缩材料，机器人架设防跌落绳，操作员全程握住 CH9，先架空验证 CP0..CP4 和完整回退，再进行 50/100/150 mm 地面测试。
- 每个检查点到达后先用 `p` 或 `Y` 确认 `state=READY`、`fault=0`、`cp=target`，再触发下一步；出现异常立即使用 CH9 或回退，不得连续推杆跳过人工检查。

### 实体安全

- CH9 `norm >= 50` 是唯一会显式立即取消当前足端轨迹并关闭越障请求的输入，同时建立锁存急停；`norm <= 20` 只表示开关释放，不会自动恢复运动。CH9 会丢弃未完成阶段，不执行自动回轨；按 LOW+LOW 解除锁存后，由随后选择的正常模式根据当前关节反馈重新建立站姿。
- 腿安全清锁要求 SBUS 新鲜、CH9 释放、LOW+LOW，以及 8 个腿电机 idle/无故障；不再强制等待四轮反馈。轮控制器保持独立锁止，只有四轮自行满足在线、低速和温度条件后才恢复轮驱。
- SBUS 丢失和一般子系统故障执行局部停止；只有 CH9 建立全机锁存。
- 进入机械危险区、接线和维护前必须物理断开动力电源。

### 机械臂和轮电机接线速查

| 设备 | 总线 | 标识 |
| --- | --- | --- |
| J0 DM4310 | FDCAN1 | 控制 `0x01`；反馈 Master ID 默认 `0x00`，兼容已配置的 `0x10` |
| J1 EL05 | FDCAN2 | 电机 ID `0x7F`，主机 ID `0xFD` |
| 四个 M3508+C620 | FDCAN3 | LF=1、RF=2、RB=3、LB=4，反馈 `0x201..0x204` |

CAN 使用 1 Mbps Classic CAN，总线只在物理两端保留 120 Ohm 终端电阻，控制板和电机电源必须共地。

### 机械臂无运动在线诊断

未进入机械臂模式时，固件每 `300 ms` 对失能的机械臂电机执行一次低频诊断：

- J0 DM4310 向标准帧 ID `0x001` 发送失能指令 `FF FF FF FF FF FF FF FD`。按手册，反馈帧 ID 是 Master ID 且默认 `0x000`；固件同时接受原先调试助手可能配置的 `0x010`。匹配反馈可以更新遥测和故障，但不会把电机标记为已使能或控制在线。
- J1 EL05 发送扩展帧 `0x0000FD7F`，数据为 8 字节全零的“获取设备 ID”请求。只接受通信类型 0、目标 `0xFE`、电机 ID `0x7F` 的应答；设备 ID 应答只确认设备存在，不更新角度、速度、力矩、温度或故障。
- 进入机械臂模式后先同时向 J0/J1 发送使能。每个关节独立等待无故障反馈、发送当前位置设零并确认新鲜反馈回到 `0 deg` 附近；任意一个关节完成即可进入机械臂位置控制，另一个关节继续后台重试。CH6 只控制已就绪的 J0，CH7 只控制已就绪的 J1。J0 保留软件位置/速度串级外环，J1 的 MIT 位置保持默认使用手册示例的 `Kp=1.0, Kd=1.0`，后续仍可通过 `ArmMotor_SetGains()` 调整。CH9/安全锁存期间仍允许上述两类无运动诊断，不发送使能、设零或位置控制帧。

`p` 和 `Y`（兼容 `y`）都会输出机械臂状态，字段含义如下：

| 字段 | 含义 |
| --- | --- |
| `present` | 最近 `1000 ms` 内收到有效诊断应答或正常状态反馈 |
| `enabled` | 主控已经请求使能；这是本地主控状态，不代表电机已确认使能 |
| `online` | 请求使能后存在不超过 `100 ms` 的有效状态反馈 |
| `feedback_age_ms` | 最后一帧真实状态反馈距当前的时间；从未收到时显示 `NA` |
| `fault` | 最后一帧真实状态反馈中的故障码；只有设备 ID 应答时显示 `NA` |

未进入机械臂模式、两个电机均有应答时，典型输出为：

```text
ARM: init=1 diag_period=300ms present_timeout=1000ms feedback_timeout=100ms
  J0 DM4310 CAN1 std feedback_id=0x00 present=1 enabled=0 online=0 feedback_age_ms=12 fault=0 ...
  J1 EL05 CAN2 ext motor_id=0x7F present=1 enabled=0 online=0 feedback_age_ms=NA fault=NA ...
```

## 1. 快速上手

串口使用 USB CDC HS 虚拟串口。串口工具可以填 `115200 8N1`，USB CDC 实际不依赖这个波特率。

命令都是单字符命令，发送后立即生效，不要求回车。`\r`、`\n` 和空格会被忽略。

上电后默认是 `RX-only` 状态：只接收 CAN 反馈和串口命令，不周期发送控制帧。

最常用调试流程：

```text
L/R/B/N  选择腿
2        选择单腿 HIP+KNEE
r        停止周期控制
e        未锁存时清错；安全锁存时仅请求 rearm
o        probe，确认闭环和编码器
p        查看状态
b        MIT boot，设置用户零点并开启 MIT PID
a/d      HIP -/+30 deg
j/l      KNEE -/+30 deg
u/i      HIP+KNEE 同步 -/+30 deg
x        idle 并停止控制
```

安全建议：

- 首次调试让腿悬空或可靠支撑。
- 每次只发一个动作命令，等 `user` 接近 `tgt` 后再发下一次。
- 看到 `PROTECT`、反馈超时或 `enc=0` 时，先将摇杆回中并检查对应电机；CH9/ESTOP 日志仍按硬急停流程处理。
- 当前 MIT 电流限幅是 `18 A`，力矩常数按 `1.2 Nm/A` 换算，理论限幅对应约 `21.6 Nm`；实际输出还受 PID 输出限幅、模式和电机能力约束，不应直接从高负载折叠姿态硬撑起整机。

## 2. 电机映射

当前电机映射位于 `User_File/task/motor_task.cpp` 的 `g_dog_motor_config[]`。

| 电机 | 腿 | 关节 | CAN | ID | 用户角方向 `dir` | 力矩方向 `tdir` | 用户角范围 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| M0 | LF | HIP | CAN1/front | 2 | -1 | +1 | -120 到 +120 deg |
| M1 | LF | KNEE | CAN1/front | 1 | -1 | +1 | -120 到 143 deg |
| M2 | RF | HIP | CAN1/front | 4 | +1 | -1 | -120 到 +120 deg |
| M3 | RF | KNEE | CAN1/front | 3 | +1 | +1 | -120 到 143 deg |
| M4 | LB | HIP | CAN2/rear | 2 | -1 | +1 | -120 到 +120 deg |
| M5 | LB | KNEE | CAN2/rear | 1 | -1 | +1 | -120 到 143 deg |
| M6 | RB | HIP | CAN2/rear | 4 | +1 | -1 | -120 到 +120 deg |
| M7 | RB | KNEE | CAN2/rear | 3 | +1 | -1 | -120 到 143 deg |

说明：

- CAN1 和 CAN2 上的 ID 可以重复，因为代码用 `CAN bus + node_id` 定位电机。
- `LF/RF/LB/RB` 分别表示左前、右前、左后、右后。
- `dir` 只影响 `user_angle` 的正负方向。
- `tdir` 只影响 MIT 扭矩输出方向。
- 如果显示角度方向反了，优先改 `dir`；如果 MIT 用力方向反了，优先改 `tdir`。

## 3. 角度含义

状态输出里最重要的是这三个角度：

| 字段 | 含义 |
| --- | --- |
| `real_angle` | 电机编码器原始角度，单位 deg。它不是用户零点角度，不建议直接用它判断姿态。 |
| `user_angle` / `user` | 扣除用户零点并乘以 `dir` 后的用户角度，单位 deg。调试时主要看这个。 |
| `tgt` | 当前目标用户角度，单位 deg。`a/d/j/l/u/i` 改的就是这个目标。 |

计算关系：

```text
user_angle = (encoder_turn - user_zero_turn) * 360 * dir
```

注意：

- `LEG1s ...` 和 `STAT ...` 只显示当前选中腿的 HIP/KNEE。
- 如果目标范围是前双腿、后双腿或全部电机，必须看 `p` 输出里的 `TARGET ...` 明细行，才能看到所有被选中电机的状态。

## 4. 目标选择

先选腿，再选调试范围。

| 命令 | 作用 |
| --- | --- |
| `L` | 当前腿选择 LF |
| `R` | 当前腿选择 RF |
| `B` | 当前腿选择 LB |
| `N` | 当前腿选择 RB，使用 `N` 是因为 `R` 已用于 RF |
| `1` | 目标范围为当前腿 HIP 单电机 |
| `2` | 目标范围为当前单腿 HIP + KNEE |
| `3` | 目标范围为前双腿 LF + RF |
| `4` | 目标范围为后双腿 LB + RB |
| `8` | 目标范围为全部 8 个电机 |

切换腿或目标范围会自动回到 `RX-only`，停止周期控制发送。

## 5. 串口命令总表

| 命令 | 作用 | 说明 |
| --- | --- | --- |
| `?` | 打印帮助菜单 | 不改变控制状态 |
| `p` | 打印当前状态 | 包含当前腿状态和选中目标明细 |
| `Y` / `y` | 打印完整诊断 | 包含 SBUS、轮腿运动和机械臂 `present/enabled/online` 状态 |
| `r` | RX-only | 停止周期控制、MIT 调试和原始 CAN 日志 |
| `e` | 清错 / 请求 rearm | 未锁存时对当前目标范围内电机发送清错命令；安全锁存时只提交 rearm 请求，不会无条件清错或恢复运动 |
| `m` | 配置位置模式 | `POSITION_CONTROL + TRAPEZOIDAL_CURVE_INPUT` |
| `o` | MIT probe | 进入闭环、读编码器，不打开周期位置控制 |
| `b` | MIT boot | probe、读编码器、设置用户零点、配置 MIT、打开 PID |
| `h` | MIT 示教保持 | 手动拖动电机，松手后保持当前姿态 |
| `a` | HIP 目标 -30 deg | MIT 模式和位置模式共用 30 deg 步进 |
| `d` | HIP 目标 +30 deg | MIT 模式和位置模式共用 30 deg 步进 |
| `j` | KNEE 目标 -30 deg | `1` 单 HIP 模式下无效 |
| `l` | KNEE 目标 +30 deg | `1` 单 HIP 模式下无效 |
| `u` | HIP+KNEE 同步 -30 deg | `1` 单 HIP 模式下只作用于 HIP |
| `i` | HIP+KNEE 同步 +30 deg | `1` 单 HIP 模式下只作用于 HIP |
| `z` | 设置用户零点 | 将当前编码器位置设为 `user_angle = 0` |
| `x` | idle 并停止 TX | 当前目标范围电机进入 idle |
| `c` | 开关原始 CAN RX 日志 | 调 CAN 帧时使用，平时建议关闭 |
| `q` | 查询编码器 | 对在线电机查询 encoder estimate/count，并打印状态 |
| `!` | 全部电机失能 | 停止控制并请求 8 个腿电机进入 idle；不会建立 ESTOP 锁存 |

当前代码参数：

```text
STEP_DEG           = 30 deg
MIT_STEP_LIMIT_DEG = 120 deg
```

也就是说，`a/d/j/l/u/i` 每次改变 30 度；MIT 调试下目标绝对值超过 `120 deg` 会被拒绝并打印 `MIT step rejected`。KNEE 电机用户角配置范围为 `-120 到 143 deg`；大小腿 `33 到 143 deg` 是另一套几何夹角定义，不能直接作为 KNEE 用户角下限。

## 6. `o`、`b`、`h` 的区别

| 命令 | 是否设置用户零点 | 是否周期控制 | 适合场景 |
| --- | --- | --- | --- |
| `o` | 否 | 否 | 只确认闭环、编码器和 `real_angle` 是否正常 |
| `b` | 是 | 是 | 标准 MIT 调试入口，随后用 `a/d/j/l/u/i` 步进 |
| `h` | 否 | 是 | 手动拖动到姿态，松手后保持 |

补充说明：

- `o` 是 probe，不是姿态保持模式。它主要用于让电机进闭环、降低阻力并读编码器，不保证支撑自身姿态。
- `b` 会把当前机械位置设为用户零点，所以发 `b` 后通常会看到 `user_angle` 接近 0。
- `h` 不重设用户零点。它会让目标跟随当前角度，检测到手动拖动停止后冻结目标并用 MIT PID 保持姿态。
- `z` 会重设用户零点和目标，但不等于写入电机内部原始编码器零点。

## 7. 用户零点

用户零点用于把当前机械姿态定义为 `user_angle = 0`。

命令：

```text
z
```

执行条件：

| 条件 | 说明 |
| --- | --- |
| 已选择目标范围 | 例如 `L` + `2`，或 `3` |
| 目标电机在线 | `p` 中对应电机 `online=1` |
| 编码器反馈新鲜 | `p` 中对应电机 `enc=1` |
| 无可用反馈时 | 先发 `q` 或 `o` |

成功后会打印：

```text
User zero set for N selected motor(s).
```

并且对应电机：

```text
user_angle = 0
tgt        = 0
```

如果打印 `User zero set for 0 selected motor(s).`，说明当前目标范围内没有可用的新鲜编码器反馈。

## 8. 状态输出怎么看

发送：

```text
p
```

典型输出：

```text
STAT real_angle=(+12.345,-3.210) enc=(1,1) loop=(1,1) user_angle=(+0.000,+0.000) tgt=(+0.000,+0.000) cmdI=(0,0)mA
TARGET FRONT_PAIR leg=LF count=4
  sel0 M0 LF HIP bus1 id2 online=1 loop=1 enc=1 fault=0 ready=1 user=+0.000 tgt=+0.000 cmdI=0mA dir=-1 tdir=1
  sel1 M1 LF KNEE bus1 id1 online=1 loop=1 enc=1 fault=0 ready=1 user=+0.000 tgt=+0.000 cmdI=0mA dir=-1 tdir=1
  sel2 M2 RF HIP bus1 id4 online=1 loop=1 enc=1 fault=0 ready=1 user=+0.000 tgt=+0.000 cmdI=0mA dir=1 tdir=-1
  sel3 M3 RF KNEE bus1 id3 online=1 loop=1 enc=1 fault=0 ready=1 user=+0.000 tgt=+0.000 cmdI=0mA dir=1 tdir=1
```

字段说明：

| 字段 | 含义 |
| --- | --- |
| `online` | 是否收到过该电机反馈 |
| `loop` | 是否处于 closed-loop |
| `enc` | 编码器反馈是否新鲜、可用 |
| `fault` | 是否存在驱动器错误 |
| `ready` | `online=1 && loop=1 && fault=0` |
| `user` | 当前用户角度 |
| `tgt` | 当前目标角度 |
| `cmdI` | MIT 外环 PID 当前输出电流，单位 mA |
| `dir` | 用户角方向 |
| `tdir` | MIT 力矩方向 |

固件每 `1000 ms` 也会自动打印一次 `LEG1s ...`，用于快速观察当前选中腿。

## 9. 单腿调试流程

以 LF 单腿为例：

```text
L
2
r
e
o
p
z
b
a
d
j
l
u
i
x
```

推荐步骤：

| 步骤 | 命令 | 作用 |
| --- | --- | --- |
| 1 | `L` | 选择 LF |
| 2 | `2` | 选择当前单腿 HIP + KNEE |
| 3 | `r` | 确保没有周期控制发送 |
| 4 | `e` | 未锁存时清除当前目标错误；若已锁存则仅请求 rearm |
| 5 | `o` | probe，确认闭环和编码器 |
| 6 | `p` | 确认 `online=1`、`loop=1`、`enc=1`、`fault=0` |
| 7 | `z` | 可选，把当前姿态设为用户零点 |
| 8 | `b` | 标准 MIT boot，自动置零并打开 PID |
| 9 | `a/d` | 验证 HIP 方向和响应 |
| 10 | `j/l` | 验证 KNEE 方向和响应 |
| 11 | `u/i` | 验证 HIP+KNEE 同步运动 |
| 12 | `x` | 退出调试，目标电机 idle |

调 RF/LB/RB 时只替换第一步：

| 腿 | 命令 |
| --- | --- |
| RF | `R` |
| LB | `B` |
| RB | `N` |

如果只调单个 HIP 电机，把第二步 `2` 换成 `1`。此时 `j/l` 无效，`u/i` 只对 HIP 生效。

## 10. 双腿调试流程

前双腿 LF + RF：

```text
3
r
e
o
p
z
b
a
d
j
l
u
i
x
```

后双腿 LB + RB：

```text
4
r
e
o
p
z
b
a
d
j
l
u
i
x
```

双腿模式说明：

| 命令 | 作用 |
| --- | --- |
| `a/d` | 两条腿 HIP 同时 -/+30 deg |
| `j/l` | 两条腿 KNEE 同时 -/+30 deg |
| `u/i` | 目标范围内 HIP 和 KNEE 同步 -/+30 deg |
| `h` | 两条腿同时进入示教保持 |

双腿调试前建议让腿悬空或可靠支撑。第一次测试只发一次 `a` 或 `d`，观察方向、幅度和 `cmdI` 是否正常，不要连续快速发命令。

## 11. 连续发送命令的注意事项

连续发送 `a/d/j/l/u/i` 会直接叠加目标角度。当前每次是 `30 deg`，如果电机还没到位就继续发，`tgt` 会继续走远，而 `user` 仍落后，容易触发：

```text
SAFETY ... err=... -> fault hold
SAFETY fault hold lost feedback -> stop TX
```

建议判断：

```text
abs(user - tgt) < 5 到 10 deg
```

再发下一次步进命令。重载、地面接触、膝关节接近机械限位时尤其要慢。

## 12. 示教保持流程

需要手动摆动电机，松手后保持姿态时使用 `h`。

单腿示例：

```text
L
2
r
e
o
h
p
x
```

行为说明：

| 阶段 | 行为 |
| --- | --- |
| 拖动中 | 目标跟随当前 `user_angle`，阻力较低 |
| 松手后 | 目标冻结在释放位置，MIT PID 输出力矩保持 |
| 再发 `a/d/j/l/u/i` | 退出示教跟随，转为明确步进目标控制 |
| 发 `z` | 重设用户零点和目标 |
| 发 `x` | 当前目标电机 idle，退出 MIT 调试 |
| 发 `r` | 停止周期控制，回到 RX-only |

如果 `h` 后保持力不足，优先确认：

1. `p` 中 `enc=1`、`loop=1`、`fault=0`。
2. `dir` 和 `tdir` 是否正确。
3. 用户零点和机械姿态是否合理。
4. 当前姿态是否需要超过 `18 A` 限流才能保持。

## 13. 安全保护

遥控与系统级安全行为：

- 只有 CH9/SD 能建立锁存式 ESTOP：`norm >= 50` 时立即急停；`norm <= 20` 表示释放，`21..49` 为迟滞区。
- SBUS 单帧 signal-lost/failsafe 只临时抑制轮、步态和机械臂命令，不改变腿部闭环；持续 `100 ms` 才确认丢失，连续 3 个正常帧后自动恢复，不要求 CH1/CH2 回中。长期无新帧使用 `250 ms` 帧超时加 `100 ms` 确认。
- 电机故障、持续反馈超时、非有限控制量、CAN bus-off、过温和站立启动失败只停止对应子系统，不建立全机锁存；条件恢复后自动重试。腿故障每 `1 s` 请求清错，故障掩码清零后再重试站立。轮驱 bus-off 会暂停发送，待 FDCAN3 状态确认恢复并重新收到四轮反馈、轮速稳定为零后自动清除本地锁止，发送失败按 `10 ms` 退避。只有 CH9 建立全机硬急停锁存。
- 清除腿安全锁存必须同时满足：SBUS 信号新鲜、CH9 已释放、CH5 LOW+CH8 LOW，以及全部 8 个腿部电机处于 idle 且无故障。四轮不再阻塞腿安全 rearm；轮侧本地锁止仍独立要求四轮反馈在线、速度低于 `0.5 rad/s` 并稳定 `200 ms`。
- CH5 LOW 是恢复/待机档，不是绝对安全档；物理断开电机动力电源才是绝对安全措施。维护、接线或进入机械危险区前必须断开动力电源，硬件急停也应采用能切断动力的独立回路。
- 安全锁存期间，串口 `e` 只提交 rearm 请求，不能绕过上述恢复条件，也不是无条件清错。

当前 MIT 调试保护：

| 保护 | 当前行为 |
| --- | --- |
| 编码器查询与新鲜度 | 每条 CAN 总线每 `2 ms` 轮询一个电机，约 `125 Hz/电机`；超过 `120 ms` 未更新才判为不新鲜，过滤瞬时总线抖动 |
| 编码器数据合理性 | 非有限值、速度或按时间计算的位移不合理时拒收该帧 |
| MIT 目标误差超过 `100 deg` | 进入故障保持 |
| MIT PID 内部误差限幅 | `+/-65 deg` |
| MIT 电流限幅 | `18 A` |
| MIT 扭矩换算 | `1.2 Nm/A` |

故障保持时，再发 `a/d/j/l/u/i` 不会恢复步进，会提示：

```text
MIT safety fault-hold active. Send 'b' or 'h' to re-arm, 'x' or 'r' to stop.
```

如果故障保持期间反馈继续丢失，会失能全部电机：

```text
SAFETY ... -> disable all motors
```

安全锁存后的推荐恢复流程：

1. 可靠支撑机器人并停止发送运动命令；需要人员接近机构时先物理断开动力电源。
2. 恢复新鲜、非 failsafe 的 SBUS 信号，并将 CH9 完全释放到 `norm <= 20`。
3. 将 CH5/CH8 保持在 LOW+LOW。固件会直接请求腿安全 rearm，并等待全部 8 个腿部电机确认 idle 且无故障；轮侧继续在自己的状态机中等待四轮在线且低速稳定 `200 ms`，不会阻塞纯点足恢复。
4. 确认安全锁存已清除后，再用 `p` 检查反馈；需要调试时重新执行 `o` 或 `b`，不要直接恢复步进。

串口 `e` 在锁存期间也只能请求 rearm，仍须满足同样的信号、CH9、CH5 和 8 电机状态条件。

如果 `user` 和 `tgt` 相差很大，先检查用户零点、方向、编码器反馈和机械负载，不要直接继续发步进命令。

## 14. 站立姿态和力矩提示

当前固件站立目标参数：

```text
DOG_STAND_HEIGHT_MM = 260
DOG_STAND_X_MM      = 0
DOG_STAND_FOOT_Z_MM = 300
DOG_REAR_FOOT_EXTRA_Z_MM = 15
```

足端 IK 坐标采用 `+Z` 向下：Z 增大表示足端向下，抬腿时 Z 减小。前腿站立目标 Z 为 `300 mm`，后腿 LB/RB 在此基础上增加 `15 mm`，因此后腿目标 Z 为 `315 mm`。串口帮助里的 stand/jump rear z 会由这个宏自动计算。

站立动作采用 `1500 ms` 足端线性轨迹，从起始高度直接上升到最终高度；不再使用 `600 ms` 关节预接近和 `2000 ms` 五次平滑上升。最终站立尺寸保持不变。

站立时 8 个关节统一使用固定 STAND PID：`Kp=1.7 A/deg`、`Ki=0`、`Kd=0.017 A/(deg/s)`，输出限制为 `18 A`。D 项由相邻控制周期的角度误差差分计算，不进行在线参数自适应、STAND 速度低通或输出电流斜率限制。

按大腿 `210 mm`、小腿 `250 mm` 计算，脚在髋关节正下方、高度 `260 mm` 时，几何角大约是：

```text
HIP  = +26.8 deg
KNEE = +68.2 deg
```

如果启动姿态是“小腿水平、大小腿夹角 33 deg”，启动瞬间需要的静态保持力矩会比站立姿态大很多，因为水平力臂更长。整机约 `15 kg`、四腿平均承重时，启动姿态可能需要十几 Nm 级别的关节力矩；当前 MIT 限流 `18 A`，按 `1.2 Nm/A` 换算的理论上限约为 `21.6 Nm`，实际输出还受 PID 和电机能力限制，因此仍不建议从最小夹角水平姿态直接硬撑起整机。

建议：

- 先让膝关节打开到 `60 到 80 deg` 附近，再尝试站立动作。
- 站立测试前先确认每条腿单独 `a/d/j/l` 方向正常。
- 整机落地测试时不要连续快速发送 30 deg 步进。

## 15. 新版四足运动移植说明

本版只移植新版中和四足运动直接相关的参数与控制逻辑，没有采用新版删除遥控器和机械臂的裁剪改动。

已移植内容：

- MIT PID 已统一：RF 右前腿不再使用独立 STAND/SWING PID，四条腿均按支撑/摆动状态选择通用 `g_dog_mit_stand_pid` 或 `g_dog_mit_swing_pid`。
- 其他三条腿仍使用通用 `g_dog_mit_stand_pid` / `g_dog_mit_swing_pid`。
- 小跑前进步幅增加 yaw trim 入口：`DOG_TROT_YAW_TRIM_X_MM = 0.0f`。默认值为 0 时步幅行为不变；后续如需修正稳定偏航，可按毫米级小幅增加。
- 后腿落足补偿从 `20 mm` 调整为 `15 mm`。
- SBUS 通道和九宫格映射见本文开头“实体遥控器操作”；除 LOW+LOW 外轮电机保持闭环可用。
- SB 从 MID 切到 LOW 时先保持 MIT 闭环，通过足端逆解在 `1500 ms` 内将前腿 Z 从 `300 mm` 平滑缩短到 `150 mm`、后腿从 `315 mm` 缩短到 `165 mm`；稳定 `200 ms` 后才撤掉腿部支撑并进入目标 LOW 状态。下降失败时保持腿部控制并以 `LOWER` 原因阻塞，不直接失能。
- M+M 使用 CH2 前后、CH1 偏航和 CH3 `40~200 rpm` 上限进行增强差速控制。H+L/H+M 入档后都先保持站立，不再自动启动原地踏步；CH1/CH2 超过启动死区后直接建立对角前进/转向步态。H+L 使用完整点足步幅并让轮驱 HOLD；H+M 的四轮在整个摆腿、触地和全支撑阶段持续接收同一弧线速度，不再按单腿相位清零。H+M 实际轮速上限取 CH3 人工上限与运动学辅助上限的较小值；后者按当前半步步长、摆动时间和 `116 mm` 轮径计算，并使用 `2.5` 辅助增益及 `30 rpm` 起动下限，三档满指令约为 `30/91/144 rpm`。所有轮模式继续使用 `30/50/60 rad/s^2` 斜率、`6 A` 持续和 `10 A/500 ms` 峰值；`70 C` 开始压缩峰值，`85 C` 过温锁止，冷却至 `65 C` 以下才允许按安全流程恢复。
- LOW+LOW 下 CH1/CH2 回中且四轮在线低速连续 `200 ms` 后建立一次机械限位许可；切到 LOW+MID 后，在当前位置启用 8 腿 MIT 闭环，小腿保持进入瞬间角度，大腿在 `800 ms` 内平滑增加 `15°`，全部关节到位后才允许轮行。抬升角由 `motor_task.h` 的 `DOG_LOW_WHEEL_HIP_LIFT_DEG` 调整。该模式支持 CH1/CH2 差速转向，并与其他轮姿态统一使用最高 `200 rpm`、`30/50/60 rad/s^2` 斜率及 `6 A` 持续、`10 A` 短时电流限制。
- CH1/CH2 任一轴超过 `25` 启动，两轴都回到 `12` 内停止；轴值按 `(abs(norm)-12)/(100-12)` 映射到 `[-1,1]`。CH2 缩放前后步幅，CH1 缩放偏航步幅，CH3 决定频率档位。每半步只快照一次输入，变化量限制为 `0.25`；运行中切换 CH3 时先完成当前落足，再插入 `150 ms` 四腿全支撑相，然后锁存新档位。
- CH3 三档参数为：LOW=`1.0 Hz/25 mm/30 mm/160 ms/40 ms错相`，MID=`2.0 Hz/55 mm/60 mm/60 ms/0 ms`，HIGH=`2.5 Hz/70 mm/100 mm/30 ms/0 ms`。顺序为频率、前进步幅、步高、四脚支撑停顿和对角错相。
- LOW 起步门控为 `4 deg / 8 deg/s / 连续200 ms / 2000 ms超时`。LOW 落足后要求全腿连续稳定 `80 ms`，下一支撑对角腿命令电流低于 `16 A`；确认触地且电流有余量、但 `500 ms` 内稳定窗口未完成时，不再第一次就退出步态，而是插入 `150 ms` 全支撑并将目标指令依次限制到 `65%`、`40%`，第三次恶化才平稳收步。连续两个稳定半步恢复一级；电流无余量或未触地仍直接收步。MID/HIGH 保持 `5 deg / 10 deg/s / 150 ms` 起步门控，并仅在已确认触地时允许相同的稳定降级。摆动足 X 使用五次轨迹，Z 在约 `32%` 进度到达最高点，约 `68%` 的摆动周期用于下降；每腿在 `80%` 进度后独立用 `1.5/0.8 A` 迟滞和 `20 ms` 确认检测接触，未接触时以 `12 mm/s` 最多下探 `5 mm`，搜索末端稳定 `40 ms` 可软着陆，否则记录失败并平稳收步。
- H+L/H+M 摇杆回中或切换到其他需要站立的九宫格时采用平稳收步；普通弧线变化在下一半步生效，明显前后反打先收步再反向。CH9/SD 建立硬急停，其他异常只停止对应运动并在恢复后自动重试。
- 串口 `Y`/`y` 可打印 SBUS 诊断：CH1/CH2 映射值、drive 请求值和半步已应用值，以及当前/待切换档位、稳定降级级别、步态相位、摆动/接触/搜索/失败掩码、四腿进度、步态兼容轮速和 H+M 实际轮速上限；末尾同时输出当前障碍选择、状态、检查点、故障、回退/退出许可和活动缝宽。
- CH9/SD 是唯一锁存式急停输入：`norm >= 50` 立即锁存 ESTOP；释放后仍需新鲜 SBUS、CH5 LOW+CH8 LOW，以及全部 8 个腿电机 idle/无故障才能恢复腿控制。轮侧锁止独立恢复，不阻塞 H+L 纯点足。
- SBUS 丢失、failsafe、首帧超时以及其他软件异常不建立全机锁存，只进行局部停止和自动恢复。

保留内容：

- `User_File/remote/sbus.c/h`、SBUS 初始化、遥控 failsafe 和 CH5/CH8 模式逻辑。
- `User_File/task/arm/arm_motor_task.cpp/h`、DM4310/EL05 机械臂控制和周期发送。
- `STM32H723XG_FLASH.ld` 中的 `.dma_buffer` 段。
- 本文开头“实体遥控器操作”中的遥控器和机械臂映射说明。

SBUS 机械臂模式位于 M+H：确认站立后，CH6/CH7 控制机械臂 J0/J1 jog；此时轮驱保持 HOLD 零速主动制动，四足不会启动步态。H+L/H+M 都接入 CH3 步态档位和 CH1+CH2 弧线融合；H+M 使用连续四轮辅助并由当前半步运动学参数限制轮速，平稳收步的 `200 ms` 内轮速也随收步进度连续衰减。轮在线、锁止和过温检测与腿步态分离：轮异常时只将轮侧切到 HOLD 并置 `wheel_degraded=1`，腿步态继续响应；轮恢复可用后自动重新加入连续辅助。CH4 姿态微调暂未接入。

## 16. OpenOCD 只烧录任务

当前 VSCode 任务保留四个：

| 任务 | 作用 |
| --- | --- |
| `CMake: configure Debug` | 使用 `cmake --preset Debug` 配置 |
| `CMake: build Debug` | 使用 `cmake --build --preset Debug` 编译 |
| `OpenOCD: flash Debug` | 只烧录，不进入 debug |
| `Build + Flash Debug` | 先编译，再烧录 |

OpenOCD 烧录命令等价于：

```powershell
F:/xpack-openocd-0.12.0-7/bin/openocd.exe `
  -f "stm32h743_daplink.cfg" `
  -c "adapter speed 200" `
  -c "init; reset halt; sleep 200; program build/Debug-cmake/dog_leg_test_SDK.elf verify; reset run; shutdown"
```

如果 DAPLink 烧录不稳定，优先检查：

1. DAPLink 固件和 USB 线。
2. 板子供电是否稳定。
3. SWDIO/SWCLK/GND/NRST 接线。
4. 降低 `adapter speed`，例如从 `200` 改成 `100` 或 `50`。
5. 确保没有另一个 OpenOCD、pyOCD 或调试会话占用 DAPLink。

## 17. 常见问题

| 现象 | 处理 |
| --- | --- |
| `enc=(0,0)` | 发 `q` 或 `o`，检查电源、CAN、ID 和反馈帧 |
| `loop=(0,0)` | 发 `e` 后再 `o` 或 `b` |
| `User zero set for 0 selected motor(s).` | 没有新鲜编码器反馈，先发 `q` 或 `o` |
| `MIT boot FAIL.` | 看 `BOOT skip/probe-fail/zero-fail` 的具体电机 |
| `MIT TX is stopped...` | 安全保护或手动停止后，需要重新 `b` 或 `h` |
| 只有一侧腿动 | 用 `p` 看 `TARGET` 明细，确认目标范围、online、loop、enc 和 ID |
| `user_angle` 方向反了 | 修改 `g_dog_motor_config[]` 对应电机的 `dir` |
| MIT 力矩方向反了 | 修改 `g_dog_motor_config[]` 对应电机的 `tdir` |
| 连续发命令后故障 | 等 `user` 接近 `tgt` 后再发下一次 |
| RF KNEE 反馈反复失效或 `enc=0` | 先单独 `R` + `1/2` 排查反馈、负载、线束和机械冲击；不要在双腿模式继续叠加目标 |

## 18. 快速命令卡

```text
L/R/B/N  选择当前腿 LF/RF/LB/RB
1        当前腿 HIP 单电机
2        当前单腿 HIP+KNEE
3        前双腿 LF+RF
4        后双腿 LB+RB
8        全部 8 电机

r        RX-only，停止周期控制
e        未锁存时清错；安全锁存时仅请求 rearm
m        配置位置模式
o        MIT probe，不打开周期控制
b        MIT boot，自动置零并打开 MIT PID
h        MIT 示教保持，拖动后松手保持

a/d      HIP -/+30 deg
j/l      KNEE -/+30 deg
u/i      HIP+KNEE 同步 -/+30 deg

z        设置用户零点
x        idle 并停止 TX
c        开关原始 CAN RX 日志
q        查询编码器
p        打印状态
!        失能全部电机（仅 SD/CH9 可触发 ESTOP）
?        帮助
```
