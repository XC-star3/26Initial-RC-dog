# 下位机实现上位机操控四足

## 1. 文档目的

本文定义 STM32 下位机通过 USB CDC 接收上位机连续控制指令、驱动四足机器人并返回状态的实现方案。本文是 USB 四足控制协议 v1 的规范源；上位机必须按本文的线协议实现，不得直接发送现有串口调试字符代替连续控制协议。

当前工程的 USB CDC 已能双向传输字节，但 USB 接收数据只进入调试命令解析器。实际连续运动控制仍由 UART5 上的 SBUS 数据驱动。因此需要新增二进制协议解析、控制权仲裁、通信看门狗和状态上报，并将 SBUS 与 USB 输入汇合到统一控制输入层。

本方案核心覆盖四足与轮驱，并为兼容当前遥控框架传输 CH6/CH7 机械臂 jog；它不定义能够确认完成的全自动抓取/放置 action。物理断电和独立硬件急停仍是最终安全措施。

## 2. 当前工程基线

- USB：CDC 虚拟串口，VID=`0x0483`，PID=`0x5740`，产品名为 `STM32 Virtual ComPort`。
- USB RX：`CDC_Receive_HS()` 将数据写入 128 字节环形缓冲，`control_task()` 每 1 ms 取字节。
- SBUS：UART5，`100000 9E2`，25 字节帧；CH1/CH2 控制偏航和前后，CH3 控制速度，CH5/CH8 选择模式，CH9 触发锁存急停。
- 控制任务：`control_task()` 先执行 `sbus_control_update()`，再处理 USB 调试字符。
- 现有安全逻辑：SBUS 超时、轮驱反馈、电机故障和 `DogSafety_*` 锁存逻辑已经存在，应复用而不是旁路。

正式版本的 USB ASCII 调试面只保留 `p` 和 `Y` 两个只读查询命令。上电帮助、周期文本状态、CAN 原始日志、VOFA 波形以及其他会引起状态改变或运动的单字符命令全部关闭，USB 未收到查询或二进制协议请求时不主动发送文本。

建议新增以下文件：

```text
User_File/BSP/USB/usb_control_protocol.h
User_File/BSP/USB/usb_control_protocol.cpp
User_File/task/robot_control_input.h
User_File/task/robot_control_input.cpp
```

## 3. 总体结构

```text
USB CDC RX -> 二进制流解析 -> 会话/序号/范围校验 -> USB 控制样本 --+
                                                               |
UART5 SBUS -> SBUS 解析 -> 实体授权/急停/接管 -------------------+-> 控制权仲裁
                                                                    |
                                                                    v
                                                        统一 RobotControlInput
                                                                    |
                                                                    v
                                                 模式状态机/步态/轮驱/安全层
                                                                    |
USB CDC TX <- 状态帧/应答帧 <--------------------------------------+
```

不得把 USB 数据伪造成 `SbusState` 写入 SBUS 模块。SBUS 是实体输入与安全授权源，USB 是独立输入源；两者只在控制权仲裁层汇合。

## 4. 传输与编码约定

### 4.1 基本约定

- 传输介质：USB CDC 字节流。
- 串口工具显示的波特率对 USB CDC 无实际影响；上位机可配置 `115200 8N1`。
- 多字节整数统一使用小端序。
- 有符号整数使用二进制补码。
- 线协议不直接发送 C/C++ `struct` 内存；必须逐字段编码/解码，避免填充和未对齐访问。
- 协议版本：`1`。
- 单帧最大 payload：`96` 字节。
- 接收解析器必须支持拆包、粘包、噪声跳过和 CRC 错误后的重新同步。

### 4.2 通用帧格式

一帧总长度为 `12 + payload_len` 字节。

| 偏移 | 长度 | 字段 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 1 | magic0 | 固定 `0xA5` |
| 1 | 1 | magic1 | 固定 `0x5A` |
| 2 | 1 | version | 固定 `0x01` |
| 3 | 1 | msg_type | 消息类型 |
| 4 | 1 | flags | 位 0=`ACK_REQ`，位 1=`RESPONSE`，其余必须为 0 |
| 5 | 1 | reserved | 必须为 0 |
| 6 | 2 | seq | 发送端帧序号，`uint16`，允许回绕 |
| 8 | 2 | payload_len | payload 长度，范围 `0..96` |
| 10 | N | payload | 消息负载 |
| 10+N | 2 | crc16 | CRC 低字节在前 |

CRC 使用 `CRC-16/CCITT-FALSE`：

```text
poly   = 0x1021
init   = 0xFFFF
refin  = false
refout = false
xorout = 0x0000
```

CRC 计算范围从 `version`（偏移 2）开始，到 payload 最后一个字节结束；不包含两个 magic 字节，也不包含 CRC 字段本身。

解析规则：

1. 扫描 `A5 5A`。
2. 检查版本、保留位和 `payload_len <= 96`。
3. 等待完整帧后检查 CRC。
4. CRC 错误时丢弃当前第一个 magic 字节，继续扫描，不得清空整个接收缓冲。
5. 未知消息类型返回 `UNSUPPORTED`，但不得影响控制状态。

### 4.3 消息类型

| 值 | 名称 | 方向 | 是否需要会话 |
| ---: | --- | --- | --- |
| `0x01` | `HELLO_REQ` | 上位机 -> 下位机 | 否 |
| `0x81` | `HELLO_RESP` | 下位机 -> 上位机 | 否 |
| `0x02` | `ACQUIRE_REQ` | 上位机 -> 下位机 | 否 |
| `0x82` | `ACQUIRE_RESP` | 下位机 -> 上位机 | 否 |
| `0x03` | `RELEASE_REQ` | 上位机 -> 下位机 | 是 |
| `0x10` | `VIRTUAL_RC_SETPOINT` | 上位机 -> 下位机 | 是 |
| `0x12` | `ESTOP_REQ` | 上位机 -> 下位机 | 否 |
| `0x20` | `STATUS_CONFIG` | 上位机 -> 下位机 | 否 |
| `0x90` | `ROBOT_STATUS` | 下位机 -> 上位机 | 否 |
| `0xE0` | `ACK` | 双向 | 否 |

## 5. 消息负载定义

### 5.1 HELLO_REQ，8 字节

| 偏移 | 类型 | 字段 | 说明 |
| ---: | --- | --- | --- |
| 0 | `uint32` | client_nonce | 上位机本次连接随机数，不能固定为 0 |
| 4 | `uint16` | desired_status_hz | 期望状态频率，范围 `1..50` |
| 6 | `uint16` | reserved | 必须为 0 |

### 5.2 HELLO_RESP，24 字节

| 偏移 | 类型 | 字段 | 说明 |
| ---: | --- | --- | --- |
| 0 | `uint32` | client_nonce | 原样回显 |
| 4 | `uint32` | device_id | MCU UID 的稳定 32 位摘要 |
| 8 | `uint32` | firmware_version | `major<<24 | minor<<16 | patch<<8 | build` |
| 12 | `uint32` | capability_bits | 能力位 |
| 16 | `uint16` | status_hz | 实际状态频率 |
| 18 | `uint16` | max_payload | v1 固定为 96 |
| 20 | `uint8` | min_protocol | v1 固定为 1 |
| 21 | `uint8` | max_protocol | v1 固定为 1 |
| 22 | `uint16` | reserved | 固定为 0 |

`capability_bits`：位 0=腿部步态，位 1=轮驱，位 2=混合轮腿，位 3=实体 SBUS 授权，位 4=状态主动上报，位 5=虚拟遥控通道帧，位 6=CH6/CH7 jog。其他位保留为 0。

### 5.3 ACQUIRE_REQ，8 字节

| 偏移 | 类型 | 字段 | 说明 |
| ---: | --- | --- | --- |
| 0 | `uint32` | client_nonce | 必须与最近一次 HELLO 一致 |
| 4 | `uint16` | requested_lease_ms | 请求租约，允许 `1000..5000` ms |
| 6 | `uint16` | reserved | 固定为 0 |

### 5.4 ACQUIRE_RESP，12 字节

| 偏移 | 类型 | 字段 | 说明 |
| ---: | --- | --- | --- |
| 0 | `uint16` | request_seq | 对应 `ACQUIRE_REQ.seq` |
| 2 | `uint8` | result | 结果码 |
| 3 | `uint8` | control_source | 当前控制源 |
| 4 | `uint32` | session_id | 成功时非 0，失败时为 0 |
| 8 | `uint16` | granted_lease_ms | 实际租约 |
| 10 | `uint16` | status_period_ms | 状态上报周期 |

### 5.5 RELEASE_REQ，4 字节

只有一个 `uint32 session_id`。有效释放后立即将运动输入置零、请求步态平稳收步、轮驱进入 HOLD，并返回 ACK。重复释放同一旧会话应返回 `OK`，保证幂等。

### 5.6 VIRTUAL_RC_SETPOINT，28 字节

上位机在取得控制权后以固定 50 Hz 发送。该消息模拟当前框架的 SBUS 运动通道和 CH5/CH8 九宫格，不直接发送电机或足端目标。

| 偏移 | 类型 | 字段 | 说明 |
| ---: | --- | --- | --- |
| 0 | `uint32` | session_id | 当前会话 ID |
| 4 | `uint32` | host_time_ms | 上位机单调时钟低 32 位，仅诊断使用 |
| 8 | `uint8` | main_switch | 等价 CH5/SB，`0=LOW,1=MID,2=HIGH` |
| 9 | `uint8` | sub_switch | 等价 CH8/SC，`0=LOW,1=MID,2=HIGH` |
| 10 | `uint16` | command_flags | 控制标志 |
| 12 | `int16` | yaw_permille | 等价 CH1，范围 `-1000..1000` |
| 14 | `int16` | forward_permille | 等价 CH2，范围 `-1000..1000` |
| 16 | `int16` | speed_permille | 等价 CH3，范围 `-1000..1000` |
| 18 | `int16` | arm_j0_permille | 等价 CH6，范围 `-1000..1000` |
| 20 | `int16` | arm_j1_permille | 等价 CH7，范围 `-1000..1000` |
| 22 | `uint16` | channel_valid_mask | v1 固定 `0x0067`，对应 CH1/2/3/6/7 |
| 24 | `uint32` | command_counter | 会话内严格递增，允许 32 位回绕 |

`command_flags`：

| 位 | 名称 | 行为 |
| ---: | --- | --- |
| 0 | `DEADMAN_HELD` | 人工持续按压或自主许可租约有效，控制连续非零通道 |
| 1 | `MOTION_ENABLE` | 允许采用虚拟九宫格并进入非待机模式 |
| 2 | `SMOOTH_STOP` | 忽略非零摇杆并请求平稳收步 |

- `MOTION_ENABLE=0`：有效 main/sub 强制为 LOW+LOW，所有连续通道为 0，CH3 取最低值；不能触发站立或其他模式动作。
- `MOTION_ENABLE=1, DEADMAN_HELD=0`：允许进入并保持请求模式，但 CH1/CH2/CH6/CH7 强制为 0。
- 两位同时为 1：在其他校验和安全条件满足时，允许采用非零连续通道。
- `SMOOTH_STOP=1`：CH1/CH2/CH6/CH7 强制为 0，步态请求平稳收步；是否继续保持站立由有效九宫格决定。

未知标志位或 valid mask 未知位非 0 时拒绝该帧。未置位的通道按 0 处理，不沿用旧值。

九宫格模式编号为 `main_switch * 3 + sub_switch`：

| 值 | 名称 | 对应行为 |
| ---: | --- | --- |
| 0 | `MOTOR_CHECK_IDLE` | LOW+LOW，无动作检测/恢复准备，轮驱 OFF |
| 1 | `LOW_WHEEL` | 机械限位低姿态轮行 |
| 2 | `LOW_SAFE_HOLD` | 腿 RX-only/安全保持，轮驱 HOLD |
| 3 | `STAND_HOLD` | MIT 站立，轮驱 HOLD |
| 4 | `STAND_WHEEL` | MIT 站立轮行 |
| 5 | `STAND_ARM` | 站立后 CH6/CH7 机械臂 jog |
| 6 | `GAIT_ONLY` | 纯点足步态，轮驱 HOLD |
| 7 | `GAIT_WHEEL` | 点足与轮驱相位同步 |
| 8 | `RESERVED_STAND` | 保留站立，轮驱 HOLD |

main/sub 任一值大于 2 必须返回 `BAD_RANGE`。模式变化走现有进入/退出状态机，不允许直接跳过站立、下降、机械许可和故障检查。

归一化规则：各 permille 除以 10 后映射为当前 SBUS `norm=-100..100`。CH3 继续按 `norm+100` 得到 `0..200 rpm` 轮速上限，并按 `<=-60 / 中间 / >=60` 得到 LOW/MID/HIGH 步态档。下位机必须再次限幅，并沿用现有进入死区 25%、退出死区 12%、半步输入变化限制和轮速斜率限制。USB 不得提供虚拟 CH9；CH9 始终读取实体 SBUS。

固定测试向量：session=`0x11223344`、host time=`0x01020304`、HIGH+LOW、flags=`0x0003`、CH1=-250、CH2=500、CH3=-800、CH6/7=0、mask=`0x0067`、counter=10、frame seq=`0x1234`。payload 为 28 字节，完整帧为 40 字节，CRC=`0x96CF`：

```text
A5 5A 01 10 00 00 34 12 1C 00 44 33 22 11 04 03 02 01
02 00 03 00 06 FF F4 01 E0 FC 00 00 00 00 67 00 0A 00
00 00 CF 96
```

### 5.7 ESTOP_REQ，4 字节

| 偏移 | 类型 | 字段 | 说明 |
| ---: | --- | --- | --- |
| 0 | `uint16` | reason | 上位机记录的原因码 |
| 2 | `uint16` | reserved | 固定为 0 |

ESTOP 不要求会话，合法 CRC 的 ESTOP 必须最高优先级处理，调用全机锁存急停并返回 ACK。USB 协议不提供解除急停命令；解除仍必须满足实体 CH9 已释放、SBUS 新鲜、LOW+LOW、腿电机无故障且轮速稳定为零等现有恢复条件。

### 5.8 STATUS_CONFIG，4 字节

| 偏移 | 类型 | 字段 | 说明 |
| ---: | --- | --- | --- |
| 0 | `uint16` | status_hz | `0` 停止主动上报，其他值限幅到 `1..50` |
| 2 | `uint16` | reserved | 固定为 0 |

### 5.9 ROBOT_STATUS，44 字节

| 偏移 | 类型 | 字段 | 说明 |
| ---: | --- | --- | --- |
| 0 | `uint32` | uptime_ms | 下位机运行时间 |
| 4 | `uint32` | session_id | 当前会话，无会话为 0 |
| 8 | `uint32` | last_command_counter | 最近接受的控制计数 |
| 12 | `uint16` | last_rx_seq | 最近有效上行帧序号 |
| 14 | `uint8` | control_source | `0=NONE, 1=SBUS, 2=USB` |
| 15 | `uint8` | requested_mode | USB/SBUS 请求模式 |
| 16 | `uint8` | active_mode | 实际已经进入的模式 |
| 17 | `uint8` | entry_state | `0=INACTIVE,1=ENTERING,2=WAIT,3=ACTIVE,4=BLOCKED` |
| 18 | `uint8` | speed_profile | 当前速度档 |
| 19 | `uint8` | link_state | `0=DOWN,1=READY,2=ACQUIRED,3=TIMEOUT` |
| 20 | `uint16` | safety_flags | 安全状态位 |
| 22 | `uint16` | fault_flags | 汇总故障位 |
| 24 | `int16` | applied_forward_permille | 实际采用的前后输入 |
| 26 | `int16` | applied_yaw_permille | 实际采用的偏航输入 |
| 28 | `uint16` | applied_wheel_max_rpm | 实际轮速上限 |
| 30 | `uint16` | setpoint_age_ms | 最近控制帧年龄，最大 65535 |
| 32 | `uint16` | leg_online_mask | 位 0..7 对应 8 个腿电机 |
| 34 | `uint8` | wheel_online_mask | 位 0..3 对应四轮 |
| 35 | `uint8` | block_reason | 模式阻塞原因 |
| 36 | `uint32` | rx_crc_error_count | CRC 错误累计 |
| 40 | `uint32` | rx_drop_count | RX 溢出/格式错误累计 |

`safety_flags`：位 0=安全锁存，位 1=CH9 inhibit，位 2=SBUS 不新鲜，位 3=USB 控制超时，位 4=实体模式未处于 LOW+LOW，位 5=实体摇杆未回中，位 6=轮驱锁止，位 7=腿部保护保持。

`fault_flags`：位 0=腿电机故障，位 1=腿反馈超时，位 2=轮驱故障，位 3=轮反馈超时，位 4=CAN bus-off，位 5=过温，位 6=模式进入失败。具体诊断继续通过已有日志或后续扩展消息提供。

### 5.10 ACK，8 字节

| 偏移 | 类型 | 字段 | 说明 |
| ---: | --- | --- | --- |
| 0 | `uint16` | acked_seq | 被确认帧序号 |
| 2 | `uint8` | acked_type | 被确认消息类型 |
| 3 | `uint8` | result | 结果码 |
| 4 | `uint16` | detail | 可选详细原因 |
| 6 | `uint16` | reserved | 固定为 0 |

结果码：

| 值 | 名称 | 含义 |
| ---: | --- | --- |
| 0 | `OK` | 已接受 |
| 1 | `BAD_VERSION` | 协议版本不支持 |
| 2 | `BAD_LENGTH` | payload 长度错误 |
| 3 | `BAD_RANGE` | 字段越界或保留位非 0 |
| 4 | `BAD_SESSION` | 会话不存在或不匹配 |
| 5 | `NOT_AUTHORIZED` | 实体授权条件不满足 |
| 6 | `BUSY` | 控制权已被占用或状态机忙 |
| 7 | `SAFETY_LATCHED` | 安全锁存中 |
| 8 | `STALE_COMMAND` | 控制计数重复或过期 |
| 9 | `UNSUPPORTED` | 消息或能力不支持 |

`VIRTUAL_RC_SETPOINT` 正常情况下不逐帧 ACK，避免 50 Hz 应答流量；上位机通过 `ROBOT_STATUS.last_command_counter` 确认接收。校验通过但业务拒绝的控制帧应发送一次 ACK 错误响应。

## 6. 控制权仲裁

### 6.1 USB 取得控制权的条件

`ACQUIRE_REQ` 只有同时满足以下条件才成功：

- SBUS 信号新鲜且不处于 signal-lost/failsafe。
- CH9 已释放，安全锁存未激活。
- CH5=LOW 且 CH8=LOW。
- CH1/CH2 均在退出死区内，即 `abs(norm) <= 12`。
- 四轮已停止；若当前处于站立状态，还需先完成安全收步/下降。
- 当前没有其他 USB 会话。

成功后生成不可预测的非 0 `session_id`，控制源切换为 USB。`session_id` 至少混合 MCU UID、当前 tick、client nonce 和一个递增启动计数；不能简单固定为 1。

### 6.2 实体遥控优先级

优先级从高到低：

1. 物理断电/硬件急停。
2. CH9 锁存急停。
3. 电机、CAN、温度等安全保护。
4. 实体 SBUS 接管动作。
5. USB 控制。

USB 会话期间发生以下任一事件，必须立即把运动输入置零、轮驱 HOLD、步态平稳收步，并撤销 USB 会话：

- CH9 触发或安全锁存建立。
- SBUS 丢失或进入 failsafe。
- CH5/CH8 离开 LOW+LOW。
- CH1/CH2 明显离开中位，建议 `abs(norm) > 25` 即视为人工接管。
- USB 断开、租约过期或收到有效 RELEASE。

默认不允许无 SBUS 控制。若产品确实取消实体遥控，必须新增独立硬件急停和物理 USB 控制使能输入，并用编译选项 `USB_CONTROL_ALLOW_WITHOUT_SBUS=0` 保持默认关闭；不能仅删除上述判断。

## 7. 看门狗与失联行为

定义：

```text
USB_CONTROL_PERIOD_MS        = 20
USB_SETPOINT_TIMEOUT_MS      = 150
USB_DEFAULT_LEASE_MS         = 1500
USB_MAX_LEASE_MS             = 5000
USB_STATUS_DEFAULT_HZ        = 20
```

- 每个有效、更新的 `VIRTUAL_RC_SETPOINT` 刷新 setpoint 时间和租约活动时间。
- 超过 150 ms 未收到新控制计数：立即设置 USB timeout，前后/偏航归零，轮驱 HOLD，步态请求平稳收步，健康腿保持闭环；不得继续执行最后一帧。
- 超过租约时间仍无有效控制帧：撤销会话，控制源变为 NONE，并保持保护姿态。
- 恢复通信后必须重新 HELLO、ACQUIRE；旧 session 和旧控制帧不得恢复运动。
- 下位机只用本地接收时刻执行超时判断，不能信任 `host_time_ms`。

## 8. 下位机代码改造建议

### 8.1 USB 接收层

- 将 `CDC_RX_RING_SIZE` 从 128 增加到至少 512。
- 新增 RX 溢出计数读取接口，不再静默丢弃。
- ISR 只写环形缓冲，不在 USB 中断中解析协议或调用运动控制。
- 二进制解析每次 `control_task()` 可限制最大处理字节数，例如 256，防止日志洪泛饿死控制循环。

### 8.2 协议层

`UsbControlProtocol_ProcessByte()` 维护有限状态机和固定大小缓冲，不动态分配内存。完成一帧后投递结构化消息到控制输入模块。解析器必须先校验完整长度和 CRC，再修改任何会话或控制状态。

正式版本的 ASCII 兼容规则：

- 检测到 `A5 5A` 后，该完整帧中的任何字节都不能送入 `handle_command()`。
- 非二进制字节只识别 `p` 和 `Y`；允许把小写 `y` 作为 `Y` 的兼容别名，但正式上位机统一发送大写 `Y`。
- `p` 只返回一次机器人、电机和轮驱状态；`Y` 只返回一次 SBUS 与模式诊断。两者不得修改目标、模式、PID、日志开关或安全状态。
- `?、L/R/B/N、1/2/3/4/8、r/e/m/o/b/h、!、a/d/j/l/u/i、z/x/s/f/g/T/[ /]/D、c/q/v/W` 等现有调试命令在正式构建中均返回无输出或统一的 `UNSUPPORTED`，且绝不能执行原行为。
- 正式构建不调用启动 `print_help()`，不运行 `lf_periodic_status()`，关闭 VOFA 和普通 ASCII CAN 日志。
- 内部 `DebugUart_Printf()` 日志默认不得发往正式版 USB，可改发 SWO/调试 UART，或通过编译开关仅在开发固件启用。
- USB 会话取得控制权后仍允许 `p/Y` 查询，但查询响应不得阻塞控制帧和二进制状态帧。
- TX 必须按完整帧原子入队；不得让文本日志插入一帧中间。

建议通过构建宏隔离，而不是运行时隐藏：

```cpp
#define USB_CDC_PRODUCTION_INTERFACE 1
```

正式构建下 `handle_command()` 只编译 `p/Y` 分支。开发固件可以通过明确的开发构建配置恢复调试命令，但生成物名称和版本号必须与正式固件区分。

### 8.3 统一遥控视图

新增与传输无关、保持现有 SBUS 通道语义的只读视图，例如：

```cpp
struct RemoteControlView {
    int16_t norm[SBUS_CHANNEL_COUNT];
    uint8_t main_switch;
    uint8_t sub_switch;
    uint8_t source;
    uint32_t update_ms;
};
```

SBUS 适配器从真实 `SbusState` 生成视图；USB 适配器把 CH1/2/3/6/7 permille 限幅并除以 10，写入对应 `norm[]`，同时填入虚拟 main/sub switch。当前 `sbus_mode_tick()`、摇杆映射、轮驱和机械臂 jog 改为读取 `RemoteControlView`，模式进入、步态启停、轮腿混合逻辑只保留一份。

真实 SBUS 的 fresh、signal-lost、failsafe、CH9 和实体接管判断不进入该视图，始终直接读取 `SbusState`。USB 不得覆盖 DMA 缓冲、SBUS 帧计数或安全通道。

不要扩大当前 `Dog_Remote_Sample` 来承载完整控制，它目前只保存站立、急停、模式和时间，不能表达连续前后/偏航输入。

### 8.4 控制任务顺序

推荐每 1 ms 执行：

```text
1. Sbus_Process()
2. USB 字节解析
3. 最高优先级安全输入检查
4. 控制权仲裁与 USB 看门狗
5. 生成唯一 RobotControlInput
6. 模式状态机和运动更新
7. 按周期生成 ROBOT_STATUS
8. 刷新 USB TX
```

安全检查不能等到 20 ms 控制帧周期才执行，CH9 和本地故障仍按当前 1 ms 级任务处理。

### 8.5 状态上报

- 默认 20 Hz，上限 50 Hz。
- USB 枚举完成但尚未收到有效 HELLO 时，不主动发送任何 ASCII 文本或二进制周期状态。HELLO 后才按协商频率发送 `ROBOT_STATUS`；这属于控制协议遥测，不属于串口调试指令输出。
- 状态帧发送失败时允许丢弃旧状态，只保留最新一帧，不能阻塞控制任务。
- ACK/ACQUIRE_RESP/ESTOP ACK 的优先级高于周期状态和文本日志。
- CDC 未配置或主机未打开时不积累无限 TX 数据。

## 9. 状态机要求

USB 会话状态：

```text
DISCONNECTED -> READY -> ACQUIRED -> TIMEOUT
      ^           |          |          |
      +-----------+----------+----------+
            USB断开/释放/实体接管
```

- `READY`：HELLO 成功，但没有运动权限。
- `ACQUIRED`：会话有效，可以接收控制帧；没有 deadman 时仍不能运动。
- `TIMEOUT`：已执行失联停止，等待租约结束或主动释放；收到旧控制帧不能直接恢复。
- 实现可在首次超时后立即销毁会话，从而直接回到 READY；无论采用哪种内部状态，对外都必须要求重新 ACQUIRE。

模式的 `requested_mode` 与 `active_mode` 必须分开。上位机请求 `GAIT_WHEEL` 时，机器人可能仍在 `WAIT_STAND`；只有 `active_mode=GAIT_WHEEL` 且 `entry_state=ACTIVE` 才表示模式真正可用。

## 10. 测试与验收

### 10.1 协议单元测试

- 单字节喂入完整帧。
- 一次喂入多帧粘包。
- magic 被拆到两个 USB 包。
- payload 内包含 `A5 5A`。
- CRC 错误后紧跟合法帧。
- `payload_len=97`、未知版本、非零 reserved。
- `seq` 和 `command_counter` 回绕。
- 重复、乱序控制帧不能恢复旧速度。

### 10.2 控制权测试

- SBUS 非 LOW+LOW 时 ACQUIRE 返回 `NOT_AUTHORIZED`。
- 摇杆未回中、CH9 未释放、安全锁存时不能取得控制权。
- USB 控制中切换 CH5/CH8或推动摇杆，150 ms 内撤销 USB 并停止运动。
- 第二个上位机不能抢占现有会话。
- RELEASE 重复发送保持幂等。

### 10.3 失联测试

- 运动中停止控制帧，150 ms 内轮目标归零并请求收步。
- 拔 USB 线得到相同行为。
- 主机暂停 1 秒后恢复发送旧 session，机器人不能恢复运动。
- CRC 错误洪泛不能刷新看门狗。
- 只有 HELLO、状态查询或重复旧 setpoint 不能刷新运动看门狗。

### 10.4 实机分级验收

1. 电机断电，仅检查帧、ACK、状态和超时。
2. 机器人可靠架空，限制为 LOW 档和低轮速，检查方向与实体接管。
3. 架空验证 CH9、USB 拔线、进程崩溃、PC 休眠。
4. 地面低速验证站立、收步、模式切换。
5. 最后开放 MID/HIGH，始终保留独立断电人员。

验收硬指标：任何通信异常、上位机线程卡死或 USB 拔线均不得让机器人持续执行最后一条非零运动命令。

### 10.5 正式版 USB 表面验收

- 上电并打开 CDC 后静置 60 秒，USB 不输出启动帮助、周期日志或 VOFA 数据。
- 逐个发送历史单字符命令，除 `p/Y` 外不得改变模式、电机目标、日志配置或安全状态。
- `p` 和 `Y` 各只产生一次查询响应，不启动周期输出。
- 未完成 HELLO 时不发送周期二进制状态；完成 HELLO 后只发送协议允许的响应和协商后的 `ROBOT_STATUS`。
- 开发版和正式版固件版本、文件名或构建标识可明确区分，避免将带运动调试指令的固件误用于现场。
