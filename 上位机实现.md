# 假设下位机没问题时上位机实现

## 1. 目标与前提

本文说明在下位机已经完整实现《下位机实现上位机操控四足》所定义 USB 四足控制协议 v1、控制权仲裁和安全看门狗的前提下，上位机如何完成 26 Robocon 全自动任务赛。虚拟遥控器不再是最终产品功能，而是 ROS 2 导航、视觉伺服和任务状态机向四足下位机输出运动命令的统一执行层，同时保留人工架空调试能力。

线协议以 [下位机实现上位机操控四足.md](./下位机实现上位机操控四足.md) 为唯一规范源。本文不重新定义另一套字段含义；实现中应把协议常量集中在一个模块，UI 不得直接拼装字节帧。

正式版下位机不会在 USB 打开后主动输出启动帮助或周期文本，ASCII 只保留 `p` 和 `Y` 两个只读查询。上位机的正常控制和状态获取必须使用二进制协议，不能依赖调试文本；`ROBOT_STATUS` 是 HELLO 后协商启用的协议遥测，不属于主动调试日志。

推荐技术栈为 Ubuntu + ROS 2、Nav2、Python/C++ 节点、PySide6、PaddleOCR、SymPy、YOLO、OpenCV、RealSense SDK 和 pyserial。无论具体语言如何选择，都必须满足独立收发、固定周期发送、单调时钟、CRC 校验、任务可取消和断线安全处理。

## 2. 上位机模块划分

```text
SerialTransport     串口枚举、打开、异步读写、拔线检测
ProtocolCodec       帧编码、流解析、CRC、消息结构
RobotSession        HELLO、控制权、租约、重连、序号
ControlScheduler    50 Hz 控制帧，deadman，输入限幅
RobotStateStore     最新状态、超时、故障和模式状态
MissionManager      全自动比赛任务状态机、重试和取消
LocalizationManager 雷达原点重置、TF、速度估计和定位质量
NavigationClient    Nav2 全局规划、局部避障和目标取消
VisualServo         物块/兑换区识别与末端视觉对准
ManipulatorClient   吸取、抓取确认、放置和机构状态
SafetySupervisor    围栏、超速、卡滞、进程和通信安全
OperatorUI          操作输入、状态展示、急停按钮
EventLog            连接、拒绝、超时、模式切换和故障记录
```

串口读取线程、50 Hz 控制调度器、ROS 2 executor、视觉推理进程和 UI 线程必须解耦。UI 或 OCR/YOLO 卡顿不能阻塞串口接收，也不能延迟停止帧。控制调度器应使用单调时钟和绝对截止时间，不能用连续 `sleep(20ms)` 累积漂移。

## 3. 核心接口：USB 虚拟遥控器

本节是上位机与当前下位机框架联调的核心规范。全自动任务的导航、视觉或任务状态机最终都只能更新一份虚拟遥控样本，由 USB bridge 以 50 Hz 发送；下位机继续使用现有 CH1/CH2/CH3、CH5/CH8 九宫格、步态、轮驱和保护逻辑。

### 3.1 设计边界

- USB 虚拟遥控器模拟运动相关通道，不直接发送电机角度、足端轨迹或 CAN 帧。
- CH1、CH2、CH3、CH5、CH8 与当前实体 SBUS 行为完全一致。
- CH6、CH7 预留给当前机械臂 jog 模式；全自动抓放仍应使用可确认完成的机构 action，不能只靠 jog 时间猜测。
- CH9/SD 不允许由 USB 模拟。实体 CH9 始终是最高优先级急停输入，USB 不能伪造 CH9 释放。
- USB 可以发送独立 `ESTOP_REQ` 建立同等级软件锁存，但协议不提供 USB 解锁命令。
- USB 不写入 `SbusState` 的 DMA 状态、帧计数、failsafe 或 CH9；下位机通过输入适配层把虚拟通道送入现有模式状态机。

### 3.2 虚拟通道定义

| 虚拟字段 | 等价实体通道 | 范围 | 当前框架含义 |
| --- | --- | --- | --- |
| `yaw_permille` | CH1 | `-1000..1000` | 负值左转，正值右转 |
| `forward_permille` | CH2 | `-1000..1000` | 负值后退，正值前进 |
| `speed_permille` | CH3 | `-1000..1000` | 同时决定步态档位和轮速上限 |
| `main_switch` | CH5/SB | `0=LOW,1=MID,2=HIGH` | 九宫格主模式 |
| `sub_switch` | CH8/SC | `0=LOW,1=MID,2=HIGH` | 九宫格子模式 |
| `arm_j0_permille` | CH6 | `-1000..1000` | STAND_ARM 下 J0 jog |
| `arm_j1_permille` | CH7 | `-1000..1000` | STAND_ARM 下 J1 jog |

CH3 只能有一个源值，不能同时独立指定步态档和轮速。下位机按当前规则换算：

```text
norm_ch3 = clamp(round(speed_permille / 10), -100, 100)

norm_ch3 <= -60 -> LOW
norm_ch3 >=  60 -> HIGH
其他             -> MID

wheel_max_rpm = clamp(norm_ch3 + 100, 0, 200)
```

上位机可以提供 `set_speed_profile()` 和 `set_wheel_max_rpm()` 便利方法，但两者最终必须更新同一个 `speed_permille`，后调用者覆盖前值。推荐直接暴露速度轴，避免生成实体遥控器无法产生的组合。

### 3.3 九宫格模式编号

`virtual_mode = main_switch * 3 + sub_switch`，与当前下位机 `sample.mode` 一致：

| 编号 | CH5+CH8 | 枚举 | 当前行为 |
| ---: | --- | --- | --- |
| 0 | LOW+LOW | `MOTOR_CHECK_IDLE` | 无动作检测/恢复准备，轮驱 OFF |
| 1 | LOW+MID | `LOW_WHEEL` | 机械限位低姿态轮行 |
| 2 | LOW+HIGH | `LOW_SAFE_HOLD` | 腿 RX-only/安全保持，轮驱 HOLD |
| 3 | MID+LOW | `STAND_HOLD` | MIT 站立，轮驱 HOLD |
| 4 | MID+MID | `STAND_WHEEL` | MIT 站立轮行 |
| 5 | MID+HIGH | `STAND_ARM` | 站立后机械臂 jog，轮驱 HOLD |
| 6 | HIGH+LOW | `GAIT_ONLY` | 纯点足步态，轮驱 HOLD |
| 7 | HIGH+MID | `GAIT_WHEEL` | 点足与轮驱相位同步 |
| 8 | HIGH+HIGH | `RESERVED_STAND` | 保留，保持站立和轮驱 HOLD |

模式切换帧必须先将所有轴归零。上位机持续发送目标九宫格，直到 `ROBOT_STATUS.active_mode` 等于请求编号且 `entry_state=ACTIVE`，之后才允许非零 CH1/CH2。下位机仍负责下降、站立、机械限位许可、收步和故障阻塞，USB 不得跳过这些过程。

### 3.4 VIRTUAL_RC_SETPOINT 线协议

沿用 USB 协议消息类型 `0x10`。正式名称为 `VIRTUAL_RC_SETPOINT`；旧文档中的 `CONTROL_SETPOINT` 视为同一消息的旧名称。payload 固定 28 字节：

| 偏移 | 类型 | 字段 | 说明 |
| ---: | --- | --- | --- |
| 0 | `uint32` | session_id | ACQUIRE 成功获得的会话 |
| 4 | `uint32` | host_time_ms | 上位机单调时钟低 32 位，仅诊断 |
| 8 | `uint8` | main_switch | `0..2` |
| 9 | `uint8` | sub_switch | `0..2` |
| 10 | `uint16` | command_flags | deadman、运动使能、平稳停止 |
| 12 | `int16` | yaw_permille | 等价 CH1，`-1000..1000` |
| 14 | `int16` | forward_permille | 等价 CH2，`-1000..1000` |
| 16 | `int16` | speed_permille | 等价 CH3，`-1000..1000` |
| 18 | `int16` | arm_j0_permille | 等价 CH6，`-1000..1000` |
| 20 | `int16` | arm_j1_permille | 等价 CH7，`-1000..1000` |
| 22 | `uint16` | channel_valid_mask | 位 0/1/2/5/6 对应 CH1/2/3/6/7 |
| 24 | `uint32` | command_counter | session 内严格递增 |

Python 编码格式：

```python
VIRTUAL_RC_FORMAT = "<IIBBHhhhhhHI"
assert struct.calcsize(VIRTUAL_RC_FORMAT) == 28
```

联调固定测试向量：session=`0x11223344`、host time=`0x01020304`、HIGH+LOW、flags=`0x0003`、CH1=-250、CH2=500、CH3=-800、CH6/7=0、mask=`0x0067`、counter=10、frame seq=`0x1234`。完整帧必须编码为：

```text
A5 5A 01 10 00 00 34 12 1C 00 44 33 22 11 04 03 02 01
02 00 03 00 06 FF F4 01 E0 FC 00 00 00 00 67 00 0A 00
00 00 CF 96
```

帧长 40 字节，payload 28 字节，CRC-16/CCITT-FALSE 为 `0x96CF`。上、下位机单元测试必须共同使用该向量。

`channel_valid_mask` v1 正常值为 `0x0067`。未置位通道必须按中位 0 处理，不能沿用旧值。未知位非 0 时下位机返回 `BAD_RANGE`。

`command_flags`：

| 位 | 名称 | 下位机行为 |
| ---: | --- | --- |
| 0 | `DEADMAN_HELD` | 自主许可/人工持续使能有效，允许连续非零通道 |
| 1 | `MOTION_ENABLE` | 允许下位机采用虚拟九宫格并进入非待机模式 |
| 2 | `SMOOTH_STOP` | CH1/CH2 强制归零并请求平稳收步 |

`MOTION_ENABLE=0` 时下位机必须把有效九宫格强制为 LOW+LOW、CH3 取最低值并把全部连续通道归零，不能触发站立等模式动作。`MOTION_ENABLE=1` 但 deadman 未保持时，允许进入/保持请求模式，但 CH1/CH2/CH6/CH7 为 0。只有两位同时置位，且 session、计数、CRC、物理授权和安全状态均有效时，连续非零通道才可生效。

### 3.5 安全零帧

连接、取得控制权、切换模式、任务暂停、状态不新鲜或释放控制时统一发送：

```text
main_switch       = LOW
sub_switch        = LOW
yaw               = 0
forward           = 0
speed             = -1000
arm_j0/arm_j1     = 0
flags             = SMOOTH_STOP（已有运动时）或 0
valid_mask        = 0x0067
```

急停不能只发送安全零帧，必须发送 `ESTOP_REQ`。普通任务取消先发至少 3 个安全零帧，再 RELEASE。

### 3.6 控制权与实体遥控仲裁

USB ACQUIRE 的物理授权条件保持：SBUS 新鲜、CH9 释放、实体 CH5/CH8=LOW+LOW、CH1/CH2 回中且安全锁存未激活。取得 USB 控制权后：

- 实体遥控继续只负责授权、CH9 和人工抢占。
- USB 的 `main_switch/sub_switch` 作为有效九宫格输入进入当前模式状态机。
- 实体 CH5/CH8 离开 LOW+LOW、实体 CH1/CH2 超过接管阈值、CH9 动作或 SBUS 丢失，立即撤销 USB session 并停止运动。
- 虚拟 LOW+LOW 只是模式请求，不能清除安全锁存；清锁仍依赖实体恢复流程。

### 3.7 下位机接入当前框架的最小改造

下位机保留 `Sbus_Process()` 和所有物理安全检查，新增：

```cpp
struct VirtualRcSample {
    int16_t ch1_yaw_permille;
    int16_t ch2_forward_permille;
    int16_t ch3_speed_permille;
    int16_t ch6_arm_j0_permille;
    int16_t ch7_arm_j1_permille;
    uint8_t main_switch;
    uint8_t sub_switch;
    uint16_t valid_mask;
    uint32_t update_ms;
};
```

推荐增加统一只读视图，而不是覆盖全局 `SbusState`：

```cpp
struct RemoteControlView {
    int16_t norm[SBUS_CHANNEL_COUNT];
    uint8_t main_switch;
    uint8_t sub_switch;
    uint8_t source;       // SBUS or USB
    uint32_t update_ms;
};
```

SBUS 适配器和 USB 适配器分别生成 `RemoteControlView`，现有 `sbus_mode_tick()`、`sbus_drive_input_from_sticks()`、`sbus_wheel_update()` 和机械臂 jog 改为读取该视图。CH9、SBUS fresh/failsafe 和安全锁存始终读取真实 `SbusState`，不得读取 USB 视图。

这项改造不改变现有九宫格状态机、轮腿控制参数、死区、斜率和故障恢复，只替换运动通道的来源。

### 3.8 上位机唯一写入接口

全自动任务层只能调用：

```python
class VirtualRemoteOutput:
    def set_mode(self, mode: RobotMode) -> None: ...
    def set_motion(self, forward: float, yaw: float) -> None: ...
    def set_speed_axis(self, value: float) -> None: ...
    def set_arm_jog(self, j0: float, j1: float) -> None: ...
    def refresh_autonomy_permit(self) -> None: ...
    def smooth_stop(self) -> None: ...
    async def emergency_stop(self, reason: int) -> None: ...
```

导航输出、视觉伺服输出和人工调试输入先由上位机仲裁器选择唯一来源，再更新这一个接口。任何 ROS 节点都不能自行打开 COM 口或编码 `0x10` 帧。

## 4. 全自动任务赛总体设计

### 4.1 需求归一化与必要修正

外部任务文档《26robocon上位机》是功能需求来源。实现前需要固定以下解释，否则坐标和安全逻辑无法唯一实现：

| 原始要求 | 本设计采用的明确解释 |
| --- | --- |
| 坐标以 mm 描述 | ROS 2、TF、Nav2 内部统一使用 m 和 rad，仅配置录入/界面可显示 mm |
| 雷达当前坐标设为原点 | 一键启动时发布静态变换，使当前 `base_link` 位姿成为 `match` 坐标系 `(0,0,0)` |
| 第 7 个物块写成 `(x0+0,x0+y0-1700)` | 视为笔误，采用 `(x0,y0-1700)` |
| 电子围栏第 4 点与第 3 点重复 | 采用矩形四角 `(x3,y3+2000)`、`(x3,y3-2000)`、`(x3+6000,y3+2000)`、`(x3+6000,y3-2000)` mm |
| 八个物块周围 700 mm 不可达 | 非当前目标物块保持半径 0.7 m 禁行；当前目标物块只允许沿规划的接近方向进入视觉对准区 |
| `kill` YOLO/颜色进程 | 使用 ROS 2 lifecycle 激活/停用，不使用无条件 kill；异常时由进程监督器重启 |
| D435 深度不再变化即抓取成功 | 深度稳定只是条件之一，还需机构反馈、目标消失/随机器人运动和超时判断 |
| 常态发使能，危险时发失能 | 运动许可由任务状态、SafetySupervisor 和下位机状态共同生成；严重超速使用锁存 ESTOP |

比赛规则若对坐标、区域编号或高分区映射有正式修订，以赛前锁定的场地配置文件为准，不在代码中散落常量。

当前 USB 虚拟遥控协议只提供 CH6/CH7 机械臂 jog，没有带完成反馈和幂等 operation ID 的吸取/放置动作。完成全自动抓放前，下位机必须额外提供第 4.12 节定义的机构 action；在该接口未完成前，软件只能完成导航和视觉对准，不能宣称完成全自动任务。

### 4.2 ROS 2 节点组成

```text
robocon_bringup
├── operator_ui_node              PySide6，一键启动、状态和急停
├── mission_manager_node          全局任务状态机，唯一任务决策者
├── math_solver_node              相机采集、PaddleOCR、SymPy、20s超时
├── localization_manager_node     TF原点、定位质量、速度估计
├── nav2 stack                    全局规划、局部控制、障碍代价地图
├── object_detector_node          YOLO + D435 深度，按需激活
├── exchange_detector_node        颜色掩码，按需激活
├── visual_servo_node             非完整机器人视觉伺服
├── quadruped_bridge_node         cmd_vel/任务模式 -> USB虚拟遥控器
├── manipulator_bridge_node       机构 action -> 下位机机构协议
├── safety_supervisor_node        围栏、速度、卡滞、心跳、故障
├── process_supervisor_node       lifecycle与进程健康
└── rosbag_recorder               关键topic录包
```

`mission_manager_node` 是唯一允许切换任务阶段的节点；`quadruped_bridge_node` 是唯一允许发送 USB 运动帧的节点；`safety_supervisor_node` 可以在任何阶段覆盖为零速度或 ESTOP。视觉节点不得直接访问串口。

### 4.3 坐标系和场地配置接口

TF 树固定为：

```text
match -> odom -> base_link -> lidar_link
                         ├── d435_color_optical_frame
                         ├── d435_depth_optical_frame
                         └── exchange_camera_optical_frame
```

- `odom -> base_link` 来自雷达定位节点，必须包含时间戳和协方差。
- 一键启动时记录当前 `odom -> base_link`，计算并发布 `match -> odom`，使当前机器人位置和朝向在 `match` 下为零。
- 所有任务点、围栏、禁行区和 Nav2 goal 使用 `match` 坐标系。
- 相机检测必须通过带时间戳的 TF 变换到 `match` 或 `base_link`，禁止使用最新 TF 代替图像采集时刻。

场地参数统一放在 `config/field.yaml`：

```yaml
frame_id: match
units: meter

anchors:
  block_origin:    {x: 0.0, y: 0.0}       # x0, y0
  exchange_origin: {x: 0.0, y: 0.0}       # x1, y1
  entrance_center: {x: 0.0, y: 0.0}       # x3, y3

block_offsets:
  - {id: 0, x: 0.85, y:  0.00}
  - {id: 1, x: 0.85, y: -0.85}
  - {id: 2, x: 0.85, y: -1.70}
  - {id: 3, x: 0.85, y: -2.55}
  - {id: 4, x: 0.00, y:  0.00}
  - {id: 5, x: 0.00, y: -0.85}
  - {id: 6, x: 0.00, y: -1.70}
  - {id: 7, x: 0.00, y: -2.55}

exchange_offsets:
  - {id: 0, x: 0.0, y:  0.0}
  - {id: 1, x: 0.0, y: -0.8}
  - {id: 2, x: 0.0, y: -1.6}
  - {id: 3, x: 0.0, y: -2.4}

geofence:
  min_x_from_entrance: 0.0
  max_x_from_entrance: 6.0
  min_y_from_entrance: -2.0
  max_y_from_entrance: 2.0

task:
  block_order: [0, 1, 2, 3, 4, 5, 6, 7]
  exclusion_radius_m: 0.70
  coarse_goal_tolerance_m: 0.50
  exchange_search_order: [0, 1, 2, 3]
  exchange_vision_enable_x_from_entrance_m: 3.50
```

UI 提供 x0/y0、x1/y1、x3/y3 的赛前录入与保存接口，但比赛开始后配置锁定。任何锚点未填写、越界或坐标单位不一致时禁止开始任务。

建议服务接口：

```text
/field_config/set_anchors    robocon_interfaces/srv/SetFieldAnchors
/field_config/get            robocon_interfaces/srv/GetFieldConfig
/localization/reset_match    std_srvs/srv/Trigger
```

`SetFieldAnchors.srv`：

```text
float64 block_x
float64 block_y
float64 exchange_x
float64 exchange_y
float64 entrance_x
float64 entrance_y
---
bool success
string message
```

### 4.4 ROS 2 公共接口总表

| 名称 | 类型 | 发布/服务方 | 使用方 | QoS/说明 |
| --- | --- | --- | --- | --- |
| `/match/start` | `std_srvs/srv/Trigger` | mission manager | UI | 只允许 WAIT_READY 状态调用 |
| `/match/cancel` | `std_srvs/srv/Trigger` | mission manager | UI/safety | 幂等，立即零运动并取消 action |
| `/match/state` | `robocon_interfaces/msg/TaskState` | mission manager | UI/录包 | reliable + transient local |
| `/math_solver/solve` | `robocon_interfaces/action/SolveMath` | math solver | mission manager | 20 s deadline |
| `/math_result` | `robocon_interfaces/msg/MathResult` | math solver | UI/mission | reliable + transient local |
| `/localization/pose` | `geometry_msgs/msg/PoseWithCovarianceStamped` | 定位 | localization/safety/nav | sensor QoS |
| `/localization/odometry` | `nav_msgs/msg/Odometry` | localization manager | nav/safety/UI | 20 Hz 以上 |
| `/scan` | `sensor_msgs/msg/LaserScan` | lidar | Nav2 | sensor QoS |
| `/camera/color/image_raw` | `sensor_msgs/msg/Image` | D435 | OCR/YOLO | sensor QoS |
| `/camera/aligned_depth_to_color/image_raw` | `sensor_msgs/msg/Image` | D435 | YOLO/抓取确认 | sensor QoS |
| `/object_detection` | `robocon_interfaces/msg/ObjectDetection` | YOLO | mission/visual servo | 只发布新鲜检测 |
| `/exchange_detection` | `robocon_interfaces/msg/ExchangeDetection` | color detector | mission/visual servo | 只发布新鲜检测 |
| `/visual_servo/align` | `robocon_interfaces/action/VisualAlign` | visual servo | mission manager | 可取消 |
| `/cmd_vel_nav` | `geometry_msgs/msg/TwistStamped` | Nav2 | quadruped bridge | 超时 100 ms |
| `/cmd_vel_visual` | `geometry_msgs/msg/TwistStamped` | visual servo | quadruped bridge | 超时 100 ms，优先于 nav |
| `/quadruped/set_mode` | `robocon_interfaces/srv/SetRobotMode` | quadruped bridge | mission manager | 等待 active mode |
| `/quadruped/status` | `robocon_interfaces/msg/QuadrupedStatus` | quadruped bridge | mission/safety/UI | 20 Hz |
| `/manipulator/execute` | `robocon_interfaces/action/ManipulatorCommand` | manipulator bridge | mission manager | 可取消、带反馈 |
| `/manipulator/status` | `robocon_interfaces/msg/ManipulatorStatus` | manipulator bridge | mission/safety/UI | 20 Hz |
| `/safety/state` | `robocon_interfaces/msg/SafetyState` | safety supervisor | 所有节点/UI | reliable + transient local |
| `/safety/estop` | `std_srvs/srv/Trigger` | safety supervisor | UI/mission | 只建立急停，不解除 |
| `/system/health` | `diagnostic_msgs/msg/DiagnosticArray` | 各节点 | supervisor/UI | 1 Hz 以上 |

所有运动相关 action 和 service 都必须支持超时；mission manager 取消任务时先停止运动，再取消视觉和机构动作。

### 4.5 自定义消息和 Action 定义

`MathResult.msg`：

```text
builtin_interfaces/Time stamp
string expression
int64 result
uint8 high_score_zone
float32 confidence
bool valid
string failure_reason
```

`SolveMath.action`：

```text
float32 timeout_s
---
robocon_interfaces/MathResult answer
---
uint8 attempt
string recognized_text
float32 best_confidence
```

`ObjectDetection.msg`：

```text
std_msgs/Header header
uint32 track_id
uint8 block_id
uint8 class_id
uint8 exchange_color_id
float32 confidence
float32 center_u
float32 center_v
float32 bbox_x
float32 bbox_y
float32 bbox_width
float32 bbox_height
float32 depth_m
geometry_msgs/PoseStamped pose
bool center_inside_bbox
```

`ExchangeDetection.msg`：

```text
std_msgs/Header header
uint8 station_id
uint8 color_id
float32 confidence
float32 center_u
float32 center_v
float32 area_ratio
geometry_msgs/PoseStamped pose
```

`VisualAlign.action`：

```text
uint8 target_kind          # 0=BLOCK, 1=EXCHANGE
uint8 expected_id
float32 desired_depth_m
float32 max_linear_mps
float32 max_yaw_rps
float32 timeout_s
---
bool success
uint8 result_code
string message
---
float32 pixel_error_x
float32 depth_error_m
float32 confidence
bool stable
```

`TaskState.msg`：

```text
builtin_interfaces/Time stamp
uint8 mission_state
uint8 current_block_index
uint8 current_exchange_index
uint8 retry_count
float32 state_elapsed_s
bool motion_allowed
string detail
```

`SetRobotMode.srv`：

```text
uint8 mode
float32 timeout_s
---
bool success
uint8 active_mode
uint8 entry_state
uint8 block_reason
string message
```

### 4.6 一键启动和 20 秒智力题

UI 一键启动只允许执行一次，调用前必须满足：USB HELLO 正常、SBUS 授权条件满足、定位/TF/相机就绪、场地锚点有效、磁盘空间足够且 SafetyState 允许启动。

启动顺序：

1. 原子锁定场地配置和比赛参数。
2. 调用 `/localization/reset_match`，确认 `match -> odom` 已发布。
3. 启动 rosbag 录制并写入本次配置快照。
4. 发送 `SolveMath` goal，deadline 固定 20 秒。
5. 智力题期间四足保持 `STAND_HOLD` 或规则要求的待机模式，运动输出强制为零。
6. OCR 成功后用严格解析器计算，发布 MathResult、TTS 播报并显示 UI。
7. 结果 `high_score_zone = mathematical_mod(result, 4)`，负数也必须得到 `0..3`。
8. 20 秒到期无论成功与否都取消 OCR action、释放相机资源并进入自动任务；失败结果不得阻止比赛开始。

表达式计算禁止 Python `eval`。只接受整数、`+ - * / ( )`，用 tokenizer + SymPy 安全解析，限制表达式长度、整数位数、AST 节点数和计算时间；除零、非整数答案或低置信度进入一次重拍，仍失败则超时降级。

`high_score_zone` 的具体比赛用途没有在需求中明确。本设计将其作为任务策略输入保存并发布，不擅自改变物块颜色到兑换区的映射；若正式规则要求优先访问该区，通过 `strategy.yaml` 配置。

### 4.7 全自动任务状态机

```text
BOOT
 -> WAIT_READY
 -> MATH_SOLVE
 -> ACQUIRE_CONTROL
 -> SELECT_BLOCK
 -> NAV_BLOCK_STAGING
 -> START_OBJECT_VISION
 -> VISUAL_ALIGN_BLOCK
 -> GRAB
 -> VERIFY_GRAB
 -> NAV_EXCHANGE_GATE
 -> SEARCH_EXCHANGE
 -> VISUAL_ALIGN_EXCHANGE
 -> PLACE
 -> VERIFY_PLACE
 -> SELECT_BLOCK ...
 -> MISSION_COMPLETE

任意运动状态 -> SAFE_STOP -> FAULT
```

各状态的进入条件、输出和退出条件：

| 状态 | 主要动作 | 成功条件 | 超时/失败行为 |
| --- | --- | --- | --- |
| WAIT_READY | 检查全部依赖和场地配置 | readiness 全部为 true | 禁止一键启动 |
| MATH_SOLVE | OCR/计算/TTS | 成功或 20 s 到期 | 记录失败后继续 |
| ACQUIRE_CONTROL | USB ACQUIRE，设置 LOW 档 | source=USB、session 匹配 | 1 次重试，仍失败则 FAULT |
| SELECT_BLOCK | 从 block_order 选未完成块 | 得到目标和 staging pose | 无目标则完成比赛 |
| NAV_BLOCK_STAGING | Nav2 到目标附近 `+/-0.5 m` | action 成功且定位新鲜 | 重规划 2 次，之后跳过/FAULT 按策略 |
| START_OBJECT_VISION | 激活 YOLO，等待检测 | 连续 3 帧同类检测 | 3 s 无检测执行小角度搜索 |
| VISUAL_ALIGN_BLOCK | 低速视觉伺服 | 像素、深度误差稳定 300 ms | 10 s 取消并退回 staging |
| GRAB | 机构进入抓取动作 | action 报告已吸取/夹持 | 最多 2 次 |
| VERIFY_GRAB | 融合机构和视觉/深度 | 连续 500 ms 满足抓取判据 | 失败则放开并重抓 |
| NAV_EXCHANGE_GATE | 停用 YOLO，导航至兑换区侧 | 到达且 x 条件满足 | 重规划/安全停止 |
| SEARCH_EXCHANGE | 激活颜色检测，按站点顺序检查 | 颜色与目标映射匹配 | 四站均失败则 FAULT |
| VISUAL_ALIGN_EXCHANGE | 视觉低速对准放置位 | 稳定 300 ms | 退回并重试一次 |
| PLACE | 机构放置 | action 成功 | 重试一次 |
| VERIFY_PLACE | 机构空载且物块留在区域 | 连续 500 ms | 重新放置或标记人工复核 |
| SAFE_STOP | 取消导航/视觉/机构，零运动 | 下位机 applied axes=0 | 必要时 ESTOP |

每个状态必须有本地单调时钟 deadline、取消处理和明确的最大重试次数。禁止使用没有退出条件的 `while` 循环。

### 4.8 导航、避障和大范围运动

大步流星模式使用 Nav2，不手写全局路径规划器。任务管理器计算物块 staging pose：目标点距离物块中心 0.4~0.5 m，朝向物块；到达容差按需求设为 x/y 各 `+/-0.5 m`，但切换视觉前还必须满足定位有效和局部速度低于 0.1 m/s。

Nav2 配置要求：

- global costmap 使用场地静态禁行区和 geofence filter。
- local costmap 使用雷达 obstacle/voxel layer 和 inflation layer。
- 七个非目标物块加入半径 0.7 m keepout；当前目标只保留其他物块禁区并规划到 staging pose。
- quadruped 是非完整约束模型，局部控制器不得输出 `linear.y`；优先使用适合差速/非完整底盘的 Nav2 controller。
- Nav2 `/cmd_vel_nav` 超过 100 ms 未更新，bridge 立即发零输入。
- 正常自主速度上限建议 1.2 m/s，留出低于比赛 1.5 m/s 限制的裕量。

`quadruped_bridge_node` 将 `TwistStamped` 转成虚拟遥控输入：

```text
forward = clamp(linear.x / configured_max_linear_mps, -1, 1)
yaw     = clamp(angular.z / configured_max_yaw_rps, -1, 1)
linear.y 必须为 0，否则拒绝命令并报告诊断
```

bridge 仍使用第 4 节的 session、50 Hz 调度和 deadman 逻辑。自主 deadman 不是 UI 按键，而是每 50 ms 由 MissionManager 更新的 `AutonomyPermit` 租约；Permit 超过 100 ms 未刷新立即清零，不能把 `motion_enable=true` 永久锁死。

### 4.9 视觉识别和视觉辅助运动

物块 YOLO 节点只在接近物块阶段激活。它输出类别、对应兑换颜色、bbox、中心像素、对齐深度和相机坐标位姿。进入视觉伺服前要求同一 track 连续 3 帧、置信度不低于配置阈值、深度有效且时间戳年龄不超过 150 ms。

视觉伺服只输出前后和 yaw，不输出横移：

```text
pixel_error = target_center_u - image_center_u
yaw_cmd = clamp(K_yaw * pixel_error, -0.4, 0.4) rad/s

depth_error = depth_m - desired_depth_m
仅当 abs(pixel_error) < yaw_gate_px 时允许前进
linear_cmd = clamp(K_depth * depth_error, -0.20, 0.25) m/s
```

对准成功必须连续 300 ms 满足：目标中心在 bbox 有效区域内、`abs(pixel_error)` 小于阈值、深度误差小于阈值、检测置信度达标且机器人速度已经降低。目标丢失超过 200 ms 立即零速；超过 1 s 取消视觉对准并退回搜索。

兑换区颜色节点只在机器人满足 `x >= x3 + 3.5 m` 且任务进入 SEARCH_EXCHANGE 后激活。按配置顺序检查四个站点，颜色需要连续多帧投票，不允许单帧命中直接放置。颜色 ID 到物块 class ID 的映射放在 `strategy.yaml`。

### 4.10 抓取和放置确认

机械臂/吸取机构按需求定义六个对外状态：

```text
DISABLED
INITIAL
VISION_RECOGNITION
VISUAL_ALIGNMENT
GRABBING
PLACING
```

抓取成功采用多条件确认：

- 下位机机构状态为 HOLDING，真空压力/夹持电流达到阈值。
- D435 目标深度在 500 ms 窗口内稳定，且符合“物块已贴近末端”的距离范围。
- 原物块在地面的检测消失，或检测位姿随机器人/末端同步移动。
- 全部样本新鲜，连续满足至少 500 ms。

仅“深度数值不变”不能判定成功，因为相机冻结、物块未动或深度空洞都会产生相同现象。相机帧序号和时间戳停止更新应判定传感器故障。

放置成功要求机构回到空载、目标离开末端近场，并在兑换区 ROI 中检测到静止物块。抓放 action 超时后先零运动，再执行机构安全回退；不得边走边重试机构。

### 4.11 安全监督器

SafetySupervisor 独立于任务状态机，每 20 ms 评估一次：

| 条件 | 判定 | 动作 |
| --- | --- | --- |
| 越出电子围栏 | 当前 pose 超出矩形，含 0.1 m 预警边界 | 立即零运动、取消 Nav2、进入 FAULT |
| 定位失联 | pose 年龄 >200 ms 或 TF 不可用 | 立即零运动 |
| 定位质量差 | xy/yaw 协方差超过配置阈值 | 降速；持续 1 s 则停止 |
| 命令但不移动 | `abs(v_cmd)>0.15 m/s` 持续 5 s，位移 <0.05 m | 零运动并取消导航，最多重规划一次 |
| 超过比赛限速 | 融合速度 >1.5 m/s 持续 100 ms | 零运动、制动并记录违规风险 |
| 严重超速 | 融合速度 >2.5 m/s 任一可信样本或连续 2 帧 | 发送 ESTOP，锁存故障 |
| 动态障碍过近 | local costmap/雷达触发停止距离 | 零运动，由 Nav2 重规划 |
| USB 状态失联 | RobotStatus >300 ms | 清 session、零运动 |
| 下位机安全/故障位 | safety_flags 或相关 fault 上升 | 零运动，按严重度 FAULT/ESTOP |
| 关键进程退出 | localization/bridge/safety/mission 异常 | 零运动；bridge/safety 退出由下位机看门狗兜底 |

速度由雷达 odometry 和位置差分融合，必须剔除时间戳跳变与定位跳点。严重超速使用 ESTOP，不使用普通 RELEASE 冒充失能。安全故障解除后不自动恢复原任务，必须由操作员检查并重新开始或执行明确的恢复流程。

### 4.12 机构下位机接口前置条件

为使上位机真的完成抓放，下位机至少需要提供以下动作语义。可以扩展 USB 二进制协议，也可以使用独立可靠总线，但上位机 ROS action 行为必须一致：

`ManipulatorCommand.action`：

```text
uint8 command              # 0=INITIAL,1=VISION,2=ALIGN,3=GRAB,4=PLACE,5=DISABLE
uint32 operation_id        # 会话内递增，用于幂等
float32 timeout_s
---
bool success
uint8 final_state
uint16 fault_code
string message
---
uint8 current_state
float32 progress
bool object_detected
float32 vacuum_kpa
float32 gripper_current_a
```

要求：

- GRAB/PLACE 使用 `operation_id` 幂等，重发不能执行两次机构动作。
- action 可取消；取消后机构进入安全保持或 INITIAL，不得继续旧动作。
- 状态反馈至少 20 Hz，反馈超时 200 ms 时上位机停止底盘。
- DISABLE 和严重故障最高优先级；USB 四足 ESTOP 同时应停止机构输出。
- 下位机必须提供真实机构反馈，不能仅按命令发送成功即返回 success。

### 4.13 UI、一键启动和人工接管

第一屏是比赛操作界面，不做营销页。至少包含：

- 一键启动、取消任务和始终可见的软件急停。
- 数学题原图、OCR 文本、表达式、答案、高分区和剩余 20 秒。
- 当前任务状态、当前物块/兑换站、重试次数和状态耗时。
- `match` 坐标、朝向、速度、定位质量和 TF 新鲜度。
- USB/session/下位机模式、应用速度、腿/轮/机构状态和安全原因。
- 各 ROS 节点 lifecycle/进程健康、相机/雷达帧率和 rosbag 状态。
- 场地锚点 x0/y0、x1/y1、x3/y3 的赛前配置与校验。

比赛启动后禁用锚点编辑。人工虚拟遥控只在“调试模式”中出现，并与自动任务互斥；切入人工模式前必须取消任务、停止 Nav2 和视觉伺服、清零 3 帧后重新申请控制权。不能让 UI 人工摇杆与自动 `/cmd_vel` 同时写入 bridge。

### 4.14 开机自启动、生命周期和录包

使用 systemd 启动 `robocon_bringup` 和 UI，开机只进入 WAIT_READY，不自动取得控制权或运动。雷达、相机和定位节点可自动启动；YOLO 与颜色识别使用 lifecycle INACTIVE，任务需要时再激活。

启动依赖顺序：驱动 -> TF/定位 -> Nav2 -> USB bridge -> safety -> mission -> UI。任何依赖未 READY 时 UI 显示具体原因。

比赛一键启动时录制：

```text
/tf /tf_static
/localization/pose /localization/odometry /scan
/math_result /object_detection /exchange_detection
/cmd_vel_nav /cmd_vel_visual
/quadruped/status /manipulator/status
/match/state /safety/state /system/health
```

图像使用压缩 topic 或限制录包频率，设置磁盘剩余空间下限和文件分段。录包失败不应阻塞控制，但必须在 UI 明确报警。每次任务同时保存 field.yaml、strategy.yaml、软件 git commit、模型版本和下位机固件版本。

### 4.15 全自动任务验收

先仿真/HIL，再架空，再实地：

1. 单元测试坐标生成、取模、消息编解码和所有状态 deadline。
2. 用 rosbag 回放定位、图像和深度，验证任务结果可重复。
3. Gazebo/仿真验证 Nav2 禁行圆、围栏、动态障碍和取消行为。
4. USB 下位机仿真器注入超时、乱序、重启、安全位和机构失败。
5. 架空验证 bridge 映射、150 ms 下位机看门狗、实体接管和 ESTOP。
6. 单物块完成“导航-识别-对准-抓取-兑换-放置”闭环。
7. 连续八物块运行，验证内存、温度、模型进程和录包空间。
8. 注入相机冻结、雷达断线、定位跳点、Nav2 卡死、USB 拔线、机构不响应和 UI 崩溃。
9. 任何故障均不得保留最后非零命令，恢复后不得自动续跑旧动作。

全自动版本的完成判据不是 UI 能发虚拟遥控，而是任务管理器能够在无人工运动操作的情况下完成全部物块循环，并在每一种单点故障下进入可解释、可复现的安全状态。

## 5. 上位机虚拟遥控器 SDK

本节定义上位机内部对 UI、键盘、游戏手柄和自动化测试开放的完整接口。实现语言可以不同，但公开行为、取值范围、线程语义和安全规则必须一致。

### 5.1 枚举

```python
from enum import IntEnum, IntFlag

class ConnectionState(IntEnum):
    DISCONNECTED = 0
    CONNECTING = 1
    READY = 2             # HELLO 成功，尚未取得控制权
    ACQUIRED = 3          # session 有效
    LINK_LOST = 4

class ControlSource(IntEnum):
    NONE = 0
    SBUS = 1
    USB = 2

class RobotMode(IntEnum):
    MOTOR_CHECK_IDLE = 0
    LOW_WHEEL = 1
    LOW_SAFE_HOLD = 2
    STAND_HOLD = 3
    STAND_WHEEL = 4
    STAND_ARM = 5
    GAIT_ONLY = 6
    GAIT_WHEEL = 7
    RESERVED_STAND = 8

class SwitchState(IntEnum):
    LOW = 0
    MID = 1
    HIGH = 2

class SpeedProfile(IntEnum):
    LOW = 0
    MID = 1
    HIGH = 2

class EntryState(IntEnum):
    INACTIVE = 0
    ENTERING = 1
    WAIT = 2
    ACTIVE = 3
    BLOCKED = 4

class LinkState(IntEnum):
    DOWN = 0
    READY = 1
    ACQUIRED = 2
    TIMEOUT = 3

class CommandFlags(IntFlag):
    DEADMAN_HELD = 1 << 0
    MOTION_ENABLE = 1 << 1
    SMOOTH_STOP = 1 << 2

class SafetyFlags(IntFlag):
    LATCHED = 1 << 0
    CH9_INHIBIT = 1 << 1
    SBUS_NOT_FRESH = 1 << 2
    USB_TIMEOUT = 1 << 3
    PHYSICAL_MODE_NOT_IDLE = 1 << 4
    PHYSICAL_STICKS_NOT_NEUTRAL = 1 << 5
    WHEEL_LOCKED = 1 << 6
    LEG_PROTECTION_HOLD = 1 << 7

class FaultFlags(IntFlag):
    LEG_MOTOR = 1 << 0
    LEG_FEEDBACK_TIMEOUT = 1 << 1
    WHEEL = 1 << 2
    WHEEL_FEEDBACK_TIMEOUT = 1 << 3
    CAN_BUS_OFF = 1 << 4
    OVERTEMPERATURE = 1 << 5
    MODE_ENTRY_FAILED = 1 << 6

class ResultCode(IntEnum):
    OK = 0
    BAD_VERSION = 1
    BAD_LENGTH = 2
    BAD_RANGE = 3
    BAD_SESSION = 4
    NOT_AUTHORIZED = 5
    BUSY = 6
    SAFETY_LATCHED = 7
    STALE_COMMAND = 8
    UNSUPPORTED = 9
```

不得用裸整数绕过枚举检查。收到未知枚举值时将状态标记为协议不兼容、清除运动使能，并保留原始值供日志记录。

### 5.2 虚拟遥控输入模型

```python
from dataclasses import dataclass

@dataclass(frozen=True)
class VirtualRemoteInput:
    mode: RobotMode = RobotMode.MOTOR_CHECK_IDLE
    forward: float = 0.0          # [-1.0, 1.0]，正值前进
    yaw: float = 0.0              # [-1.0, 1.0]，正值右转
    speed_axis: float = -1.0      # [-1.0, 1.0]，等价 CH3
    arm_j0: float = 0.0           # [-1.0, 1.0]，等价 CH6
    arm_j1: float = 0.0           # [-1.0, 1.0]，等价 CH7
    deadman_held: bool = False
    motion_enable: bool = False
    smooth_stop: bool = False
```

该对象是 UI/输入设备与 50 Hz 调度器之间唯一的运动数据接口。更新时必须整体替换不可变快照，禁止分别写 forward、yaw 和 deadman 导致调度器读到一半新、一半旧的数据。

输入归一化必须在写入快照前完成：

- `forward/yaw/speed_axis/arm_j0/arm_j1` 非有限值直接变为安全值，并产生输入错误事件。
- 超出 `[-1,1]` 的值限幅。
- `MOTOR_CHECK_IDLE`、`LOW_SAFE_HOLD`、`STAND_HOLD`、`STAND_ARM`、`RESERVED_STAND` 模式强制把 forward/yaw 置零。
- 非 `STAND_ARM` 模式强制把 arm_j0/arm_j1 置零。
- deadman 或 motion enable 任一个为 false 时，调度器发送的 forward/yaw/arm_j0/arm_j1 必须为零。

### 5.3 状态模型

```python
@dataclass(frozen=True)
class RobotStatus:
    received_monotonic_s: float
    uptime_ms: int
    session_id: int
    last_command_counter: int
    last_rx_seq: int
    control_source: ControlSource
    requested_mode: RobotMode
    active_mode: RobotMode
    entry_state: EntryState
    speed_profile: SpeedProfile
    link_state: LinkState
    safety_flags: SafetyFlags
    fault_flags: FaultFlags
    applied_forward: float
    applied_yaw: float
    applied_wheel_max_rpm: int
    setpoint_age_ms: int
    leg_online_mask: int
    wheel_online_mask: int
    block_reason: int
    rx_crc_error_count: int
    rx_drop_count: int

@dataclass(frozen=True)
class DeviceInfo:
    port: str
    usb_serial: str
    device_id: int
    firmware_version: int
    protocol_version: int
    capability_bits: int
    status_hz: int

@dataclass(frozen=True)
class UsbDeviceCandidate:
    port: str
    usb_serial: str
    vid: int
    pid: int
    product: str

@dataclass(frozen=True)
class DecodedFrame:
    version: int
    msg_type: int
    flags: int
    seq: int
    payload: bytes

@dataclass(frozen=True)
class HelloResponse:
    client_nonce: int
    device_id: int
    firmware_version: int
    capability_bits: int
    status_hz: int
    max_payload: int
    min_protocol: int
    max_protocol: int

@dataclass(frozen=True)
class AcquireResponse:
    request_seq: int
    result: ResultCode
    control_source: ControlSource
    session_id: int
    granted_lease_ms: int
    status_period_ms: int

@dataclass(frozen=True)
class Ack:
    acked_seq: int
    acked_type: int
    result: ResultCode
    detail: int
```

`RobotStatus` 必须保留本机接收时间 `received_monotonic_s`。判断状态是否新鲜时使用该时间，不能使用 `uptime_ms` 或界面刷新时间。

安全位定义：

| 位 | 名称 | 上位机行为 |
| ---: | --- | --- |
| 0 | safety latched | 清除运动使能，禁止重新取得控制权 |
| 1 | CH9 inhibit | 清除运动使能，提示释放实体急停 |
| 2 | SBUS not fresh | 清除运动使能，USB 会话视为不可用 |
| 3 | USB control timeout | 销毁本地 session，要求重新 ACQUIRE |
| 4 | physical mode not LOW+LOW | 清除运动使能，视为实体遥控接管 |
| 5 | physical sticks not neutral | 清除运动使能，视为实体遥控接管 |
| 6 | wheel locked | 禁止轮行模式非零输入 |
| 7 | leg protection hold | 禁止步态模式非零输入 |

故障位定义：位 0=腿电机故障，位 1=腿反馈超时，位 2=轮驱故障，位 3=轮反馈超时，位 4=CAN bus-off，位 5=过温，位 6=模式进入失败。任何新增未知故障位也必须触发本地运动使能清除。

### 5.4 对 UI 和任务层开放的控制器接口

推荐实现以下异步接口；同步语言可以返回 Future/Task，但不能在 UI 线程阻塞等待串口：

```python
from collections.abc import Callable

class VirtualRemoteController:
    def list_devices(self) -> list[UsbDeviceCandidate]: ...

    async def connect(self, usb_serial: str | None = None) -> DeviceInfo: ...
    async def disconnect(self) -> None: ...

    async def acquire_control(self, lease_ms: int = 1500) -> int: ...
    async def release_control(self) -> None: ...

    def set_mode(self, mode: RobotMode) -> None: ...
    def set_axes(self, forward: float, yaw: float) -> None: ...
    def set_speed_axis(self, value: float) -> None: ...
    def set_speed_profile(self, profile: SpeedProfile) -> None: ...
    def set_wheel_max_rpm(self, rpm: int) -> None: ...
    def set_arm_jog(self, j0: float, j1: float) -> None: ...
    def set_motion_enable(self, enabled: bool) -> None: ...
    def set_deadman(self, held: bool) -> None: ...
    def refresh_autonomy_permit(self) -> None: ...
    def smooth_stop(self) -> None: ...

    async def emergency_stop(self, reason: int) -> None: ...
    async def request_text_status(self) -> str: ...       # ASCII p
    async def request_sbus_diagnostics(self) -> str: ...  # ASCII Y

    def get_connection_state(self) -> ConnectionState: ...
    def get_input_snapshot(self) -> VirtualRemoteInput: ...
    def get_latest_status(self) -> RobotStatus | None: ...

    def on_status(self, callback: Callable[[RobotStatus], None]) -> Callable[[], None]: ...
    def on_connection_changed(self, callback: Callable[[ConnectionState], None]) -> Callable[[], None]: ...
    def on_control_revoked(self, callback: Callable[[str], None]) -> Callable[[], None]: ...
    def on_error(self, callback: Callable[[Exception], None]) -> Callable[[], None]: ...
```

接口行为：

- `connect()` 完成端口打开和 HELLO；成功返回时状态必须是 READY。
- `list_devices()` 只返回操作系统枚举信息；固件、能力和 device_id 只有 HELLO 后才出现在 `DeviceInfo`。
- `disconnect()` 先清零输入并尝试 RELEASE，再关闭端口；该方法必须幂等。
- `acquire_control()` 只在 READY 调用，成功返回 `session_id`；失败抛出包含 `ResultCode` 的异常。
- `release_control()` 先发 3 个零输入平滑停止帧，再释放 session；重复调用不报错。
- 所有 `set_*()` 只更新输入快照，不直接写串口。唯一发送 `VIRTUAL_RC_SETPOINT` 的组件是 50 Hz 调度器。
- `set_speed_axis()` 是 CH3 的主接口；`set_speed_profile()` 写入规范值 LOW=-1、MID=0、HIGH=1，`set_wheel_max_rpm()` 按 `(rpm-100)/100` 反算同一 speed_axis，后调用覆盖前调用。
- `refresh_autonomy_permit()` 只供任务管理器使用，100 ms 内未刷新时等价 `set_deadman(False)`。
- `set_deadman(False)` 必须在返回前原子地把轴清零；不能等待下一个 UI 事件。
- `set_motion_enable(False)` 同样原子清零并设置 smooth stop。
- `set_mode()` 必须原子清零两个轴并设置 smooth stop，再更新请求模式；只有状态确认新模式 ACTIVE 后才能重新放行非零输入。
- `smooth_stop()` 清零轴并至少保持 `SMOOTH_STOP` 三个发送周期，然后保持零输入。
- `emergency_stop()` 不依赖 session，在任何已连接状态都能调用；它不得提供对应的解除接口。
- 四个订阅方法返回取消订阅函数。回调统一投递到 UI 事件队列，不能在串口读线程直接执行耗时逻辑。

建议异常类型：

```python
class VirtualRemoteError(Exception): ...
class DeviceNotFoundError(VirtualRemoteError): ...
class HandshakeTimeoutError(VirtualRemoteError): ...
class ProtocolMismatchError(VirtualRemoteError): ...
class AcquireRejectedError(VirtualRemoteError):
    result: ResultCode
    detail: int
class LinkLostError(VirtualRemoteError): ...
class InvalidOperationError(VirtualRemoteError): ...
```

### 5.5 输入设备适配器接口

键盘、手柄和后续网络操纵端不得直接调用协议编码器，只能产生标准输入事件：

```python
@dataclass(frozen=True)
class InputDeviceSample:
    connected: bool
    forward: float
    yaw: float
    deadman_held: bool
    sample_monotonic_s: float

class InputDeviceAdapter:
    def start(self, publish: Callable[[InputDeviceSample], None]) -> None: ...
    def stop(self) -> None: ...
```

适配器规则：

- 手柄轴事件至少以 50 Hz 采样，推荐 100 Hz；控制发送仍固定为 50 Hz。
- 输入样本超过 100 ms 未更新，视为输入设备失联，立即发布零轴且 deadman=false。
- 键盘窗口失焦、手柄拔出、适配器停止或发生异常，都必须发布同样的安全零样本。
- 只能有一个活动输入适配器；切换输入设备时先清零，再启用新设备。
- 自动化测试可以实现同一接口，但正式 UI 中不得允许脚本绕过 deadman。

### 5.6 方法到 USB 消息的映射

| 应用层操作 | USB 消息 | 发送策略 | 成功判据 |
| --- | --- | --- | --- |
| `connect()` | `HELLO_REQ` | 发送一次，1 秒超时 | nonce 匹配的 `HELLO_RESP` |
| `acquire_control()` | `ACQUIRE_REQ` | 500 ms 超时，最多重试一次 | `ACQUIRE_RESP.result=OK` 且 session 非 0 |
| 调度器 tick | `VIRTUAL_RC_SETPOINT` | ACQUIRED 时固定 50 Hz | status 回显 command_counter |
| `smooth_stop()` | `VIRTUAL_RC_SETPOINT` | 至少 3 帧零轴 + SMOOTH_STOP | applied axes 为零 |
| `release_control()` | `RELEASE_REQ` | 停止帧之后发送 | ACK 或本地 300 ms 超时 |
| `emergency_stop()` | `ESTOP_REQ` | 3 帧，新 seq，间隔 20 ms | 任一 ACK 或状态 safety latched |
| 状态频率配置 | `STATUS_CONFIG` | HELLO 后一次 | 后续状态周期符合配置 |
| `request_text_status()` | ASCII `p` | 人工调用一次 | 收到一次文本响应 |
| `request_sbus_diagnostics()` | ASCII `Y` | 人工调用一次 | 收到一次文本响应 |

### 5.7 完整线协议编码接口

上位机协议模块至少暴露下列常量和编解码函数：

```python
MAGIC = b"\xA5\x5A"
PROTOCOL_VERSION = 1
MAX_PAYLOAD = 96

HELLO_REQ = 0x01
ACQUIRE_REQ = 0x02
RELEASE_REQ = 0x03
VIRTUAL_RC_SETPOINT = 0x10
ESTOP_REQ = 0x12
STATUS_CONFIG = 0x20
HELLO_RESP = 0x81
ACQUIRE_RESP = 0x82
ROBOT_STATUS = 0x90
ACK = 0xE0

ACK_REQ = 1 << 0
RESPONSE = 1 << 1

def encode_frame(msg_type: int, flags: int, seq: int, payload: bytes) -> bytes: ...
def feed_bytes(data: bytes) -> list[DecodedFrame]: ...
def crc16_ccitt_false(data: bytes) -> int: ...

def pack_hello_req(client_nonce: int, desired_status_hz: int) -> bytes: ...
def unpack_hello_resp(payload: bytes) -> HelloResponse: ...
def pack_acquire_req(client_nonce: int, lease_ms: int) -> bytes: ...
def unpack_acquire_resp(payload: bytes) -> AcquireResponse: ...
def pack_release_req(session_id: int) -> bytes: ...
def pack_setpoint(session_id: int, command: VirtualRemoteInput,
                  host_time_ms: int, command_counter: int) -> bytes: ...
def pack_estop(reason: int) -> bytes: ...
def pack_status_config(status_hz: int) -> bytes: ...
def unpack_robot_status(payload: bytes, received_time: float) -> RobotStatus: ...
def unpack_ack(payload: bytes) -> Ack: ...
```

Python `struct` 格式必须如下，其他语言按相同顺序和小端编码：

| 对象 | `struct` 格式 | 字节数 |
| --- | --- | ---: |
| magic 后的帧头 | `<BBBBHH` | 8 |
| HELLO_REQ | `<IHH` | 8 |
| HELLO_RESP | `<IIIIHHBBH` | 24 |
| ACQUIRE_REQ | `<IHH` | 8 |
| ACQUIRE_RESP | `<HBBIHH` | 12 |
| RELEASE_REQ | `<I` | 4 |
| VIRTUAL_RC_SETPOINT | `<IIBBHhhhhhHI` | 28 |
| ESTOP_REQ | `<HH` | 4 |
| STATUS_CONFIG | `<HH` | 4 |
| ROBOT_STATUS | `<IIIHBBBBBBHHhhHHHBBII` | 44 |
| ACK | `<HBBHH` | 8 |

通用帧为：`magic(2) + frame_header(8) + payload(N) + crc16(2)`。帧头字段顺序为 `version, msg_type, flags, reserved, seq, payload_len`。CRC 范围从 version 开始，到 payload 最后一个字节结束，CRC 本身按小端附加。

`pack_setpoint()` 的字段映射必须固定：

| 顺序 | 字段 | 来源/转换 |
| ---: | --- | --- |
| 1 | session_id | 当前 RobotSession |
| 2 | host_time_ms | 单调时钟毫秒低 32 位 |
| 3 | main_switch | `VirtualRemoteInput.mode // 3` |
| 4 | sub_switch | `VirtualRemoteInput.mode % 3` |
| 5 | command_flags | deadman/motion/smooth_stop |
| 6 | yaw_permille | `round(clamp(yaw)*1000)`，CH1 |
| 7 | forward_permille | `round(clamp(forward)*1000)`，CH2 |
| 8 | speed_permille | `round(clamp(speed_axis)*1000)`，CH3 |
| 9 | arm_j0_permille | `round(clamp(arm_j0)*1000)`，CH6 |
| 10 | arm_j1_permille | `round(clamp(arm_j1)*1000)`，CH7 |
| 11 | channel_valid_mask | v1 固定 `0x0067` |
| 12 | command_counter | 当前 session 内递增计数 |

如果 deadman 或 motion enable 未同时成立，`pack_setpoint()` 必须再次把 CH1/CH2/CH6/CH7 编码为 0；如果 motion enable 为 false，还必须编码 LOW+LOW 和 CH3=-1000。该检查既在控制器层执行，也在 codec 边界执行，防止调用错误形成非零运动帧。USB payload 中不存在 CH9。

### 5.8 虚拟遥控器调用时序

```python
remote = VirtualRemoteController()
remote.on_status(render_status)
remote.on_control_revoked(show_revoke_reason)
remote.on_error(show_error)

device = await remote.connect(selected_usb_serial)
session_id = await remote.acquire_control(lease_ms=1500)

remote.set_mode(RobotMode.STAND_HOLD)
remote.set_speed_axis(-0.8)  # 等价 CH3 norm=-80，LOW 档、轮速上限20rpm
remote.set_motion_enable(True)

# 操作员持续按住 deadman 后，输入设备事件更新摇杆。
remote.set_deadman(True)
remote.set_axes(forward=0.20, yaw=0.0)

# 松开 deadman 的事件必须先于任何界面刷新处理。
remote.set_deadman(False)
remote.smooth_stop()

await remote.release_control()
await remote.disconnect()
```

上述示例只展示 API 调用。真实程序的 `set_deadman/set_axes` 来自 `InputDeviceAdapter`，不能在 UI 中用一个保持选中的复选框模拟持续按压 deadman。

### 5.9 控制器内部状态转换

```text
DISCONNECTED
  -> connect/HELLO成功 -> READY
READY
  -> acquire成功 -> ACQUIRED
ACQUIRED
  -> release -> READY
  -> 实体接管/状态超时/错误session/设备复位 -> READY或LINK_LOST
任意已连接状态
  -> 串口异常/USB拔线 -> LINK_LOST -> DISCONNECTED
```

进入 READY、LINK_LOST 或 DISCONNECTED 的动作必须统一执行：清零轴、清除 deadman、清除 motion enable、清除 smooth-stop 计数和旧 session。不得为重连保留断线前输入。

### 5.10 内部传输接口

协议层以下只保留一个串口写入口，确保 ESTOP、会话请求和周期 setpoint 不会交叉写坏帧：

```python
class SerialTransport:
    @staticmethod
    def enumerate(vid: int, pid: int) -> list[UsbDeviceCandidate]: ...

    async def open(self, port: str) -> None: ...
    async def close(self) -> None: ...
    async def write_frame(self, frame: bytes, priority: int = 10) -> None: ...
    def is_open(self) -> bool: ...
    def on_bytes(self, callback: Callable[[bytes], None]) -> Callable[[], None]: ...
    def on_disconnected(self, callback: Callable[[Exception | None], None]) -> Callable[[], None]: ...
```

写优先级固定为：ESTOP=0、RELEASE/零输入停止帧=1、HELLO/ACQUIRE/配置=5、周期 setpoint=10、ASCII `p/Y`=20。周期 setpoint 使用“最新值覆盖”槽位，不能进入无界 FIFO。`write_frame()` 只接受已经完成 CRC 的完整帧；ASCII 查询通过单独的 `write_diagnostic_byte()` 或受限诊断入口发送，不能伪装成协议帧。

读取回调只喂给一个复合流分离器：优先识别合法二进制帧；仅在等待 `p/Y` 查询响应期间收集 ASCII 文本。未发起诊断查询时收到的文本只记为异常，不参与机器人状态判断。

## 6. 设备发现和连接

当前设备描述符为：

```text
VID          0x0483
PID          0x5740
Product      STM32 Virtual ComPort
Interface    CDC Interface
```

发现流程：

1. 优先按 VID/PID 筛选端口，但不能只依赖 COM 号。
2. 存在多个匹配设备时显示 USB serial number，由操作员选择；保存 serial number，不保存 COM 号。
3. 打开串口后清空本地解析缓冲，但不要依赖串口 flush 清除下位机状态。
4. 生成随机非 0 `client_nonce`，发送 `HELLO_REQ`。
5. 1 秒内未收到匹配 nonce 的 `HELLO_RESP`，关闭端口并报告握手失败。
6. 检查协议版本包含 v1、`max_payload>=96`，并要求 capability 位 5（虚拟遥控通道帧）；需要 CH6/CH7 jog 时还要求位 6。
7. 进入“已连接、未取得控制权”状态，不自动让机器人运动。

USB CDC 波特率可填 `115200 8N1`。端口打开成功不等于协议连接成功，只有 HELLO_RESP 校验成功才显示在线。

连接后看到 CDC 长时间无任何文本输出是正式版的正常行为。上位机必须主动发送 HELLO，不得等待启动 banner 判断设备是否在线。

## 7. 协议编解码

### 7.1 编码要求

- 所有整数使用小端序。
- 每个发送方向维护独立 `uint16 seq`，每帧加 1，允许自然回绕。
- CRC 使用 `CRC-16/CCITT-FALSE`，计算范围和参数严格按协议规范。
- 发送函数接收结构化消息，统一完成 header、长度和 CRC，不允许 UI 传入裸字节。
- 不发送非零保留位，不使用规范外的消息类型。

建议接口：

```text
encode_frame(msg_type, flags, seq, payload) -> bytes
feed_bytes(data) -> list[DecodedFrame]
crc16_ccitt_false(data) -> uint16
```

### 7.2 流解析要求

串口读取没有消息边界，一次 read 可能得到半帧、一帧或多帧。解析器必须：

- 在任意噪声中寻找 `A5 5A`。
- 等待完整 10 字节头部后再读取长度。
- 拒绝 payload 大于 96 的帧。
- 等待完整 payload 和 2 字节 CRC。
- CRC 错误只滑动一个字节重新同步。
- 跳过调试文本，不能把文本解码失败当成设备断线。
- 对每类响应同时校验 msg type、request seq、nonce/session，不能只看“收到了一帧”。

上位机应记录 CRC 错误频率。偶发错误可继续运行；短时间连续错误应显示链路异常并主动把本地运动使能撤销。

### 7.3 ASCII 诊断兼容

`p` 和 `Y` 只用于人工维护或协议联调，不进入自动控制流程：

- `p`：请求一次机器人、电机和轮驱状态文本。
- `Y`：请求一次 SBUS 与模式诊断文本。
- 上位机发送后可在独立诊断窗口显示返回文本，但不能从文本反向推导控制状态。
- 不得发送其他历史调试字符，尤其不能用 `s/T/[ /]/x/!` 等字符控制运动或停止。
- 收到一次查询结果后不得期待周期文本；需要结构化状态时使用 `ROBOT_STATUS`。
- 进行 `p/Y` 查询时，二进制解析器仍持续工作；文本和二进制输出必须在接收层正确分流。

## 8. 会话与控制权

### 8.1 获取控制权

操作员点击“取得控制权”时：

1. 确认已收到 500 ms 内的新鲜 `ROBOT_STATUS`。
2. 确认状态显示实体遥控为 LOW+LOW、CH9 释放、无安全锁存、摇杆回中。
3. 发送 `ACQUIRE_REQ(client_nonce, 1500ms)`。
4. 等待最多 500 ms；未响应可用同一请求 seq 重试一次。
5. 收到成功 `ACQUIRE_RESP` 后保存非 0 `session_id`。
6. 立即启动 50 Hz 控制发送，但初始帧必须是 `MOTOR_CHECK_IDLE`、所有轴为零、CH3=-1000、`MOTION_ENABLE=0`。
7. 只有状态中的 `control_source=USB` 且 session 匹配，UI 才显示“控制权已取得”。

失败结果必须直接展示原因，例如“实体模式不是 LOW+LOW”“CH9 未释放”“安全锁存”“控制权被占用”，不能无限自动重试。

### 8.2 释放控制权

正常退出流程：

1. 立即清除本地 deadman 和 motion enable。
2. 连续发送至少 3 帧零输入、`SMOOTH_STOP=1` 的控制帧。
3. 观察状态确认应用输入归零；最多等待 500 ms。
4. 发送 `RELEASE_REQ(session_id)`。
5. 收到 ACK 或 300 ms 超时后都清除本地 session。

窗口关闭、切换端口、应用退出和操作系统关机事件都应调用同一释放流程。但不能把安全建立在 RELEASE 一定成功上；下位机 150 ms 看门狗才是最后保障。

## 9. 50 Hz 控制循环

控制调度器固定每 20 ms 生成一帧 `VIRTUAL_RC_SETPOINT`。每一帧都从最新 UI/任务输入快照构造，不在发送队列中累计旧控制帧。

推荐伪代码：

```text
deadline += 20 ms

if no valid session:
    do not send VIRTUAL_RC_SETPOINT
else:
    input = atomic_snapshot(operator_input)
    status_fresh = now - last_status_time <= 300 ms
    owns_control = (status.session_id == session_id and
                    status.control_source == USB and
                    status.link_state == ACQUIRED)
    mode_ready = (status.requested_mode == input.mode and
                  status.active_mode == input.mode and
                  status.entry_state == ACTIVE)
    safety_clear = status.safety_flags == 0 and status.fault_flags == 0
    can_move = (input.deadman and input.motion_enable and status_fresh and
                owns_control and mode_ready and safety_clear)

    if not can_move:
        forward = 0
        yaw = 0
        flags = SMOOTH_STOP when previously moving else 0
    else:
        forward = clamp(input.forward, -1000, 1000)
        yaw = clamp(input.yaw, -1000, 1000)
        flags = DEADMAN_HELD | MOTION_ENABLE

    command_counter += 1
    send latest setpoint
```

若一次调度延迟超过 20 ms，不得补发多条过期控制帧；应丢弃错过的周期，从下一个绝对 deadline 继续。串口写队列中若已经存在未发送 setpoint，应替换为最新 setpoint，而不是排队。

### 9.1 输入映射

推荐使用游戏手柄；键盘只能用于低速调试。统一内部范围为 `[-1.0, 1.0]`，再转换为 permille。

| 操作 | 协议字段 | 约定 |
| --- | --- | --- |
| 左右轴 | `yaw_permille` | 左为负，右为正 |
| 前后轴 | `forward_permille` | 后为负，前为正 |
| 按住使能键 | `DEADMAN_HELD` | 松手立即清零 |
| 独立运动开关 | `MOTION_ENABLE` | 默认关闭 |
| 速度轴 | `speed_permille` | CH3，`-1000..1000` |
| 速度档便利选择 | `set_speed_profile()` | 写入同一个 CH3 速度轴 |
| 轮速便利限制 | `set_wheel_max_rpm()` | 反算并覆盖同一个 CH3 速度轴 |

上位机输入建议增加 5% 轴死区和短时低通，但不能改变左右、前后的协议符号约定。下位机仍会执行最终死区和限幅。

键盘映射建议：`W/S` 前后，`A/D` 左右，空格按住作为 deadman，松开空格即停止。窗口失焦、键盘 hook 失效或手柄断开时必须原子清除所有轴和两个使能位。

### 9.2 模式操作

UI 提供枚举选择，不允许用户输入任意整数：

| 模式 | 使用场景 | 非零运动是否允许 |
| --- | --- | --- |
| MOTOR_CHECK_IDLE | 无动作检测/恢复准备 | 否 |
| LOW_WHEEL | 低姿态轮行 | 是 |
| LOW_SAFE_HOLD | 腿 RX-only/安全保持 | 否 |
| STAND_HOLD | 站立保持 | 否 |
| STAND_WHEEL | 站立轮行 | 是 |
| STAND_ARM | 站立机械臂 jog | 仅 CH6/CH7 |
| GAIT_ONLY | 纯腿步态 | 是 |
| GAIT_WHEEL | 轮腿混合 | 是 |
| RESERVED_STAND | 保留站立 | 否 |

模式切换后 UI 显示“正在进入”，直到状态满足 `requested_mode == active_mode` 且 `entry_state == ACTIVE`。等待期间继续发送同一请求模式和零运动输入；不得因为等待时间长而切换到另一个模式。收到 BLOCKED 时显示 `block_reason` 并保持零输入。

## 10. 状态处理

状态接收时间必须取本机单调时钟，不能用 UI 刷新时间。建议阈值：

```text
状态正常       age <= 150 ms
状态延迟       150 ms < age <= 300 ms
状态失联       age > 300 ms
```

状态超过 300 ms：

- 立即清除 deadman 和 motion enable。
- 停止生成非零控制帧。
- UI 显示链路失联，禁止再次使能。
- 串口仍打开时可以继续发送零输入帧，但 1 秒内没有恢复则关闭会话并进入重连。

UI 至少显示：

- 连接端口、USB serial、协议和固件版本。
- 控制源、session、链路年龄和 setpoint 年龄。
- 请求模式、实际模式、进入状态、阻塞原因。
- 应用后的 forward/yaw/轮速上限，而不只是 UI 请求值。
- 安全锁存、CH9、SBUS 新鲜度、轮/腿在线掩码和汇总故障。
- CRC 错误数和 RX 丢弃数。

状态颜色只能作为辅助，必须同时显示文字；急停或故障不能只用红色表达。

## 11. 急停与停止语义

上位机应区分三种动作：

| 动作 | 实现 | 用途 |
| --- | --- | --- |
| 正常停止 | 轴归零并发送 `SMOOTH_STOP` | 日常停车/收步 |
| 释放控制 | 正常停止后发送 RELEASE | 交还控制权 |
| 软件急停 | 发送 `ESTOP_REQ` | 明确的危险事件 |

软件急停按钮必须始终可用，即使没有 session。按下后立即发送 3 次相同语义的 ESTOP 帧，每帧使用新 seq，间隔约 20 ms；任一 ACK 即确认。软件急停会建立下位机锁存，不能在上位机提供“一键解除”。解除只能按实体安全流程执行。

上位机软件急停不能替代物理断电或硬件急停。UI 中不得把关闭窗口、断开串口标成“急停”；它们依赖通信看门狗，安全等级不同。

## 12. 重连策略

发生 USB 拔线、串口异常、设备复位或状态超时时：

1. 原子清除全部操作输入、deadman、motion enable 和 session。
2. 停止旧端口写入并关闭端口。
3. 使用 USB serial number 等待同一设备重新出现。
4. 采用 `0.5s, 1s, 2s, 2s...` 退避重连，避免高频打开端口。
5. 重新生成 client nonce，重新 HELLO。
6. 重连后保持“未取得控制权”，不得自动 ACQUIRE，不得恢复断线前模式、速度或摇杆值。
7. 必须由操作员重新确认并按下取得控制权。

设备 `uptime_ms` 倒退表示下位机复位，即使串口没有断开，也必须销毁 session 并重新握手。

## 13. 并发与数据一致性

- 串口读取线程只负责解析并投递消息，不能直接操作 UI 控件。
- 控制调度器读取一个不可变的操作输入快照。
- session 的建立/销毁和输入清零必须是同一原子状态变更。
- 所有 pending request 以 `(msg_type, seq)` 索引，并设置超时。
- ROBOT_STATUS 是最新值覆盖模型，不排队回放旧状态。
- 日志写盘放在低优先级线程，磁盘卡顿不能阻塞控制发送。

## 14. 推荐日志

每条事件至少记录本机单调时间、墙上时间、USB serial、session 和原因：

- 端口发现、打开、关闭和异常。
- HELLO/ACQUIRE/RELEASE 结果。
- 控制源或模式变化。
- 状态超过 150 ms 和 300 ms。
- deadman 按下/释放、手柄断开、窗口失焦。
- 软件急停发送与 ACK。
- safety/fault/block 位的上升沿和下降沿。
- 下位机复位、CRC 错误突增和 RX drop 增长。

不要以 50 Hz 记录每个控制帧到普通文本日志。需要波形分析时使用有界二进制记录，并设置最大文件大小和轮转。

## 15. 上位机测试

### 15.1 Codec 测试

- 使用固定输入验证 CRC 和完整帧字节。
- 随机分块喂入解析器，结果必须与一次性喂入一致。
- 在帧前后插入任意文本和噪声。
- CRC 错帧、超长帧后能找到下一合法帧。
- 验证所有 payload 长度、符号和小端序。

### 15.2 Session 测试

- HELLO nonce 不匹配时忽略响应。
- ACQUIRE 超时只重试一次，不无限抢占。
- 错误 session 状态不能显示已控制。
- uptime 倒退立即销毁 session。
- RELEASE 丢包后本地仍进入未控制状态。

### 15.3 控制调度测试

- 正常发送频率为 `50 Hz +/- 2 Hz`。
- UI 卡顿时控制线程仍运行；控制线程卡顿时下位机看门狗能够触发。
- 松开 deadman 后下一帧必须为零输入。
- 手柄拔出、窗口失焦、应用退出均清零。
- 写端阻塞时不累计旧 setpoint。

### 15.4 联调验收顺序

1. 下位机电机断电，验证 HELLO、ACQUIRE 拒绝原因、状态和 CRC。
2. 打开 CDC 静置 60 秒，确认没有启动帮助或周期文本；验证 `p/Y` 为一次性查询且其他历史命令无动作。
3. 使用协议测试器发送零输入 10 分钟，确认无 RX drop、无错误接管。
4. 机器人架空，只开放 LOW 档和小轮速。
5. 验证松开 deadman、拔 USB、杀进程、PC 休眠和手柄断开。
6. 验证推动实体摇杆、切换 CH5/CH8 和 CH9 均能抢占 USB。
7. 验证站立、纯腿、轮腿模式进入与 BLOCKED 提示。
8. 地面由低速逐级开放，保留独立物理断电人员。

发布前必须通过的核心条件是：上位机任何单线程崩溃、进程退出、USB 拔线或操作输入设备断开，都不会让机器人继续保持最后一次非零运动输入。
