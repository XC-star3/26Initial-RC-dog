# RC-dog

8DOF 轮足四足机器人 STM32 固件与比赛功能开发仓库。

## 开发路线

当前主线工作为第一阶段任务赛通讯和第二阶段无 IMU 障碍动作。IMU 姿态增强与高墙均为不阻塞主线验收的可选专项；后续工作不得破坏已通过的障碍动作、已验收的控制方式和安全逻辑。

| 阶段 | 目标 | 主要控制入口 | 完成判据 |
| --- | --- | --- | --- |
| 第一阶段：任务赛通讯 | 完成上位机到 STM32 的 `0x10` USB 控制，使 ROS2 能控制模式、前后、转向和速度 | 上位机 USB 为任务输入，实体 SBUS/CH9 保留最高安全优先级 | 50 Hz 稳定控制；USB 失联 150 ms 内停止；可完成任务赛底盘运动闭环 |
| 当前越障基线 | 已通过直角绕杆、限高杆、大斜坡和木桥 A；保持其动作与参数可回归 | 实体 SBUS 为主 | 每次新增障碍动作后均可重复通过这 4 项 |
| 第二阶段：无 IMU 障碍动作 | 完成砂砾碎木坑、木桥 B 和 T 字形台阶逐级上台；当前已有木桥 B 软缝和三级上台软件状态机 | 实体 SBUS `HIGH+HIGH`；关节编码器、电机反馈和轮驱保护 | 完成 150 mm 护栏、木桥 B 横向踏板缝和 100 mm 三级上台阶的专项动作，失败能安全停止或撤回 |
| 可选专项：IMU 姿态增强 | 视实机风险再接入并验证 `0x11 VIRTUAL_IMU`，先做姿态超限停止，再评估补偿 | USB `0x11`、实体 SBUS/CH9 保持最高优先级 | 不阻塞第二阶段；启用时需完成新鲜度、方向和失效处理验证 |
| 可选专项：高墙 | 视时间和安全条件专项开发 300 mm 高、50 mm 厚高墙的攀爬或动态跨越 | 独立高墙动作与保护状态机 | 不阻塞其他障碍；仅在保护场地中重复验证后尝试 |

第二阶段不要求 `0x11 VIRTUAL_IMU`。当前障碍动作只开放给实体 SBUS：实体 `HIGH+HIGH` 进入障碍模式；USB `0x10` 的 mode 8 仍保持 `RESERVED` 站立 HOLD，不能启动越障。`0x11` 姿态增强和高墙均仅在主线稳定后按需立项。

## 文档导航

| 文档 | 唯一职责 |
| --- | --- |
| [COMMUNICATION_README.md](COMMUNICATION_README.md) | 上位机与下位机的 USB 线协议真源 |
| [任务赛通信实现.md](任务赛通信实现.md) | 第一阶段上下位机改造、ROS2 任务架构和联调验收 |
| [8DOF轮足四足越障分析.md](8DOF轮足四足越障分析.md) | 第二、三阶段障碍运动学、动作和验收条件 |
| [使用文档.md](使用文档.md) | 实体遥控器、电机、串口命令、烧录和底层调试手册 |

协议字段只在 `COMMUNICATION_README.md` 定义。其他文档可以引用关键字段解释实现，但出现差异时一律以通信协议为准，不得维护另一套扩展消息。

## 当前代码状态

- 已完成实体 SBUS 接收、CH5/CH8 九宫格模式、CH9 锁存安全、腿部 MIT 控制、四轮控制和轮腿混合步态。
- 已实现固定 40 字节 USB 解析、`0x10` 控制、USB/SBUS/NONE 仲裁和 150 ms 看门狗；正式 ASCII 接口仅保留只读 `p/Y`。
- 已实现 Python 上位机 codec、50 Hz 串口控制流、命令行联调和协议单元测试。
- 第一阶段软件已通过主机协议测试和 STM32 Debug/Release 构建，仍需在实体遥控器授权和机器人架空条件下完成硬件联调。
- 当前实机越障基线已通过直角绕杆、限高杆、大斜坡和木桥 A；其余常规障碍为第二阶段主线目标，高墙为可选专项。
- 已加入实体 `HIGH+HIGH` 障碍入口：CH3 LOW/MID/HIGH 分别选择木桥 B、T 字形台阶、砂砾碎木坑；进入后 CH2 必须先回中，正/反方向越过阈值的边沿分别请求前进一步/回退一步。
- 木桥 B 软件轨迹按轴距 400 mm、踏板长度 400 mm 建立 CP0..CP4 五个检查点并支持逐步回退；当前仍只开放 50 mm 软缝。T 字形台阶已按规则图确认的阶高 100 mm、踏面 300 mm、单侧进深 900 mm 和通道宽 400 mm 启用三级上台状态机：一次 CH2 前进边沿完成一级，第三级自动进入顶平台并恢复普通站姿；故障只回退当前级。两项功能均待实体保护测试，且台阶启动前必须确认整机横向运动包络小于 400 mm。台阶不开发下台或顶部动作交接，因此不代表完整 T 台阶通过。砂砾碎木坑仍返回 `UNAVAILABLE`。
- 木桥 B 和 T 台阶进入前均要求四轮在线、轮速绝对值小于 0.5 rad/s 并连续保持 200 ms；台阶分段还执行 16 区间全路径预检、逐级 300 ms 等待和关节稳定确认。非 CH9 运行故障可返回阶段或当前级起点，只有 CH9 会显式立即取消足端轨迹并关闭越障请求。
- 木桥 B 正式目标缝宽为 150 mm，固件当前活动缝和编译期测试上限均为 50 mm。必须按 50/100/150 mm 顺序重新计算几何、显式提升测试上限并完成实体测试；现阶段不得表述为已通过木桥 B。
- `0x11` 当前只校验并丢弃；IMU 解码、姿态超限停止和姿态补偿为可选专项。
- 116 mm 驱动轮已用于当前轮速和轮腿同步计算。

实际实现状态以代码和测试结果为准；规划文档中的接口不得被描述为已经完成。
