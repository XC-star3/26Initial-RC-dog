# 8DOF 四足遥控器映射指南

本文档面向当前 8DOF 四足机器人，也就是每条腿 2 个自由度的结构。当前机械结构适合前进、后退、原地转向、弧线转弯，不适合真正横移。

## ET07 / RF207S 确认

- ET07 是 10 通道遥控器。
- RF207S 接收机的 W.BUS 输出兼容 S.BUS。
- ET07 的 `通用功能 -> 辅助通道` 可以自定义 CH5-CH10。
- 物理控件类型：
  - SA：两挡长柄开关。
  - SB：三挡短柄开关。
  - SC：三挡短柄开关。
  - SD：两挡弹簧回中瞬时开关。
  - V1：旋钮。
  - V2：波轮。
- 说明书示例中 CH8 默认可能是关闭状态，当前工程需要手动设置 `CH8 -> SC`。

## 8DOF 运动能力边界

当前每条腿主要在 `x/z` 平面运动：

- `x`：足端前后方向
- `z`：足端竖直方向，采用 `+Z` 向下的坐标系；Z 增大表示足端向下，抬腿时 Z 减小

因此推荐控制目标是：

- 前进 / 后退
- 原地左转 / 右转
- 弧线转弯
- 站立高度调整
- 俯仰、横滚姿态微调

不推荐把遥控器通道映射为真实横移，因为 8DOF 没有每条腿的髋外展/内收自由度，无法主动控制足端 `y` 方向位置。强行横移通常只能依赖脚底打滑，稳定性和可控性都不好。

## 推荐通道映射

以 ET07 + RF207S + SBUS 为例，推荐分配如下：

| SBUS 通道 | ET07 控件 | 8DOF 推荐用途 | 说明 |
| --- | --- | --- | --- |
| CH1 | 右摇杆横向 | 转向角速度 | 左右转向 |
| CH2 | 右摇杆纵向 | 前进 / 后退速度 | 核心运动输入 |
| CH3 | 左摇杆纵向 / 油门 | 速度档 | LOW/MID/HIGH 三档步态速度 |
| CH4 | 左摇杆横向 | Roll 姿态微调或保留 | 不建议做横移 |
| CH5 | SB 三挡 | 主模式 | LOW 停止，MID 站立，HIGH 步态 |
| CH6 | V1 旋钮 | 机械臂 J0 | DM4310 |
| CH7 | V2 波轮 | 机械臂 J1 | EL05 |
| CH8 | SC 三挡 | 子模式 | HIGH 可进入机械臂模式 |
| CH9 | SD 两挡弹簧回中开关 | 锁存式急停输入 | 高于释放阈值立即锁存 ESTOP |
| CH10 | SA 或关闭 | 扩展 | 保留 |

推荐在 ET07 中进入 `通用功能 -> 辅助通道`，确认：

| 通道 | 推荐设置 |
| --- | --- |
| CH5 | SB |
| CH6 | V1 |
| CH7 | V2 |
| CH8 | SC |
| CH9 | SD |

如果 CH8 没有分配给 SC，机械臂子模式无法进入。

SD 是自动回中的两挡瞬时开关，不适合做主模式、速度档或机械臂使能这类保持型功能。当前安全判断不是边沿触发：CH9 只要高于释放阈值（`norm > 20`）就立即锁存 ESTOP，并在未释放时持续禁止恢复。拨到 HIGH 侧（`norm >= 50`）一定会触发，但不会同时执行清故障。

| SD 动作 | 建议功能 |
| --- | --- |
| CH9 高于释放阈值 | 立即锁存 ESTOP，停止四足和机械臂输出 |
| 松手且 CH9 回到 `norm <= 20` | 解除 CH9 inhibit；仍需满足完整恢复条件，不能自动恢复运动 |

## 四足模式建议

CH5 作为主模式开关：

| CH5 档位 | 建议含义 | 当前工程行为 |
| --- | --- | --- |
| LOW | 恢复 / 待机 / RX-only | 停止四足控制输出，关闭机械臂；也是安全锁存恢复的必要档位 |
| MID | 站立 / 姿态模式 | 自动选择全部 8 个腿部电机并执行 MIT foot IK 站立 |
| HIGH | 行走 / 步态模式 | 根据 CH1/CH2 启动离散步态，摇杆回中停止并保持站立 |

安全锁存只有在以下条件同时满足后才允许清除：SBUS 信号新鲜、CH9 已释放（`norm <= 20`）、CH5 LOW 稳定至少约 `150 ms`，并且全部 8 个腿部电机都已确认处于 idle 且无故障。恢复后仍保持 CH5 LOW 待机，需要重新切换模式才会运动。

SBUS 丢失或进入 failsafe 会锁存 ESTOP；上电后超过接收超时时间仍未收到首帧也按信号丢失处理。CH5 LOW 只是软件恢复/待机档，不是绝对安全保证；物理断开电机动力电源才是绝对安全措施。维护、接线或进入机械危险区前必须断开动力电源，硬件急停也应采用能切断动力的独立回路。

腿部编码器查询按每条 CAN 总线每 `2 ms` 轮询一个电机，单电机反馈频率约为 `125 Hz`。编码器反馈超过 `40 ms` 未更新即视为不新鲜，受控运动会被安全停止。

在 CH5 HIGH 行走模式下，当前这样使用 CH1/CH2：

| 通道 | 摇杆动作 | 建议行为 |
| --- | --- | --- |
| CH2 > 死区 | 右摇杆向前 | 前进小跑 |
| CH2 < -死区 | 右摇杆向后 | 后退小跑 |
| CH1 > 死区 且 CH2 回中 | 右摇杆向右 | 原地右转 |
| CH1 < -死区 且 CH2 回中 | 右摇杆向左 | 原地左转 |
| CH2 和 CH1 同时有输入 | 前后优先 | 先执行 CH2 前进/后退，不做弧线转弯 |
| CH1/CH2 都回中 | 摇杆回中 | 停止步态并保持 MIT 站立 |

当前摇杆步态使用迟滞死区：超过 `25` 才进入前进/后退/转向命令，对应代码中的 `SBUS_MOVE_ENTER_DEADBAND`；回到 `12` 以内才退出当前步态命令，对应 `SBUS_MOVE_EXIT_DEADBAND`。SBUS 归一化中心死区为 `5`，机械臂 CH6/CH7 jog 另加 `8` 的中心死区。这样可以避免摇杆、旋钮或波轮在阈值附近抖动时反复启动/停止或缓慢漂移。

CH1/CH2 触发的 cycloid 离散步态会在名义摆腿时间结束后等待摆动腿关节误差收敛，默认阈值为 `4 deg`，最多额外等待 `120 ms`。因此实际步态频率可能因关节跟踪误差略低于 CH3 标称档位频率。

## 速度映射

当前 CH3 已接入三档速度，影响小跑频率、前进/后退步幅和原地转向步幅：

| CH3 档位 | 小跑频率 | 前进/后退步幅 | 原地转向步幅 |
| --- | --- | --- | --- |
| LOW | `2.0 Hz` | `70 mm` | `30 mm` |
| MID | `2.0 Hz` | `70 mm` | `30 mm` |
| HIGH | `3.0 Hz` | `70 mm` | `30 mm` |

其他固定参数：

- `DOG_REAR_FOOT_EXTRA_Z_MM = 15.0f`
- `DOG_TROT_YAW_TRIM_X_MM = 0.0f`

CH3 是摇杆输入，当前按归一化值分段：`norm <= -60` 为 LOW，`norm >= 60` 为 HIGH，中间为 MID。LOW/MID 都保持稳态 `2.0 Hz`，HIGH 为快速 `3.0 Hz`；步长固定 `70 mm`，步高固定 `40 mm`，CH3 不参与站立高度或步态高度控制。切档时如果正在行走，下一步态周期会使用新档位参数。

串口 `Y`/`y` 可打印 SBUS 诊断信息，包括 CH1/CH2/CH3/CH5/CH8/CH9 的 raw/norm、online/failsafe、当前安全按钮状态和解码出的步态命令。遥控器无响应时，优先用这个命令确认是否收到 SBUS 帧以及 CH5 是否进入 MID/HIGH。

当前第一版逻辑：

- CH2 小于死区：不前进/后退
- CH1 小于死区：不转向
- CH2 有输入：优先启动前进/后退步态
- CH1 有输入且 CH2 小：启动原地转向
- CH2 和 CH1 都有输入：CH2 前进/后退优先，后续再扩展为弧线转弯

## 当前工程状态

当前工程已经完成：

- SBUS 接收
- CH5 主模式：LOW 停止、MID MIT 站立、HIGH 离散步态
- CH8 子模式：CH5 MID + CH8 HIGH 进入机械臂 jog 模式
- CH6/CH7 机械臂微动
- CH1/CH2 到原地转向、前进/后退小跑的离散映射
- CH3 到低/中/高三档步态速度的映射
- CH9/SD 锁存式安全输入：高于释放阈值立即触发 ESTOP，满足完整恢复条件后才能清锁
- SBUS 丢失、failsafe 或上电后超时仍无首帧时锁存 ESTOP

但当前还没有完成：

- CH4 到姿态微调的映射
- CH1 + CH2 弧线转弯
- 连续步态调速接口

串口调试命令仍可用于离线调试和手动验证：

| 串口命令 | 行为 |
| --- | --- |
| `8` | 选择全部 8 个腿部电机 |
| `s` | 站立 |
| `T` | 小跑/前进步态测试 |
| `[` | 原地左转 |
| `]` | 原地右转 |
| `x` | 停止 |

## 推荐落地顺序

1. 上电和恢复时保持 CH5 LOW；它是软件待机档，不代替物理断电或硬件急停。
2. CH5 MID 只做站立和姿态微调。
3. CH5 HIGH 才允许步态遥控。
4. 先接 CH2 前进/后退和 CH1 原地转向。
5. CH3 速度档只调步态速度，不参与高度控制。
6. 最后再考虑 CH2 + CH1 的弧线转弯。

## 建议的最终手感

推荐类似遥控车/坦克的操作方式：

- 右摇杆前后：走快/走慢/后退
- 右摇杆左右：左转/右转
- 左摇杆上下：速度档
- 左摇杆左右：姿态微调，不做横移

这样最符合 8DOF 机械能力，也最容易调稳定。
