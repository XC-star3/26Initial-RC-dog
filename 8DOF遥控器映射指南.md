# 8DOF 四足遥控器映射指南

本文档面向当前 8DOF 四足机器人，也就是每条腿 2 个自由度的结构。当前机械结构适合前进、后退、原地转向、弧线转弯，不适合真正横移。

## ET07 / RF207S 确认

- ET07 是 10 通道遥控器。
- RF207S 接收机的 W.BUS 输出兼容 S.BUS。
- ET07 的 `通用功能 -> 辅助通道` 可以自定义 CH5-CH10。
- 物理控件类型：
  - SA：两挡长柄开关。
  - SB：三挡短柄开关。
  - SC：三挡短柄开关。
  - SD：两挡弹簧回中瞬时开关。
  - V1：旋钮。
  - V2：波轮。
- 说明书示例中 CH8 默认可能是关闭状态，当前工程需要手动设置 `CH8 -> SC`。

## 8DOF 运动能力边界

当前每条腿主要在 `x/z` 平面运动：

- `x`：足端前后方向
- `z`：足端竖直方向，采用 `+Z` 向下的坐标系；Z 增大表示足端向下，抬腿时 Z 减小

因此推荐控制目标是：

- 前进 / 后退
- 原地左转 / 右转
- 弧线转弯
- 站立高度调整
- 俯仰、横滚姿态微调

不推荐把遥控器通道映射为真实横移，因为 8DOF 没有每条腿的髋外展/内收自由度，无法主动控制足端 `y` 方向位置。强行横移通常只能依赖脚底打滑，稳定性和可控性都不好。

## 推荐通道映射

以 ET07 + RF207S + SBUS 为例，推荐分配如下：

| SBUS 通道 | ET07 控件 | 8DOF 推荐用途 | 说明 |
| --- | --- | --- | --- |
| CH1 | 右摇杆横向 | 转向 | L+M、M+M 直接差速转向；H+L 只控制点足偏航，H+M 同时控制步态和轮驱偏航 |
| CH2 | 右摇杆纵向 | 前进 / 后退速度 | L+M、M+M 直接轮行；H+L 控制纯点足前进，H+M 经步态平滑后同步驱动腿和轮 |
| CH3 | 左摇杆纵向 / 油门 | 轮速上限 / 步态速度档 | 轮驱时线性设置 `0~200 rpm`；步态时选择 LOW/MID/HIGH |
| CH4 | 左摇杆横向 | Roll 姿态微调或保留 | 不建议做横移 |
| CH5 | SB 三挡 | 九宫格行选择 | 与 CH8/SC 共同确定完整模式，不单独决定动作 |
| CH6 | V1 旋钮 | 机械臂 J0 | DM4310 |
| CH7 | V2 波轮 | 机械臂 J1 | EL05 |
| CH8 | SC 三挡 | 九宫格列选择 | 与 CH5/SB 共同确定完整模式，详见下方九宫格 |
| CH9 | SD 两挡弹簧回中开关 | 唯一锁存式急停输入 | `norm >= 50` 立即锁存 ESTOP |
| CH10 | SA 或关闭 | 扩展 | 保留 |

推荐在 ET07 中进入 `通用功能 -> 辅助通道`，确认：

| 通道 | 推荐设置 |
| --- | --- |
| CH5 | SB |
| CH6 | V1 |
| CH7 | V2 |
| CH8 | SC |
| CH9 | SD |

如果 CH8 没有分配给 SC，机械臂子模式无法进入。

## 机械臂 CAN 接线

| 关节 | 电机 | CAN 总线 | CAN 标识 |
| --- | --- | --- | --- |
| J0 | DM4310 | FDCAN1，与前腿电机共线 | 标准帧控制 `0x01`，反馈 `0x10` |
| J1 | EL05 | FDCAN2，与后腿电机共线 | 扩展帧电机 ID `0x7F`，主机 ID `0xFD` |

两条总线均为 `1 Mbps` Classic CAN。J1 必须接入 FDCAN2 的 CANH/CANL，并与后腿电机共用总线；CAN 总线仅在物理两端各保留一个 `120 Ohm` 终端电阻，控制板与电机电源必须共地。

四个 M3508+C620 独占 FDCAN3（PD12 RX、PD13 TX），同样使用 `1 Mbps` Classic CAN。俯视顺时针 ID 为 LF=`1`、RF=`2`、RB=`3`、LB=`4`，反馈帧为 `0x201..0x204`，同步控制帧为 `0x200`。

进入 `CH5=MID + CH8=HIGH` 后，固件先向 J0 发送安全使能、向 J1 设置 MIT 模式并请求主动反馈；随后等待反馈。J0/J1 任一关节反馈新鲜即可进入机械臂 jog，只有在线关节响应其对应的 CH6/CH7 输入；两者均离线时仅停止机械臂输出，不建立全机锁定，反馈恢复后自动重试。

SD 是自动回中的两挡瞬时开关，不适合做主模式、速度档或机械臂使能这类保持型功能。CH9 是唯一能建立急停锁存的输入：`norm >= 50` 触发 ESTOP，`norm <= 20` 表示释放，`21..49` 为迟滞区。触发急停不会同时执行清故障。

| SD 动作 | 建议功能 |
| --- | --- |
| CH9 `norm >= 50` | 立即锁存 ESTOP，停止四足和机械臂输出 |
| 松手且 CH9 回到 `norm <= 20` | 解除 CH9 inhibit；仍需满足完整恢复条件，不能自动恢复运动 |

## 四足模式建议

CH5（SB）与 CH8（SC）共同组成九宫格模式：

| SB+SC | 当前功能 | 轮电机状态 |
| --- | --- | --- |
| LOW+LOW | 无动作电机检测、恢复及机械限位许可准备 | OFF |
| LOW+MID | 机械限位最低姿态轮行 | DRIVE |
| LOW+HIGH | 腿 RX-only 安全姿态 | HOLD |
| MID+LOW | MIT 站立 | HOLD |
| MID+MID | MIT 站立轮行 | DRIVE |
| MID+HIGH | 确认站立后的机械臂 jog | HOLD |
| HIGH+LOW | 纯点足步态，轮电机主动锁止 | HOLD |
| HIGH+MID | 点足步态与轮驱同步运动 | DRIVE |
| HIGH+HIGH | 保留模式，保持站立 | HOLD |

除 LOW+LOW 外轮电机都保持闭环可用；HOLD 表示零轮速主动制动，不是轮轴绝对位置环。

从任一 SB=MID 模式切到 SB=LOW 时，腿部不会立即撤掉力矩。固件保持 8 腿 MIT 闭环，使用站立足端逆解将前腿 Z 从 `300 mm` 五次平滑缩短到 `150 mm`、后腿从 `315 mm` 缩短到 `165 mm`，下降时间 `1500 ms`；目标附近稳定 `200 ms` 后才进入对应 LOW 模式的 RX-only/机械等待状态。下降期间 L+L 轮驱 OFF，L+M/L+H 轮驱 HOLD。若 IK、反馈或稳定检查失败，腿部保持控制并将模式阻塞为 `LOWER`，不会回退为直接零力矩。

安全锁存只有在以下条件同时满足后才允许清除：SBUS 信号新鲜、CH9 已释放（`norm <= 20`）、CH5 LOW+CH8 LOW，并且全部 8 个腿部电机都已确认处于 idle 且无故障；四轮还必须反馈在线且绝对速度低于 `0.5 rad/s` 持续 `200 ms`。恢复后仍保持 LOW+LOW 待机，需要重新切换模式才会运动。

机械限位低姿态轮行必须先在 CH5 LOW+CH8 LOW 下将 CH1/CH2 回中到 `abs(norm) <= 12`，并让四轮在线且速度低于 `0.5 rad/s` 连续 `200 ms`。随后切到 CH8 MID 会一次性消耗该许可，向 8 个腿电机请求 IDLE；只有全部电机在本次请求后反馈 IDLE、在线且无故障，并继续静置 `500 ms`，CH1/CH2/CH3 才能差速控制四轮。该模式与其他轮姿态统一使用最高 `200 rpm`、`30/50/60 rad/s^2` 加速/减速/回中制动斜率，以及 `6 A` 持续和 `10 A` 短时电流上限；差异只保留在机械限位进入许可和腿电机 IDLE 安全门控。

上电时已经位于 LOW+MID 不会建立许可。离开 LOW+MID、任一腿反馈超时/故障/非 IDLE、CH9、SBUS 丢失或 CAN 故障都会立即令四轮归零；再次进入前必须回到 LOW+LOW 重新完成预备。

HIGH+LOW 为纯点足：CH1/CH2 经过步态迟滞和半步平滑后只控制腿部前进、后退和偏航，腿贡献固定为 `100%`，四个轮电机始终保持 HOLD 零速主动锁止。HIGH+MID 为相位同步轮腿模式，最大按腿 `70%`、轮 `30%` 分配推进；CH3 按 `0~200 rpm` 设置轮速硬上限并同时选择步态档位，轮速上限不足时自动降低实际轮贡献，腿贡献补为 `1 - 轮贡献`。支撑腿对应的轮执行步态兼容轮速，摆动腿对应的轮在摆动前 `60%` 保持零速，`60%..90%` 使用五次平滑曲线匹配触地轮速。腿到轮映射为 `LF->W1、RF->W2、LB->W4、RB->W3`。

H+L 与 H+M 互切时不打断当前轨迹，轮腿比例在下一半步快照生效。H+M 任一轮故障时轮目标立即归零，下一半步腿贡献自动补到 `100%`，点足继续运行；轮恢复后先完成纯点足半步，再在后续半步通过现有轮速斜坡恢复混合推进，不执行故障前缓存的轮速命令。步态尚未起步和 `ENTRY_SETTLE` 阶段轮速为零，摇杆回中收步时四轮随回中进度平滑降零。

轮驱模式下，CH1/CH2 在 `abs(norm) <= 12` 时视为零，死区外线性映射到 `[-1,1]`。转向采用增强混控：小幅输入保持曲率转向；CH1 绝对值从 `0.45` 增加到 `0.85` 时，逐步恢复完整转向量并把前进分量最多降到 `50%`，允许大转向时内轮反转。前进和倒车保持相同车体偏航方向，混控结果等比归一化，任一轮不超过 CH3 上限。正常档加速和减速斜率为 `30/50 rad/s^2`；换向必须先平滑穿过零点。CH1/CH2 都回中或 CH3 为 `0 rpm` 时使用 `60 rad/s^2` 主动制动，从 `200 rpm` 到零的目标时间约 `0.35 s`，接近停止后清除积分以避免反向回弹。

所有轮模式的每台 M3508 按 C620 `16384 raw = 20 A` 换算，持续电流限制为 `6 A`（约 `4915 raw`），速度误差较大时允许最高 `10 A`（`8192 raw`）持续约 `500 ms`；峰值需求消失后约 `1 s` 恢复预算。电机温度超过 `70 C` 后逐步压缩峰值区间，达到 `85 C` 时四轮归零锁止，冷却到 `65 C` 以下并满足原安全恢复条件后才能清锁。

任一轮反馈持续超过 `250 ms` 未更新或 FDCAN3 bus-off 时，仅故障轮驱停止输出。bus-off 后先停止发送并重启 FDCAN3，只有协议状态再次确认健康才发送一次零电流；发送失败按 `10 ms` 退避，避免每 `2 ms` 重试淹没发送队列。恢复后必须重新收到四个轮电机的反馈并确认四轮停止，随后自动清除轮驱本地锁止，不要求 CH1/CH2 回中，也不会执行故障前缓存的旧速度命令。轮驱命令看门狗仍为 `100 ms`；腿部执行阻塞式站立准备时会先将已启用轮驱切到 HOLD 并持续刷新，避免准备过程误触发看门狗。

SBUS 单帧 signal-lost/failsafe 先进入瞬时抑制：轮子 HOLD、机械臂停止、步态请求收步，但不改变腿部闭环和活动模式。异常持续 `100 ms` 才确认丢失并保持健康腿、IDLE 故障轴；连续收到 3 个正常帧后自动恢复，不要求 CH1/CH2 回中或回到 L+L。长期没有新帧按 `250 ms` 帧超时再加 `100 ms` 确认处理。CH9 是唯一建立全机硬急停锁存的输入。物理断开电机动力电源仍是绝对安全措施，维护、接线或进入机械危险区前必须断开动力电源。

腿部编码器查询按每条 CAN 总线每 `2 ms` 轮询一个电机，单电机反馈频率约为 `125 Hz`。编码器反馈超过 `120 ms` 未更新才视为不新鲜。除 CH9 外，真实电机故障、非有限控制量、SBUS 丢失、CAN bus-off、过温和启动失败只做子系统局部收敛，不建立全机保护锁定：健康腿保持当前位置，无法可靠闭环的轴请求 IDLE，轮驱 HOLD/零输出；条件恢复后自动重新进入当前九宫格模式。腿故障存在时每 `1 s` 请求一次清错，故障掩码清零后再重试站立。

在 H+L 纯点足和 H+M 点足轮驱模式下，CH1/CH2 的腿部控制一致：

| 通道 | 摇杆动作 | 建议行为 |
| --- | --- | --- |
| CH2 > 死区 | 右摇杆向前 | 前进小跑 |
| CH2 < -死区 | 右摇杆向后 | 后退小跑 |
| CH1 > 死区 且 CH2 回中 | 右摇杆向右 | 原地右转 |
| CH1 < -死区 且 CH2 回中 | 右摇杆向左 | 原地左转 |
| CH2 和 CH1 同时有输入 | 纵向 + 偏航融合 | 按摇杆比例执行前进/后退弧线转弯 |
| CH1/CH2 都回中 | 从 SB=MID 的模式切入 HIGH 行 | 按 CH3 当前速度档持续原地踱步；H+L 锁轮，H+M 轮速为零 |
| CH1/CH2 都回中 | 行走步态中松开摇杆 | 请求平稳收步，完整周期回到中立站姿后保持 MIT 站立 |

当前摇杆步态使用迟滞死区：CH1/CH2 任一轴超过 `25` 才启动，运行中两轴都回到 `12` 以内才停止。超过退出死区后的输入按 `(abs(norm)-12)/(100-12)` 映射到 `[-1,1]`；CH2 比例缩放前后步幅，CH1 比例缩放偏航步幅，CH3 单独选择频率档位。新输入只在下一半步快照，每个半步最多变化 `0.25`。

摆动足 X 方向采用五次平滑轨迹，Z 方向分成上升和下降两段，在约 `45%` 摆动进度到达最高点，并用剩余 `55%` 缓速落地。每条摆动腿在进度超过 `80%` 后独立检测电机 `Iq`：相对基线增加 `1.5 A` 并连续确认 `20 ms` 判为接触，下降到 `0.8 A` 以下取消候选。提前接触只冻结对应腿，另一条对角腿继续完成落地；未检测到接触时以 `40 mm/s` 最多下探 `8 mm`。搜索末端关节在目标附近稳定 `40 ms` 可作为软接触回退，否则记录无接触掩码并平稳收步，不直接切换下一对角步。

MID/HIGH 启动要求全部 8 个关节误差不超过 `5 deg`、低通后的绝对速度不超过 `10 deg/s` 并连续稳定 `150 ms`。LOW 收紧为 `4 deg / 8 deg/s / 200 ms`，最长等待 `2000 ms`。LOW 落足后还要求连续稳定 `80 ms`，且下一支撑对角腿命令电流低于 `16 A`；`500 ms` 内不能满足则平稳收步停止。

## 速度映射

轮驱模式下，CH3 按 `max_rpm = clamp(norm + 100, 0, 200)` 设置轮轴速度上限：最低位置为 `0 rpm`，中位为 `100 rpm`，最高位置为 `200 rpm`。200 rpm 对应约 `20.94 rad/s`，直径 116 mm 时理论线速度约 `1.21 m/s`。

步态模式继续使用 CH3 三档速度，影响原地踱步节奏，以及小跑频率、前进/后退步幅、步高和四脚支撑停顿：

| CH3 档位 | 小跑频率 | 前进/后退步幅 | 步高 | 四脚支撑停顿 | 对角错相 | 原地转向步幅 |
| --- | --- | --- | --- | --- | --- | --- |
| LOW | `1.0 Hz` | `25 mm` | `30 mm` | `160 ms` | `40 ms` | `30 mm` |
| MID | `2.0 Hz` | `55 mm` | `60 mm` | `60 ms` | `0 ms` | `30 mm` |
| HIGH | `2.5 Hz` | `70 mm` | `100 mm` | `30 ms` | `0 ms` | `30 mm` |

其他固定参数：

- `DOG_REAR_FOOT_EXTRA_Z_MM = 15.0f`
- `DOG_TROT_YAW_TRIM_X_MM = 0.0f`

CH3 是摇杆输入，当前按归一化值分段：`norm <= -60` 为 LOW，`norm >= 60` 为 HIGH，中间为 MID。从 SB=MID 的模式切入 HIGH 行时，原地踱步会快照当前速度档，LOW/MID/HIGH 分别使用 `250/125/100 ms` 抬腿停留和 `150/75/50 ms` 换腿间隔；小跑切档仍在下一半步快照新参数。H+M 同时用 CH3 连续值设置轮速上限，H+L 中轮子保持锁止；CH3 不参与最终站立高度控制。

摇杆回中或从 H+L/H+M 切到需要站立的九宫格时使用平稳收步。普通曲率变化在下一半步平滑生效；明显的前进/后退反打会先收步回到中立姿态，再重新通过起步门控。SD/CH9 立即建立硬急停；其他异常只停止对应运动并在条件恢复后自动重试。

串口 `Y`/`y` 可打印 SBUS 诊断信息，包括 CH1/CH2/CH3/CH5/CH8/CH9 的 raw/norm、online/failsafe、九宫格请求/活动模式、进入阶段、阻塞原因、映射后的 stick drive、请求值和半步已应用值；还会输出步态相位、半步版本、摆动/接触/搜索/失败掩码、四腿进度、实际轮腿比例、兼容轮速和各腿搜索量。轮驱明细同时显示四轮请求目标、相位缩放、最终目标和斜坡目标。

当前第一版逻辑：

- CH2 小于死区：不前进/后退
- CH1 小于死区：不转向
- CH2 有输入：按比例控制前进/后退步幅
- CH1 有输入：按比例叠加机身偏航，后退时偏航语义不反转
- CH2 和 CH1 同时有输入：连续混合为不同半径的弧线步态

## 当前工程状态

当前工程已经完成：

- SBUS 接收
- CH5/SB 与 CH8/SC 的九宫格状态机，九个组合均有独立进入、退出和活动状态
- L+M、M+M 直接轮行，H+L 使用 `100%` 纯点足并锁轮，H+M 使用最大 `70%` 腿加 `30%` 相位同步轮驱，其余非 L+L 模式锁轮
- CH6/CH7 机械臂微动
- CH1/CH2 到原地转向、直行和弧线小跑的连续映射
- CH1/CH2 轮驱差速混控，CH3 线性设置 `0~200 rpm` 轮速上限
- CH3 到低/中/高三档步态速度的映射
- CH9/SD 是唯一锁存式安全输入：`norm >= 50` 立即触发 ESTOP，满足完整恢复条件后才能清锁
- SBUS 丢失、failsafe、反馈异常和其他软件异常只进行局部停止并自动恢复；只有 CH9 建立全机硬急停锁存

但当前还没有完成：

- CH4 到姿态微调的映射

串口调试命令仍可用于离线调试和手动验证：

| 串口命令 | 行为 |
| --- | --- |
| `8` | 选择全部 8 个腿部电机 |
| `s` | 站立 |
| `T` | 小跑/前进步态测试 |
| `[` | 原地左转 |
| `]` | 原地右转 |
| `x` | 停止 |

## 推荐落地顺序

1. 上电和恢复时保持 CH5 LOW；它是软件待机档，不代替物理断电或硬件急停。
2. 先用 M+M 架空验证四轮方向和零速制动；M+H 只在站立成功后允许机械臂 jog。
3. H+L 为纯点足并主动锁轮；H+M 允许步态与轮驱同步运动；H+H 保持站立并主动锁轮。
4. 架空确认 CH2 前进/后退方向，再验证 CH1 原地转向和 CH1+CH2 弧线混控。
5. 轮驱从 CH3 最低位置开始逐步增加；H+M 中 CH3 同时设置 `0~200 rpm` 轮速上限并选择步态档位，H+L 中只选择步态档位，不参与高度控制。
6. 最后验证 CH2 + CH1 弧线融合及明显前后反打的平稳收步。

## 建议的最终手感

推荐类似遥控车/坦克的操作方式：

- 右摇杆前后：走快/走慢/后退
- 右摇杆左右：左转/右转
- 左摇杆上下：速度档
- 左摇杆左右：姿态微调，不做横移

这样最符合 8DOF 机械能力，也最容易调稳定。
