# 任务赛上位机与下位机通信实现

## 1. 目标与边界

第一阶段目标是让 ROS2 上位机通过 USB CDC 稳定控制 STM32 四足底盘，形成任务赛所需的导航、识别、接近、抓取、运输和放置闭环。

本阶段必须完成：

- 上位机按 `COMMUNICATION_README.md` 以 50 Hz 发送 `0x10 VIRTUAL_RC_SETPOINT`。
- 下位机解析固定 40 字节帧，将 USB 输入接入现有九宫格、MIT 步态和轮驱。
- 在 USB、实体 SBUS 和 NONE 之间安全仲裁，实体 CH9 始终最高优先级。
- 上位机输入异常或 USB 超过 150 ms 没有合法控制帧时停车。
- 不依赖 `0x11` IMU，不假设存在下位机状态回传。

第二阶段在不接入 IMU 的前提下完成砂砾碎木坑、木桥 B 和 T 字形台阶逐级上台及顶部安全停稳；不开发下台阶或动作交接。`0x11` 姿态超限停止/补偿与高墙动作均为可选专项，统一参考 `8DOF轮足四足越障分析.md`，不阻塞第二阶段。

## 2. 事实来源

| 内容 | 来源 |
| --- | --- |
| USB 帧、CRC、字段、session 和频率 | `COMMUNICATION_README.md` |
| 当前固件行为 | STM32 代码 |
| 实体遥控操作和底层调试 | `使用文档.md` |
| 开发阶段与越障目标 | `README.md`、`8DOF轮足四足越障分析.md` |

本文件只定义实现和联调方法。协议出现差异时以 `COMMUNICATION_README.md` 为准。

## 3. 当前实现状态

### 3.1 当前已实现

- UART5 SBUS 接收和 `-100..100` 通道归一化。
- CH5/CH8 九宫格模式和模式进入/阻塞状态机。
- CH9 锁存安全、SBUS 丢失和恢复流程。
- 8 个腿电机 MIT 控制、4 个轮电机控制和轮腿混合步态。
- CH1/CH2 前后与转向、CH3 速度档/轮速；CH6/CH7 当前保留。
- USB CDC 字节收发和 256 字节 RX 环形缓冲/溢出计数。
- 固定 40 字节流解析、CRC、字段校验和错误后重同步。
- `0x10` 控制样本、session、command_counter 和 150 ms 看门狗。
- SBUS/USB/NONE 仲裁和独立 USB IDLE 映射。
- Python codec、50 Hz 串口链路、CLI 和固定向量测试。

### 3.2 尚未完成

- `0x11` 业务解码与姿态保护为可选 IMU 专项，不阻塞无 IMU 障碍动作；当前固件只校验并忽略合法帧。
- 双向状态回传、软件急停帧和正式 ACQUIRE/RELEASE。
- 真实 USB 设备、实体 SBUS 授权和架空电机联调。

### 3.3 已处理的 USB 字节分流

当前链路已经改为：

```text
CDC_Receive_HS()
  -> 256 字节 s_cdc_rx_ring
  -> CDC_GetByte_HS()
  -> UsbFrameProtocol_FeedByte()
       |-- 合法 0x10 -> VirtualRcSample
       |-- 合法 0x11 -> 第一阶段及第二阶段第一步计数并丢弃
       `-- 独立 ASCII p/Y -> 只读诊断
```

合法二进制 payload 和 CRC 不会再次进入 `handle_command()`。正式控制版本默认关闭上电帮助、周期文本和 CAN 原始日志，只保留独立的只读 `p` 与 `Y/y` 人工诊断入口；自动任务不依赖其文本结果。

## 4. 总体结构

```text
上位机
  mission_manager
    |-- Nav2 /cmd_vel_nav
    |-- vision /cmd_vel_visual
    |-- stop/cancel
    v
  motion_command_mux
    v
  quadruped_usb_bridge
    `-- 0x10 @ 50 Hz
          |
          v
下位机
  USB 40 字节解析器
    -> VirtualRcSample
    -> SBUS/USB/NONE 仲裁
    -> RemoteControlView
    -> 九宫格 / MIT 步态 / 轮驱
```

上位机只有 `quadruped_usb_bridge` 可以打开四足 USB CDC。Nav2、视觉、UI 和任务状态机不得直接写 COM 口。

## 5. 上位机实现

当前上位机最小实现位于：

```text
host/quadruped_protocol.py
host/quadruped_link.py
host/quadruped_control_cli.py
```

### 5.1 ROS2 节点

| 节点 | 职责 | 优先级 |
| --- | --- | --- |
| `quadruped_usb_bridge` | USB 枚举、`0x10` 编码、50 Hz 调度和断线清零 | P0 |
| `motion_command_mux` | 停止、视觉和 Nav2 速度仲裁 | P0 |
| `mission_manager` | 任务状态机、deadline、重试和取消 | P0 |
| `safety_supervisor` | 定位、围栏、节点和命令新鲜度 | P0 |
| `localization_manager` | 雷达里程计、TF 和比赛坐标系 | P0 |
| `navigation_node` | Nav2 goal 和大范围移动 | P0 |
| `object_detector` | 物资箱识别 | P1 |
| `exchange_detector` | 归位区识别 | P1 |
| `visual_servo` | 前后和 yaw 低速对准 | P1 |
| `math_solver` | 20 秒智力题 | P1 |
| `operator_ui` | 一键启动、取消和观察 | P2 |

### 5.2 USB 设备

```text
VID      0x0483
PID      0x5740
Product  STM32 Virtual ComPort
```

按 VID/PID 枚举，多个候选时使用 USB serial number，不保存易变化的 COM 号。端口按 `115200 8N1` 打开；打开成功只表示可以发送安全零帧，不代表下位机已经确认控制。

### 5.3 控制流

1. 每次连接生成随机非零 `session_id`。
2. command_counter 递增。
3. 先发送至少 3 帧安全零帧。
4. 持续发送目标模式的零轴帧。
5. 只有命令新鲜、任务许可和安全条件均满足时才发送非零轴。
6. 断线或重连后生成新 session，不恢复旧输入。

v1 没有下位机状态响应，UI 只能显示“USB 已打开”“控制流发送中”或“本地已停止”，不得显示“下位机已确认模式”。

### 5.4 50 Hz 调度

```text
deadline += 20 ms
snapshot = latest command

if USB closed or no session:
    do not write
elif command stale or safety denied:
    send zero axes; request SMOOTH_STOP when needed
else:
    send requested mode and axes

if one period missed:
    discard old period
```

周期帧使用最新值覆盖，不能排队积累。关闭应用前尽力发送 3 帧安全零帧，最终停止保障由下位机 150 ms 看门狗提供。

### 5.5 命令模型

```python
@dataclass(frozen=True)
class MotionCommand:
    mode: int
    forward: float       # -1..1
    yaw: float           # -1..1
    speed_axis: float    # -1..1
    motion_enable: bool = False
    deadman: bool = False
    smooth_stop: bool = False
    stamp_monotonic: float = 0.0
```

业务层和 codec 边界都要限幅。8DOF 结构不支持真实横移，收到非零 `linear.y` 时拒绝命令并停车。

### 5.6 速度仲裁

```text
STOP / 本地故障
  > 视觉低速对准
  > Nav2
  > 零输入
```

| 输入 | 新鲜度上限 | 超时行为 |
| --- | ---: | --- |
| Nav2 | 100 ms | 零轴 + SMOOTH_STOP |
| 视觉 | 100 ms | 零轴并退出对准 |
| 任务许可 | 100 ms | 清 deadman 和 motion_enable |
| 定位 | 200 ms | 取消导航并停车 |

自主 deadman 是由任务状态机周期刷新的短租约，不允许永久置为 true。

### 5.7 Twist 映射

```text
forward = clamp(linear.x / max_linear_mps, -1, 1)
yaw     = clamp(angular.z / max_yaw_rps, -1, 1)
```

初始建议：

```yaml
max_linear_mps: 0.40
max_yaw_rps: 0.60
speed_axis: -0.80
```

空场连续测试后再逐步提高速度。

## 6. 下位机实现

### 6.1 建议新增文件

```text
User_File/BSP/USB/usb_frame_protocol.h
User_File/BSP/USB/usb_frame_protocol.cpp
User_File/task/remote_control_view.h
User_File/task/remote_control_view.cpp
```

当前 CMake 显式列出源码，新 `.cpp` 必须加入 `CMakeLists.txt`。

### 6.2 USB 接收层

- 将 RX 环形缓冲从 128 字节扩大到至少 256 字节。
- 增加 `rx_ring_drop_count`。
- 在控制任务中排空字节并优先喂给二进制解析器。
- 支持拆包、粘包、噪声和 CRC 错误后重同步。
- 第一阶段和第二阶段第一步对合法 `0x11` 计数后丢弃，不进入 ASCII 命令。
- 解析器空闲且确认收到独立 ASCII 字节时，仅把 `p`、`Y` 交给只读诊断处理；小写 `y` 可作为兼容别名。

协议层只发布最新不可变样本，不直接驱动电机。

### 6.3 通道转换

USB 通道是 `-1000..1000`，当前 SBUS 内部是 `-100..100`：

```text
norm100 = round(clamp(permille, -1000, 1000) / 10)
```

| USB | 当前控制输入 |
| --- | --- |
| yaw/CH1 | `norm[0]` |
| forward/CH2 | `norm[1]` |
| speed/CH3 | `norm[2]` |
| arm J0/CH6 | `norm[5]` |
| arm J1/CH7 | `norm[6]` |

README 的 `-800/0/800` 转换成 `-80/0/80`，可进入当前 LOW/MID/HIGH 速度档。

### 6.4 USB 模式 0

当前实体 LOW+LOW 对应 MOTOR_CHECK/恢复准备，而协议模式 0 是安全 IDLE。USB 不能直接把 `virtual_mode` 强转成当前枚举。

USB 模式 0 只允许：

- 连续轴归零。
- 步态停止或收步。
- 轮驱 OFF/HOLD。
- 腿部安全保持。

不得自动执行故障清除、电机探测或实体 MOTOR_CHECK 行为。

### 6.5 统一控制视图

```cpp
struct RemoteControlView {
    int16_t norm[7];
    uint8_t main_switch;
    uint8_t sub_switch;
    uint8_t deadman;
    uint8_t motion_enable;
    uint8_t smooth_stop;
    uint8_t source;       // NONE / SBUS / USB
    uint32_t sample_tick_ms;
};
```

现有轮驱和步态函数应读取统一视图或安全适配层。不得把 USB 伪造成 25 字节 SBUS 帧，也不得改写 `SbusState`。

### 6.6 控制任务顺序

```text
1. Sbus_Process()
2. 排空 CDC RX，解析二进制帧
3. 发布最新 VirtualRcSample
4. 处理实体 CH9 和安全锁存
5. 选择 SBUS / USB / NONE
6. 构造 RemoteControlView
7. 执行模式、步态和轮驱
8. 执行 USB/SBUS 看门狗
9. 低优先级刷新受限诊断
```

### 6.7 控制仲裁

```text
实体 CH9/安全锁存
  > 新鲜且被操作的实体 SBUS
  > 合法且新鲜的 USB
  > NONE 安全保持
```

USB 成为候选必须满足：

- SBUS 新鲜且无 failsafe。
- CH9 释放，安全未锁存。
- 实体 LOW+LOW，CH1/CH2/CH6/CH7 回中。
- 已通过安全零帧锁定非零 session。
- command_counter 前进，最近控制帧不超过 150 ms。

实体模式离开 LOW+LOW、摇杆动作、CH9 触发或 SBUS failsafe 时，当周期撤销 USB 非零输入。

### 6.8 看门狗

超过 150 ms 没有合法 `0x10`：

1. CH1/CH2/CH6/CH7 归零。
2. 请求步态平稳收步。
3. 轮驱 HOLD，不能保留最后转速。
4. source 切为 NONE。
5. 清 session，不自动恢复旧输入。

## 7. 第一阶段任务状态机

```text
BOOT
 -> WAIT_READY
 -> MATH_SOLVE
 -> START_CONTROL_STREAM
 -> SELECT_BLOCK
 -> NAV_BLOCK_STAGING
 -> DETECT_BLOCK
 -> VISUAL_ALIGN_BLOCK
 -> GRASP
 -> VERIFY_GRASP
 -> NAV_EXCHANGE_STAGING
 -> DETECT_EXCHANGE
 -> VISUAL_ALIGN_EXCHANGE
 -> PLACE
 -> VERIFY_PLACE
 -> SELECT_BLOCK ...
 -> MISSION_COMPLETE

任意运动状态 -> SAFE_STOP -> FAULT
```

每个状态必须有单调时钟 deadline、有限重试和取消处理。禁止没有退出条件的循环。

第一阶段模式建议只开放：

| 模式 | 用途 |
| --- | --- |
| IDLE | 启动、断线、暂停 |
| STAND_HOLD | 等待、识别、抓放 |
| STAND_WHEEL | 平整场地导航和视觉对准 |

GAIT_WHEEL、LOW_WHEEL 和障碍模式在底盘通信稳定后再开放。

## 8. 导航、视觉和智力题

### 8.1 定位与导航

```text
match -> odom -> base_link -> lidar/camera
```

一键启动时建立 `match -> odom`，比赛点、围栏和 Nav2 goal 使用 `match`。Nav2 使用非完整约束底盘控制器，不输出 `linear.y`；目标物资箱前使用 staging pose。

### 8.2 视觉低速对准

只输出前后和 yaw。目标丢失超过 100 ms 立即停车，连续丢失超过 1 s 取消对准。成功必须连续稳定，不能单帧命中。

### 8.3 智力题

- deadline 20 s。
- 只接受整数和 `+ - * / ( )`。
- 禁止使用 `eval`。
- 到期后记录失败并继续任务。

## 9. 安全要求

- 实体 CH9 和物理断电始终是最终安全措施。
- v1 安全零帧和看门狗用于停车，不等于软件急停锁存。
- USB、Nav2、视觉、定位或任务节点异常时，上位机停止非零输出。
- 下位机任何时候都不能让 USB 释放 CH9 或绕过安全锁存。
- 第一阶段新增代码不能削弱已有电机反馈、过温、CAN 和局部故障保护。

## 10. 实现顺序

1. 完成 codec 固定向量、CRC、拆包和粘包测试。
2. 完成下位机解析器，确认二进制字节不进入历史命令。
3. 完成 USB bridge 50 Hz 零帧和下位机 150 ms 看门狗。
4. 引入 RemoteControlView 和 SBUS/USB/NONE 仲裁。
5. 架空验证 STAND_HOLD、STAND_WHEEL、前后和转向。
6. 完成 command mux、输入超时和 safety supervisor。
7. 接入 Nav2，再接视觉低速对准。
8. 接入智力题和完整任务状态机；新增执行机构必须另行定义硬件与协议版本。

## 11. 最小联调命令

安装依赖：

```powershell
python -m pip install -r host/requirements.txt
```

只读查询：

```powershell
python -m host.quadruped_control_cli COM7 --diag p
python -m host.quadruped_control_cli COM7 --diag Y
```

只请求站立保持，不发送非零运动轴：

```powershell
python -m host.quadruped_control_cli COM7 --mode STAND_HOLD --enable --duration 2
```

架空、CH9 旁有人值守并确认方向后，才允许低速轮行测试：

```powershell
python -m host.quadruped_control_cli COM7 --mode STAND_WHEEL --forward 0.10 --speed -0.80 --enable --deadman --duration 1
```

未同时传入 `--enable` 和 `--deadman` 时，codec 会把连续运动轴清零。命令结束、Ctrl+C 或正常关闭时会发送安全零帧。

## 12. 验收

### 12.1 协议与固件

- 固定向量 CRC 为 `0x96CF`，线上为 `CF 96`。
- `0x10` 保持 `50 Hz +/- 2 Hz`，连续 10 分钟无 RX drop。
- 拆包、粘包、噪声和错误帧后能够恢复。
- payload 中出现历史命令字符不会触发 `handle_command()`。
- 二进制控制运行期间按需发送独立 `p/Y` 能得到一次诊断响应，且不改变控制状态。
- 不发送 `0x11` 时全部任务赛控制功能正常。

### 12.2 仲裁与故障

- 松开任务许可后的下一帧为零轴。
- USB 拔线或 bridge 退出后，下位机 150 ms 内停车。
- Nav2、视觉、mission 或 UI 退出时不保留非零命令。
- 实体模式、摇杆和 CH9 可立即撤销 USB 控制。
- SBUS failsafe 时 USB 不能继续运动。

### 12.3 任务赛

- 先完成单物资箱导航、对准、抓取、运输和放置闭环。
- 再进行多物资箱连续运行。
- 每个状态都有 deadline、有限重试和可解释日志。
- 任一通信或节点故障后进入 SAFE_STOP，不自动恢复旧动作。

第一阶段完成标准不是“串口能发帧”，而是机器人能在无人工底盘操控的情况下完成任务赛运动流程，并在单点故障后停止。
